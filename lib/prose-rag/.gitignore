# uv venv (recreate with `uv sync`); never commit
.venv/
__pycache__/
*.pyc
# any local index built inside the tool dir (default lives in ~/.claude/prose-rag/)
*.db
# rust build output + the deployed engine binary (build with: cargo build --release)
rust/target/
bin/prose-rag-rs
