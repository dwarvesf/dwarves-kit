# Task Backlog

> **`_meta/` charter.** This folder holds live, mutable, maintainer-facing project
> *state*: trackers, queues, and manifests of work. Durable reference and the design
> record live in `docs/` (specs, decisions, handoff, retro, research). Today the only
> artifact meeting that bar is this backlog, and that is expected, not a gap.
>
> **Do not add** to `_meta/`: a `ROADMAP` (the v2-candidates + parking-lot tiers below
> are the roadmap), a `SCHEMAS` file (the backlog schema lives in this file and in
> `docs/architecture.md`; schemas belong next to their artifact), a `LAB_LOG` (use
> `docs/handoff/` + `docs/retro/`), an ADR index (`docs/decisions/` self-indexes by
> number), or `checks/`/`infra/` (that is `tests/` + `.github/`). An `INVENTORY`
> manifest is allowed *only* if it becomes the single source `tests/test-meta.sh` and
> the README derive from, never a hand-maintained fourth copy. Resist the junk drawer.

The kit's single source of truth for "what's left to do." Three tiers, decreasing commitment:

1. **Active queue** - committed work, each with a stable `ID-NNN` id and a status. This is what `/user:start` and `/user:next` read when you ask "what's left?". Shipped items drop off the queue (the CHANGELOG is the canonical "what shipped" record).
2. **v2 candidates** - not committed; become real only after a spec is written and `/spec-validate` passes. Listed for visibility.
3. **Parking lot** - revisit only if a real usage signal arrives.

Completed cycles live in `docs/handoff/v<version>.md` (build notes) and `docs/retro/v<version>.md` (cycle retros). Per-release CHANGELOG entries are the canonical "what shipped" record. Completed work is NOT mirrored back here, to avoid drift.

> Schema note: the column + status-vocabulary contract is formalized in the **[Schema](#schema)** section below (SPEC-005 / ID-006). The session-start rendering of this queue is wired in SPEC-006.

## Active queue

Status vocabulary: `queued` (committed, no spec yet) -> `claimed` (pulled by a session via `/kit:assign --next`; the cross-session claim lives in `lib/goal-registry.sh`, this records the board state; render/flip mechanically with `lib/backlog.sh board|next|set`) -> `speccing` (spec being drafted) -> `validated` (spec passed `/spec-validate`) -> `executing` (in build) -> `shipped` (drops off, see CHANGELOG). Off-ramps: `parked` (a drafted spec deliberately set aside; the spec holds its own revisit note) or `dropped` (deliberately cleared from the active path during a re-evaluation; a one-line reason in the Status cell; the ID retires and is never reused).

Lane = the WORKFLOW.md risk tier (`tiny` / `normal` / `full`).

| ID | Title | Source | Target artifact | Lane | Status |
|----|-------|--------|-----------------|------|--------|
| ID-084 | **DAG wavefront scheduling in the orchestrator.** Run dep-independent sub-goals concurrently: ready-set from the (already-parsed) `depends` tokens, one worktree per concurrent `claude -p` session, dispatch-gate prove-or-serialize across each wave, per-edge `HANDOFF-<id>.md` (replaces the single HOT file for parallel paths), flock-guarded box flips, cap N=2. A ~150-200-line wavefront extension of `lib/orchestrate.sh`, deliberately NOT the deferred GSD-v2 engine (no priority/cross-machine/recovery-store). Needs a mini-ADR first (narrowly re-opens ADR-0028's DAG deferral). Full design + exit criteria (5 negative controls incl. linear-chain regression): `docs/specs/DECISION-BRIEF-dag-wavefront.md`. Run AFTER kit-telemetry ships | operator ask 2026-07-02 + orchestrate.sh code audit | `lib/orchestrate.sh` + `lib/dispatch-gate.sh` reuse + tests | full | queued |
| ID-083 | **mega-merge PR-routing defense-in-depth (gate-tagged / final-PR exclusion).** TIER-4 security review LOW finding on the assembled `mega/kit-hardening` branch: the mega-merge PR-routing rule (excluding gate-tagged / final PRs from auto-merge) is enforced only at the PROMPT level in `commands/mega.md`; there is no code-level check in `lib/mega-merge.sh`. Deferred (not blocking, LOW severity) rather than fixed in the TIER-4 close-gate pass. Fix: add a defense-in-depth PR label/state cross-check in `lib/mega-merge.sh` so the exclusion holds even if the prompt-level instruction is not followed | TIER-4 security review (2026-07-02, mega/kit-hardening close-gate audit) | `lib/mega-merge.sh` | TBD | queued |
| ID-082 | **Gate-ledger durability + per-gate override reasons (ban blanket overrides).** Two defects from the 2026-07-02 process audit: (1) the 2026-07-01 plugin reinstall recreated `~/.claude/dwarves-kit` and WIPED all prior run-ledger records, so telemetry cannot survive to feed `/kit:retro`; (2) the sole surviving run record overrode all 11 gates with one identical pasted reason at one timestamp, defeating the per-gate audit trail. Fix: durable ledger storage outside the plugin-reinstall blast zone (+ migration note), require a per-gate override reason (reject N identical reasons stamped in one call, or force per-phase entry), keep `lane-telemetry.sh` reading the new path. Candidate follow-on to the kit-hardening mega-goal (its NOTES tracks this as candidate-09); NOT in the ADR-0028 blessed wave, so it runs its own cycle. URGENCY BUMP 2026-07-02: the 8 kit-harden run ledgers (the first real telemetry corpus, the data ID-067/SPEC-073 needs) now sit in the wipeable dir; do this before any plugin reinstall or copy the logs out | ops-toolkit `research/2026-07-02-process-effectiveness-audit.md` (R1) | `lib/gate-ledger.sh` + `lib/proof-ledger.sh` storage + `lib/lane-telemetry.sh` | TBD | queued |
| ID-081 | **Intermittent test-hooks flake, ~1/10 runs, nameless.** Twice observed (2026-06-11, wave-1 review loop and SPEC-077 finalize): suite reports Failed: 1 with no FAIL line captured; immediate rerun green; 4-11 consecutive runs green when hunted. Suspect a tmpdir/race in one fixture. Next sighting: capture the full output to a file BEFORE rerunning, diff the PASS list against a green run to name the silent assertion | live flake, 4 sightings | TBD | bug | shipped [FIXED 2026-06-11: root cause was NOT load, BSD script(1) tcgetattr on a SOCKET stdin (agent/CI harness) fails ENOTSUP and script exits before the child runs; fix = explicit </dev/null stdin + read the typescript file; NC: color gate broken -> exactly 1 RED -> restored; NAMED 2026-06-11 sightings 3+4: "colors: PTY progress emits escape bytes" tests/test-hooks.sh:256; capture docs/research/2026-06-11-id081-flake-capture.txt; hypothesis: BSD script(1) PTY-drain race loses child output before od counts escapes; candidate fix = bounded retry x3 keeping the NC falsifiable (broken gate fails all retries)] |
| ID-075 | **Absorb: anchored-confidence merge for review-team.** Reviewer JSON contract (5 behavioral confidence anchors with self-tests), fingerprint dedup (file + line-bucket +-3 + normalized title), cross-reviewer corroboration promotion, LATE <75 confidence gate (P0 at 50+); report gains a Confidence column | absorption 2026-06-11 #1, 15/16 (EveryInc ce-code-review Stage 5 @4719dc5, MIT) | TBD | full | shipped   |
| ID-076 | **Absorb: apply-class routing for review findings.** Route column per finding: gated_auto (concrete fix -> responding-to-review) / manual (design input -> board row) / advisory (spec ## Review only); owner + requires_verification fields; severity = urgency, class = follow-up shape | absorption 2026-06-11 #2, 15/16 (EveryInc action-class rubric; upstream deprecated safe_auto) | TBD | normal | shipped   |
| ID-077 | **Absorb: verify-this delta into /kit:verify.** Claim restated falsifiably (condition + metric + threshold) BEFORE measuring; baseline-vs-treatment artifact pair for comparative claims; INCONCLUSIVE as a legal verdict (proof-ledger untouched: INCONCLUSIVE is not a pass) | absorption 2026-06-11 #3, 14/16 (cursor verify-this @74dd229, MIT) | TBD | normal | shipped   |
| ID-078 | **Absorb: model tiering on review-team dispatch.** Security lens inherits the session model; architecture + test-coverage dispatch mid-tier; one fallback sentence. Halves the 3x token self-description | absorption 2026-06-11 #4, 14/16 (EveryInc Stage 4) | TBD | tiny | shipped   |
| ID-079 | **Absorb: per-finding validator wave (scaled).** One independent read-only validator per VERDICT-DRIVING finding (not batched); P0/P1 mandatory; validator infra failure never drops a P0/P1 | absorption 2026-06-11 #5, 13/16 (EveryInc Stage 5b) | TBD | normal | shipped   |
| ID-080 | **Absorb: two architecture-lens tripwires.** (1) a PR must not push a file past 1k lines without a stated reason; (2) weird-ifs-in-random-places = design problem, not a nit; appended to Reviewer 2 prompt | absorption 2026-06-11 #6, 13/16 (cursor thermo-nuclear, 2 rules only) | TBD | tiny | shipped   |
| ID-074 | **A 12th task type: review.** Standalone review intent (review this PR adversarially / multi-lens review the diff / audit branch for security) has NO route: all 3 real phrasings fall to spec-feature while /kit:review[-team] sits unrouted from intake. Decide the taxonomy addition , ALL SPEC-057 parity surfaces: classifier rule + anchors, registry row (artifact/skill/class/agent), loops-table row, test-design-standard 5b dialect row, parity pin 11->12 in test-meta, assign.md type list; the 3 phrasings are the ready-made truth-table rows; HIGH per the ID-065 trace | use-case path audit 2026-06-11 (docs/research/2026-06-11-use-case-path-audit.md, misfires 2-4) | TBD | full | shipped   |
| ID-073 | **stack-merge: chain resume skips the reconcile step.** Both live failures in the wave-1 merge (2026-06-11): when a chain is resumed after a blocked link, the child PR is already retargeted, and the script proceeds to squash-merge the child WITHOUT the merge -X ours reconcile, hitting GraphQL merge-conflicts; the reconcile also silently failed to commit on the first chain run. Make reconcile unconditional per link (keyed to branch state, not to the retarget having just happened) + pin with a resume fixture | live miss 2026-06-11, wave-1 stack-merge of #55..#58 (manual -X ours by SHA recovered both links) | TBD | bug | shipped   |
| ID-072 | **START amend path.** gate-ledger START lines are append-only with no correction verb; a mis-recorded lane/type at intake can only be patched by a second START, which trips the MULTI-START flag and leaves telemetry counting the wrong first line. Add an amend/correct verb (or make trace/report honor a CORRECTION marker) so an honest fix does not read as a misfire | live miss 2026-06-11 (wave-1 PR B intake: lane recorded bug, kit-machinery hard-gate says full; corrected via second START + action note) | TBD | normal | shipped   |
| ID-063 | ship-gate: SPEC-069 boardless advisory is unreachable for spec-less pushes. `[ -n "$SPEC" ] || exit 0` sits above the advisory block, so every freeform (no-spec) branch exits before the "give it a board row" nudge fires; exactly the work most likely to be un-boarded. Move the advisory above the spec check + pin with a spec-less-push test | live miss 2026-06-10: ops-toolkit economics-track (PR #184) shipped un-boarded, advisory silent; renumbered from ID-060 (collision with merged #53) | TBD | tiny | shipped   |
| ID-064 | lane-classify: markdown-only bootstrap work falls to the `normal` default (live probe 2026-06-10: "bootstrap a learning track folder with README, notes and reading list (markdown only)" -> normal). Add docs-lane anchors from the real phrasing + truth-table rows, per the SPEC-060 narrow-anchor method | live probe 2026-06-10, ops-toolkit economics-track session; renumbered from ID-061 (collision with merged #53) | TBD | tiny | shipped   |
| ID-065 | **Use-case path audit: research loop, autoreview loop, build-experiment loop.** Trace the three real loop shapes end to end through classify + lane routing; record which path each takes, compare output quality against the operator skills covering the same ground today; every misfire becomes a truth-table row per the SPEC-060 method | operator intake 2026-06-10 (item 0) | TBD | normal | shipped   |
| ID-066 | **Lane-path combination audit + wiring.** Enumerate the full lane x type x phase matrix (every reachable path), wire every undefined cell, pin the coverage so no combination can be skipped or forgotten | operator intake 2026-06-10 (item 1) | TBD | full | shipped   |
| ID-067 | **Telemetry + feedback-loop + proof-of-done effectiveness eval: design now, run later.** Write the evaluation design + criteria immediately; execute only after 3-5 days of real-usage data per the 2026-06-10 retro decision. UNPARKED 2026-07-02: the precondition is met , the kit-hardening run wrote 8 per-sub-goal gate-ledger runs (`logs/runs/kit-harden-*`) + captured-evidence proofs, the first real telemetry corpus; and ops-toolkit `research/2026-07-02-process-effectiveness-audit.md` already supplies the 60-day proof-effectiveness evidence (CONSUME it, do not redo that half). ORDER: do ID-082 first (or copy the logs out) , the corpus sits in the plugin-reinstall-wipeable dir. Overlaps ops-toolkit ID-149 (lane-classify rule audit); run together | operator intake 2026-06-10 (item 2), design SPEC-073 | SPEC-073 | normal | validated   |
| ID-068 | **Full V-model, two arms, review-gated descent on EVERY lane.** Each left-arm step (intent -> spec -> design -> build) passes a review lens + feedback loop before descending; the right arm verifies level for level. Operator decision 2026-06-10: mandatory on ALL lanes including tiny; scale the lens weight to the lane, never waive the gate | operator intake 2026-06-10 (item 4) | TBD | full | shipped   |
| ID-069 | **Absorb from the two pinned kits.** EveryInc/compound-engineering + cursor/cursor-team-kit; prizes already scouted: verify-this evidence gate, parallel-persona JSON merge, apply-class | operator intake 2026-06-10 (item 6); seeds pinned in ops-toolkit repo memory | TBD | normal | shipped   |
| ID-070 | **Doc sync wave: README + WORKFLOW + AGENTS.** Sweep the three operator-facing docs to match reality after the 2026-06-10 intake wave ships | operator intake 2026-06-10 (item 7); depends on ID-065..069 | TBD | normal | shipped [SPEC-085 2026-06-11: README counts fixed (hooks 16, commands 25) + 4 missing rows (incl ship-gate) + 6 stale descriptions refreshed; 2 computed parity pins added so README tables cannot drift again; AGENTS audited clean] |
| ID-060 | **Doc loop second entry path.** WORKFLOW.md `## Type loops` defines the doc loop as "diff sweep -> update every affected doc -> doc-verifier", which only covers code-diff-triggered doc sync. Add the standalone-revision entry path (content brief -> rewrite -> doc-verifier) so a doc rewrite with no code diff has a described route to the same exit gate | this session 2026-06-10 (README closed-loop reframe ran as a doc task with no code diff; loop entry undefined, doc-verifier exit still applied and caught 4 overclaims) | TBD | tiny | shipped   |
| ID-061 | **proof-gate class upgrade false positive on "behavior" keyword.** `proof-gate.sh contract` returned `class=behavioral` for a doc task whose text contained "no behavior change" (registry default for `doc` is `inert`). Confirm the keyword trigger, make the upgrade match negation/word-boundary aware (or floor doc-type at its registry default), add a regression test case | this session 2026-06-10 (README reframe; contract output `behavioral` vs registry `inert`) | TBD | bug | shipped   |
| ID-062 | **Ship warn when a behavioral run opens a PR without a verification record.** /kit:ship Step 8 (or the ship-gate hook) warns when the run's proof class is behavioral/stateful and no `docs/verification/<slug>.md` rides the branch; warn-not-block per Detect-don't-dictate. The `.claude/debug/` ledger is gitignored by design, so without this nudge the acceptance evidence dies with the session | this session 2026-06-10 (CI-fix bug lane: green runs + negative control existed but the committable record was skipped until the operator asked) | TBD | normal | shipped   |
| ID-049 | **/kit:grill universal intake interview.** Type-shaped one-question-at-a-time requirements grilling between classify and Done=, write-as-you-go (glossary/ADR/goal-draft Context). Source: mattpocock grill-with-docs. | maintainer 2026-06-10 (the old grill step vanished from the new flow) | SPEC-058 | full | shipped [CHANGELOG [Unreleased]; SPEC-058; review SHIP after fixes] [build complete, review pending] [pulled via --next flow]    |
| ID-050 | progress checklist visual order: ✓ marks can appear past the ▶ pointer (faithful to the ledger but misleading at a glance; sort-by-plan-order display option) | shipped [from SPEC-063 review F4, by-design today]   |
| ID-051 | Hook precision: safety-gate + ship-gate parse the command's real argv instead of string-matching the whole text (heredoc bodies, prose, quoted args immune); cd-prefix-aware engagement. Every 2026-06-10 false positive (the word master in a merge-by-SHA, a destructive-delete literal inside a heredoc, push+base-edit compound) becomes a test | shipped [PR pending] [wave approved in-session 2026-06-10; run spec-064 in flight; 5th false positive: the gate fired on this very row's prose while enqueuing]  |
| ID-052 | lib/spec-next.sh: collision-proof next SPEC number (scans docs/specs/ + remote branches); two renumber incidents this week | shipped [PR pending] [rides spec-064 with ID-051]  |
| ID-053 | Stack-merge codification: lib/stack-merge.sh wrapping the proven retarget-child -> merge-parent -> merge -X ours by SHA sequence; ship prose points at it | shipped [PR #44] [run spec-065] [wave 2026-06-10]   |
| ID-054 | Kit distribution install-by-copy: hooks deploy as copies + version stamp instead of symlinks that follow the checkout branch (live regression observed 2026-06-08); upgrade path documented | shipped [PR #45] [run spec-066] [wave 2026-06-10]   |
| ID-055 | Golden-run e2e harness: tests/test-e2e.sh builds a temp repo and walks assign -> ship asserting ledger + telemetry + progress agree end to end | shipped [PR #46] [run spec-067] [wave 2026-06-10]   |
| ID-056 | Precedent lookup at intake: lib/precedent.sh greps specs/retros/ledgers for similar past work at assign/grill time (second-brain feed-forward) | shipped [PR #47] [run spec-068] [wave 2026-06-10]   |
| ID-057 | task-type-classify: bare cli anchor steals feature-work-ON-a-cli (found by the SPEC-067 golden run: add a --version flag to the demo CLI -> data-tool); narrow to build/wrap/port-a-CLI phrasings + truth-table rows | shipped [disposition of a live misfire, 2026-06-10]   |
| ID-058 | Quality follow-ups from the operator's 5-question retro: boardless-run + shipped-incomplete detectors in telemetry, ship-gate advisory line, review-team escalation rule for lib/hooks runs, codebase-memory line in grill orientation, TTY-gated colors for plan/progress/trace, retro-patch the spec-064 think gap | shipped [PR #48] [run spec-069] [retro disposition 2026-06-10]   |
| ID-059 | rid standardization: rid = branch slug everywhere (assign derives it at START; ship-gate mirrors disappear; untracked metric becomes honest). Worst friction of the quality wave: 5 mirror-record dances in one day | shipped [retro 2026-06-10, top priority]   |
| ID-047 | **Research: prior art for pull-based agent kanban + dynamic persona dispatch.** Survey OpenClaw Workboard, GSD v2 runtime, Claude Code native task/agent surfaces, and the kit's own registry/dispatch, so the next phase (autonomous pull + persona selection) builds on evidence. Cited report into docs/research/. | mega-goal kit-north-star sub-goal 05 (2026-06-10) | docs/research/2026-06-10-pull-kanban-prior-art.md | normal | shipped [report docs/research/2026-06-10-pull-kanban-prior-art.md; proof docs/verification/pull-kanban-prior-art/] [pulled via assign --next flow, claim ID-047-pull-kanban-prior-art]   |
| **I1 -- operating manual / intake** | | | | | |
| ID-033 | **I1: operating-manual integration.** Re-evaluate `MANUAL.md` and fold the `/goal` loop + the orchestration layer (`WORKFLOW.md` / `docs/ORCHESTRATION.md` / `docs/operating-layer-vision.md`) into one coherent operating manual, so the manual reflects how the kit is actually driven (interpret-and-bridge + the state machine + the goal loop), not a flat command list | this session 2026-05-22 re-eval (maintainer pivot, initiative 1) | TBD | normal | shipped [SPEC-083 merged-pending-PR 2026-06-11; wire audit: hops 2-5 already wired, hop 1 (context-readiness) fixed: board:Nq token + intent-first suggestions; re-eval 2026-05-23 REVISED, drop reversed: maintainer feedback this session is that the operating docs (MANUAL/AGENTS/README) and the assistant's own responses still read as a command catalog the user must learn, not a talk-to-it interface; the folded-docs evidence stands, but ID-033's real intent (drive the kit by stating intent, not by memorizing slash commands) is unmet. Re-scoped from "fold into one manual" to "make the orchestration layer the user-facing interface, docs included"; relates to ID-036 layering; now on branch docs/kit-as-interface; 2026-06-10 intake item 5 folded in: the orchestration layer must be the kit's verified ENTRY POINT, check the wire from session start through orchestration to commands] |
| **I2 -- V-model + parallel + verification arm** | | | | | |
| ID-037 | **Mega-goal lane (sequenced multi-goal decomposition).** A planning-only `/kit:mega` that decomposes one destination into 3-8 dependent sub-goals + a committed roadmap + a pointer that drives the bounded `/goal` loop to ship them one PR at a time. The sequential/dependent complement to ID-035's parallel/independent dispatch; gsd-2 is the DAG case. No PHILOSOPHY amendment (it is the blessed bounded in-session loop, not the rejected outer loop); supersedes the abandoned ADR-0017. Synthesized from plan-for-mega-goal (`zvadaadam/az-skills`); the plan-for-goal analog already ships as `/kit:assign` | this session 2026-05-22 (plan-for-mega-goal study) | SPEC-034 | full | validated [re-eval 2026-05-23: kept, /kit:mega is not built (no commands/mega.md; CHANGELOG marks SPEC-034 "still queued"); it is the sequential/dependent complement to the parallel /kit:dispatch (SPEC-032), not superseded by it] **2026-06-27 安全net revisit: đây là gap I2 còn sót lớn nhất sau đợt ship.** I2 (V-model + parallel + verification arm) gần như shipped hết (ID-034/035/038/039/020-pre), chỉ còn ID-037 `/kit:mega` chưa build + ID-020/ID-021 verifier blind spots. Khi picked up: write commands/mega.md + lib/mega-decompose.sh + ship 1 real mega-goal (ops-toolkit dùng `plan-for-mega-goal` skill manually rồi, có runway). Note: `plan-for-mega-goal` skill (ops-toolkit ID-209) là reference doc lớn (33KB), khi build /kit:mega thì gộp luôn vào thin-trigger + GUIDE.md split để tránh duplicate token weight. **2026-07-02 shipped** [SPEC-096, kit-hardening SG-08, branch feat/kit-harden-08-megamirror]: `commands/mega.md` built (mirrors the ops-toolkit skill's decompose + front-load-checkpoint + per-run-merge-config beats, reuses this row's SPEC-034 roadmap conventions); `lib/mega-merge.sh` adds the ship-layer auto-merge enforcement ADR-0028 P3 calls for (rides `lib/gate-ledger.sh check`, dry-run default, load-bearing negative control). Proof: `docs/verification/mega-reconcile.md`. |
| ID-024 | Context-switch affordance across specs (worktree-per-spec) + an explicit ABANDONED terminal (state hygiene; vs the existing `parked`) | operating-layer-vision gap analysis 2026-05-22 | TBD | normal | dropped (both named deliverables landed: the worktree-per-spec context-switch shipped via /kit:dispatch (SPEC-032), and the explicit ABANDONED terminal is now a state in the docs/architecture.md SDLC state machine; [re-eval 2026-05-23]) |
| ID-035 | **I2: first-class cross-goal parallel execution.** Fan out N independent (disjoint-file) specs into isolated worktrees, backed by the worktree-per-spec isolation model. (Originally scoped as intra-`/kit:execute` task parallelism; reframed during SPEC-032 to cross-goal `/kit:dispatch`, with intra-`/execute` parallelism deferred.) First step was the superseding ADR + the PHILOSOPHY / kit-health reject-list update, because it reversed standing boundaries (conflict C1, now superseded by ADR-0019: the former `operating-layer-vision.md` "does NOT cover parallel execution", the "one agent session at a time" + L5/Nimbalyst line, the "sequential by design" note, PHILOSOPHY "Shallow and wide"). Absorbed ID-028 + the promoted v2 parallel-dispatch candidate | this session 2026-05-22 re-eval (initiative 2; maintainer: "it's the way to go") | SPEC-032 | full | shipped (CHANGELOG [Unreleased]; shipped as cross-goal `/kit:dispatch` + `lib/dispatch-gate.sh` gate/drift-guard + `lib/lane-classify.sh` + ADR-0019; proven on a live 2-worker concurrent dispatch. Intra-`/execute` task parallelism deferred per SPEC-032 Out-of-Scope. ID-024 worktree-isolation prereq satisfied inline per SPEC-032 DEC-004) |
| ID-040 | **I2: multi-session concurrent goals.** The cross-session complement to ID-035: extend concurrency from one lead fanning out workers in a single chat to several independent Claude sessions (one goal each, one machine) that never collide. A passive running-goal registry (`lib/goal-registry.sh`) under `.git`, one single-writer file per goal, reuses the ID-035 disjointness gate to refuse a goal that overlaps an active one, is the cross-session monitor (`list`), and carries each goal's attempt log. First step was the superseding ADR + the PHILOSOPHY / kit-health bend (conflict C4, the multi-session twin of C1), because it reverses the standing "multi-session stays L5" boundary. No runtime / DAG / daemon; across-machines + 3+ live operators + ordering chains stay L5 (mirrors ID-035's first-step pattern) | this session 2026-05-23 (goal-loop: "several sessions, not just one lead fanning out") | SPEC-036 | full | shipped (CHANGELOG [Unreleased]; ADR-0022 + `lib/goal-registry.sh` + claim/monitor/attempt-log wiring + meta/hook tests) |
| ID-020 | Verification-pipeline absence-checks: teach task-verifier + integration-checker to assert that removed/replaced content is GONE for "replace, don't duplicate" / "remove X" tasks, not just that the new artifact exists. A replace-task left both copies and passed BOTH verifiers this cycle; only the independent review caught it | SPEC-024 retro 2026-05-21 (pipeline blind spot) | TBD | normal | shipped [implemented on master: task-verifier §1b + integration-checker §4 (SPEC-039 SHIPPED 2026-06-10); board migration] [re-eval 2026-05-23: kept, still live; agents/task-verifier.md + agents/integration-checker.md assert presence only, no absence/removed/replace check; the V-model right arm shipped (/kit:verify, ID-038) without closing this blind spot] **2026-06-27 revisit: VẪN OPEN, đã qua 2 re-eval.** Spec để `shipped` nhưng notes nói rõ "still live; assert presence only". Quy về trạng thái thật: đây là gap I2 verification-arm còn sót. Khi picked up: verify thật bằng 1 test "replace X with Y" có chạy task-verifier + integration-checker, confirm cả 2 có flag presence-of-old hay không. Nếu flag = đã fix, close hẳn. Nếu không flag = reopen lane, thêm absence-check vào agents/task-verifier.md §1b + agents/integration-checker.md §4. |
| ID-021 | Reduce execute-worker shell/hook friction: pre-warn the recurring gotchas in `commands/execute.md`'s worker template (fish `noclobber` -> `>|`; no heredoc commit `-m` -> `git commit -F`; no `rm` -> `mv`), and investigate the commit-format/commit-msg heredoc mis-parse (it read a multi-line `-m` body as a 459-char subject) | SPEC-024 retro 2026-05-21 (worker friction) | TBD | normal | shipped [implemented on master: execute.md Shell gotchas pre-warn (SPEC-040 SHIPPED 2026-06-10); board migration] [re-eval 2026-05-23: kept, still live; commands/execute.md's worker template still carries none of the recurring-gotcha pre-warnings (fish noclobber redirect, git commit -F over heredoc -m, mv over rm); untouched by the V-model/concurrent work] **2026-06-27 revisit: VẪN OPEN, cùng class với ID-020 (spec nói shipped nhưng notes nói still live).** Gap I2 worker-template. Khi picked up: verify commands/execute.md worker template có 3 gotcha pre-warn chưa (fish `>|`, `git commit -F`, `mv` over `rm`). Có rồi = close. Chưa = thêm vào + pin với 1 meta-test. **CLOSED 2026-07-02: verified all 3 pre-warns present in `commands/execute.md` "## Shell gotchas (pre-warn)" (noclobber `>|` · `git commit -F` · `mv` over `rm`); the row's own criterion (Có rồi = close) is met.** |
| ID-038 | **`/kit:verify` standalone command.** Add the one defensible verify-side command from the V-model command/agent symmetry review: re-run `task-verifier` + `integration-checker` on the current spec/branch without rebuilding (a uniform "verify" handle for humans and the `/goal` loop). Reverses the standing "verification lives inside `/kit:execute`" boundary, so the first build task is a superseding ADR-0021 + a PHILOSOPHY / kit-health note (mirrors ID-035's first-step pattern). The three other symmetry candidates (`/kit:accept`, `/kit:check-reqs`, `/kit:doc-spec`) were rejected as phantom duplications of the ship gate + retro. | this session 2026-05-23 (V-model symmetry review; maintainer chose the one defensible command) | SPEC-035 | full | shipped (CHANGELOG [Unreleased]; ADR-0021 + commands/verify.md + 3 meta-tests) |
| ID-039 | **Wire the `security-auditor` orphan agent.** `agents/security-auditor.md` exists but no command dispatches it, so the deep-security review of the code artifact is unreachable (a phantom by the kit's no-phantom rule, surfaced by the 2026-05-23 V-model coverage map). Fix: dispatch it from `/kit:review-team` as a deep-security pass, or add a thin `/kit:security-review`; prefer wiring over a new command. | this session 2026-05-23 (V-model coverage-gap analysis) | TBD | normal | shipped (wired into /kit:review-team; CHANGELOG [Unreleased]) |
| ID-041 | **Implementation-notes log during `/kit:execute` and `/kit:next`.** Workers and contractors maintain `docs/implementation-notes/<spec-slug>.md` (5-bullet entries: Context / Decision / Why / Alternatives / Impact) for any off-spec decision, deviation, tradeoff, or missing constraint hit during the build, so the PR reviewer and the `/wrap-session` LAB_LOG entry have an anchor for what the build chose where the spec was silent. Empty case writes a `No deviations` line so silence is intentional. Shipped as: `/kit:execute` worker template + completion-summary updates (rule, reporting, ship hint), `/kit:next` Step 4 hand-off + file scaffold, four pinning meta-tests in `tests/test-meta.sh`, and the paired rule in the maintainer's dotfiles overlay (`~/.claude/CLAUDE.md` § "When implementing from specs"). Tiny-lane, no behavior change to the verifier pipeline. | this session 2026-05-28 (maintainer asked to formalize "running implementation-notes" + apply SDD back) | SPEC-041 | tiny | shipped (CHANGELOG [Unreleased]; kit commit 2dd80b9, dotfiles commit 3bc78eb; SPEC-041 SHIPPED) |
| ID-043 | **Lane-classification floor check.** Close the residual gap from the kit-as-interface review: `lib/lane-classify.sh` SUGGESTS a lane from task text, but nothing catches when the lane actually CHOSEN (in `/kit:assign`) is lighter than that deterministic floor, so an under-sized `full`/`bug` task slips through silently. Advisory `lane-classify.sh check <chosen> "<desc>"` floor-check (warn + log to completeness.log, never block, "Detect, don't dictate"), wired into `commands/assign.md` Step 5. Originally PR #13 (2026-05-23, as SPEC-041/ID-041 before those numbers were taken); re-ported 2026-06-10 onto the flag-scoring classifier. | 2026-05-23 classify-then-route audit; revived 2026-06-10 | SPEC-053 | normal | shipped (CHANGELOG [Unreleased]; PR #13 revived; `lib/lane-classify.sh check` + assign.md Step 5 wiring + 7 hook tests + 2 meta guards) |
| ID-044 | **Right-sized loops per work type.** Five non-code task types had proof shapes but no cycle. WORKFLOW `## Type loops` table (entry -> phases -> exit per type), registry `agent` column (preassigned/dynamic executor), type-first routing in `/kit:assign` + `/kit:start`. | PHILOSOPHY §6 N1 (2026-06-10 north-star) | SPEC-054 | normal | shipped (CHANGELOG [Unreleased]; mega-goal kit-north-star sub-goal 02) |
| ID-045 | **Backlog as kanban + pull mode.** `lib/backlog.sh board\|next\|set`, `claimed` state, `/kit:assign --next`, `/kit:start` board nudge. Found + fixed 3 drifted rows on first render. | PHILOSOPHY §6 N2 (2026-06-10 north-star) | SPEC-055 | normal | shipped (CHANGELOG [Unreleased]; mega-goal kit-north-star sub-goal 03) |
| ID-046 | **Per-type test dialects + test-first default.** test-design-standard §5b (one spine, six bodies); type-aware /kit:test-plan; cycle-table default for normal/full. | PHILOSOPHY §6 N3 (2026-06-10 north-star) | SPEC-056 | normal | shipped (CHANGELOG [Unreleased]; mega-goal kit-north-star sub-goal 04) |
| ID-048 | **Taxonomy 6 -> 11 + universal done-first.** incident/reconcile/operate/planning/learning become first-class (rule+row+loop+dialect each); 3 folds documented; phase-0 done scenario universal; assign requires Done=. | LAB_LOG evidence sweep (2026-06-10; 6 types covered ~47%) | SPEC-057 | full | shipped (CHANGELOG [Unreleased]; post-megagoal wave) |
| **I3 -- layering contract** | | | | | |
| ID-036 | **I3: layering contract.** Declare how orchestration, hooks, and agents compose: orchestration-first (LLM-driven), hooks as the fallback enforcement for what the LLM cannot be trusted to do (safety subset stays hard-hooked; reconciles conflict C3 with "Guardrails over guidance"), agents as the step-actors that may call commands or act on their own. Design the clear orchestration first, then place each hook as a named fallback. Folds the ID-012 P2 loop QA-gate + ID-027 autonomy-gate-lens considerations. The command/agent half of the rule is captured in `docs/architecture.md` ("Command vs agent"); the hooks-fallback layer is still open here. | this session 2026-05-22 re-eval (initiative 3) | TBD | full | shipped [SPEC-084 2026-06-11: hook fallback layer declared in architecture.md (3-layer rule, placement test, 16-row classed inventory, parity pin); C3 reconciled; ID-012-P2 dispositioned as placement-test worked example; ID-027 landed as spec-validate Reviewer 4 autonomy-gate bullet; re-eval 2026-05-23: kept, rescoped to the hooks-as-fallback layer; the command/agent half landed (docs/architecture.md:161 explicitly marks "the hook-fallback layer is still open"); the V-model command/agent inventory firmed up the command/agent half but did not declare the hooks layer] **2026-06-27 revisit: I3 command/agent half đã land sạch, hooks-as-fallback layer VẪN OPEN.** Đây là gap I3 cuối cùng còn sót. Khi picked up: walk từng hook trong 16-row inventory, áp placement test, đánh dấu orchestration-fallback vs hard-hooked safety. Low u nhưng high architectural debt (hooks sai layer = root cause của nhiều false-positive gate fires). |
| ID-012 | Goal-loop fidelity (one theme, two parts). P1: stop-criteria in the `/spec` template (pin `## Verification` + `## Open questions` so specs are pointer-`/goal`-ready) -- build now, tiny lane. P2: QA gate around the autonomous loop (verify-into-loops + SPEC-006 completeness clauses + Reviewer 5) -- held until the pointer-`/goal` pattern has real runs | maintainer Q3 2026-05-20 + goal-readiness convergence 2026-05-21 | SPEC-012 | normal (P1) / full (P2) | parked [board migration 2026-06-10: leading keyword normalized; substance unchanged ->]1 shipped (see CHANGELOG); P2 held (overlaps ID-036) [re-eval 2026-05-23: kept, the hold condition (real pointer-/goal runs) is now met and /kit:verify (ID-038) delivers the verify-into-loops half of P2; the residual loop QA-gate + Reviewer-5 considerations fold into ID-036, so P2 stays held there]  |
| **Parked** | | | | | |
| ID-002 | Absorb skills/hooks developed in ops-toolkit into the kit (internal lane) | item e | SPEC-007 (PARKED; see its Parked note) | full | parked |
| **Terminal -- shipped / dropped (kept as the 2026-05-22 re-eval audit trail; not live work)** | | | | | |
| ID-003 | Deep orchestration scan of the copied repos vs our WORKFLOW (report + absorb plan) | item 2 | `docs/research/2026-05-20-orchestration-deep-scan.md` | normal | shipped (report delivered; recs folded into SPEC-006) |
| ID-015 | AGENTS.md operating layer: tool-agnostic entrypoint + brownfield backfill lane + six-section goal projection + observable After-state spec section (ships ADR-0013) | ADR-0013 (hoangnb24/harness-experimental study, 2026-05-21) | SPEC-024 | full | shipped (CHANGELOG [Unreleased], no version bump; PR #7) |
| ID-016 | Promote the "new SPEC -> BACKLOG status row" doc-impact check to a guard (a hook or a kit-health line); it was missed across multiple cycles | SPEC-025 retro 2026-05-21 (recurrence clears the PHILOSOPHY section 5 bar) | TBD | normal | dropped (2026-05-22 re-eval: a hook built ahead of the I3 layering inverts "orchestration first, hook as fallback"; abandon branch `feat/doc-impact-guard`) |
| ID-017 | Reconcile retro-file naming across the `retro` skill body + description, CLAUDE.md, and WORKFLOW.md to the repo's actual `RETRO-YYYY-MM-DD-<slug>.md` convention | SPEC-025 retro 2026-05-21 | TBD | tiny | dropped (2026-05-22 re-eval: minor doc hygiene, off the V-model/parallel path; fix opportunistically) |
| ID-018 | install.sh prints blind `cp` tips for AGENTS.md and CLAUDE.md; harden both to copy-if-absent at the command level (`cp -n` or a guard) so the printed command matches its "if absent" prose, or record why prose-only suffices | SPEC-024 review 2026-05-21 (security lens) | TBD | tiny | dropped (2026-05-22 re-eval: install hygiene, off-path) |
| ID-019 | Demo `examples/hello-spec/docs/specs/SPEC-001-version-flag.md` lacks a `## After state` section; add one so the demo is a complete exemplar of the post-SPEC-024 spec template | SPEC-024 review 2026-05-21 (test-coverage lens) | TBD | tiny | dropped (2026-05-22 re-eval: demo polish; the I2 V-model spec-template rework redoes it) |
| ID-022 | Freeform front door: extend `/kit:assign` to accept freeform intent (not only `ID-NNN`), auto-allocating an ID + BACKLOG row so "apply SDD to X" / a vague brief is a native one-shot intake (unparks the SPEC-024-deferred griller entry; preserves ID-first traceability) | PLAYBOOK.md scenarios 2026-05-22 | SPEC-026 | normal | shipped (CHANGELOG [Unreleased]; freeform path in commands/assign.md, commit d920df3, documented in MANUAL.md; SPEC-026 SHIPPED; settled 2026-05-23 maintainer call on the re-eval status-drift flag) |
| ID-023 | Mid-flight spec amend: a path to add scope to a VALIDATED / in-build spec without restarting the lane (state machine transition BUILDING -> SPECIFYING) | operating-layer-vision gap analysis 2026-05-22 | SPEC-027 | normal | shipped (CHANGELOG [Unreleased]; local branch feat/mid-flight-amend, no tag/PR) |
| ID-025 | Re-open-shipped convention: a follow-up spec spun from a SHIPPED one (state machine transition SHIPPED -> TRIAGING) | operating-layer-vision gap analysis 2026-05-22 | TBD | tiny | dropped (2026-05-22 re-eval: state-machine hygiene, off-path) |
| ID-026 | Release-hygiene guard: a kit-health line or hook that flags VERSION cut-but-untagged AND `[Unreleased]` spanning more than one spec (catch the messy-release state before it compounds) | SPEC-027 retro 2026-05-22 (recurrence of the SPEC-018 finding; clears the PHILOSOPHY section-5 bar) | SPEC-028 | normal | shipped (CHANGELOG [Unreleased]; local branch feat/release-hygiene-guard, no tag/PR; the guard self-fired on its own ship) |
| ID-027 | spec-validate autonomy-gate lens: for a spec whose behavior runs inside an autonomous loop (`/execute`, `/goal`), check whether it lets the loop make a scope / architecture / risk decision without a human gate | SPEC-027 retro 2026-05-22 (the cycle's HIGH was caught at review, should have been catchable at validate) | TBD | normal | dropped (2026-05-22 sort: folded into ID-036; its autonomy-gate-lens is a consideration ID-036 owns) |
| ID-028 | execute.md: acknowledge disjoint-file parallel worker dispatch (sequential is the safe default only for shared-file or dependent tasks; independent non-overlapping tasks may parallelize) | SPEC-027 retro 2026-05-22 | TBD | tiny | dropped (2026-05-22 re-eval: folded into ID-035 first-class parallel execution) |
| ID-029 | Carve out "identical-by-contract" from the no-premature-abstraction (3x) rule: when two snippet copies MUST stay byte-identical (a shared check across surfaces), single-source or pin the exact block even at two uses, do not wait for the third | SPEC-028 retro 2026-05-22 (DEC-003's two inlined copies drifted three times in one cycle) | TBD | normal | dropped (2026-05-22 re-eval: code-rule nuance, off-path; revisit only inside the C2 philosophy ADR if ever) |
| ID-030 | execute.md worker-dispatch guidance: when two tasks must produce identical logic, give both workers the exact same block, not one exact + one narrative (the asymmetry that seeded the SPEC-028 drift) | SPEC-028 retro 2026-05-22 | TBD | tiny | dropped (2026-05-22 re-eval: execute polish, subsumed by ID-035 parallel rework) |
| ID-031 | Command discoverability + invocation fix (superpowers pattern): (1) prefix every `commands/*.md` `description:` with a `(dwarves-kit)` source tag so the Claude Code `/` menu labels kit entries by source while the command name stays bare (mirrors superpowers' `(superpowers)` description prefix); (2) align the documented invocation to what CC 2.x actually resolves, verified via `/` autocomplete (bare `/<command>`, not `/user:<command>`), sweeping the stale `/user:` form across README/MANUAL/install.sh + cross-refs; (3) pin both with a meta-test (every command description tagged; no `/user:` in shipped docs/install) | this session: `/user:kit-health` unknown on Claude Code 2.1.148; doc/install invocation drift the kit's guards never caught | SPEC-029 | normal | shipped (CHANGELOG [Unreleased]; PR #9) |
| ID-032 | SPEC-029 fallout: the `/user:` sweep + namespace guard missed non-`.md` surfaces. (1) sweep `install.sh` echoes to bare `/<cmd>` (the bash-install path resolves bare) and the hooks' SUGGEST/echo strings to a form-agnostic command name (no slash; two install paths, no single slash form fits both); (2) extend the SPEC-029 meta-test guard to scan `install.sh` + `hooks/*.sh`, not only `*.md`, so the install/runtime surfaces it claims to cover are actually checked | this session 2026-05-22: SPEC-029 (PR #9) shipped with `install.sh` + hooks still emitting `/user:`; the guard globs `*.md` only so it never saw them (re-open-shipped, ID-025 class) | SPEC-030 | normal | shipped (CHANGELOG [Unreleased]; PR #10) |
| ID-034 | **I2: V-model SDLC mapping.** Map every command / skill / agent onto V-model phases (brief -> requirement -> solution-design -> ui-design -> test-design -> code -> code-review -> test-exec -> docs -> ship -> retro), with the left arm mirrored by the right arm (each definition phase has its verification phase), and the lanes deciding measure-twice vs run-lite per task type (update-doc / bug-fix / vague / full). Surfaces conflict C2 (the "8 phases" frame + rejection criterion #2 need rewording) | this session 2026-05-22 re-eval (initiative 2) | SPEC-031 | full | shipped (2026-05-22 sort: CHANGELOG [Unreleased]; built + reviewed this branch; SPEC-031 done-state) |
Dependency notes:
- ID-012 P1 (spec stop-criteria) shipped (SPEC-012, normal lane; see CHANGELOG); P2 (loop QA gate) is held until the pointer-`/goal` pattern has real runs; it will build on the now-shipped SPEC-006 completeness clauses + the existing verification pipeline.
- ID-002 (internal absorption lane, SPEC-007) is parked; independent of the orchestration chain.
- ID-016 / ID-017 came out of the SPEC-025 retro; both are kit-internal hygiene, independent of the SPEC-024 chain.
- ID-018 / ID-019 came out of the SPEC-024 /review-team pass (deferred LOW findings); both are tiny-lane polish, independent of the SPEC-024 ship.
- ID-020 / ID-021 came out of the SPEC-024 retro; ID-020 (verifier absence-checks) relates to ID-016 (both are guard/verifier hardening) and is the highest-signal kit finding of the cycle.
- ID-022 came out of writing `docs/PLAYBOOK.md` (scenarios S2/S5): the freeform-intent gap. It unparks the SPEC-024-deferred griller entry; SPEC-026 drafts it. Independent of the other chains.
- ID-023 / ID-024 / ID-025 came out of `docs/operating-layer-vision.md` (the SDLC state-machine gap analysis): the transitions with no clean path today. ID-022 is the highest-priority of the operating-layer set; these three follow it. All trace to the vision doc as their north-star. ID-023 shipped (SPEC-027); ID-024 / ID-025 remain queued.
- ID-026 / ID-027 / ID-028 came out of the SPEC-027 retro. ID-026 (release-hygiene guard) is a two-cycle recurrence (SPEC-018 + SPEC-027) and relates to the ID-016 / ID-020 guard-promotion theme; it is the highest-signal of the three. ID-027 (validate autonomy-gate lens) would have caught the SPEC-027 review HIGH earlier. ID-028 is tiny-lane polish. ID-026 shipped (SPEC-028); ID-027 / ID-028 remain queued.
- ID-031 came out of this session: `/user:kit-health` returned "unknown command" on Claude Code 2.1.148 (a flat personal command resolves bare, `/kit-health`). It pairs an invocation-doc fix (sweep the stale `/user:` form) with a discoverability upgrade (the superpowers-style `(dwarves-kit)` description prefix). It is the same doc-drift class as the release-hygiene phantom cut (ID-026): guidance the kit's own guards never checked. The meta-test in part (3) is the guard that would have caught it.
  - **Findings 2026-05-22 (revise the spec against these; part of the original premise is wrong):**
    - **The part-(1) premise is false.** superpowers does NOT label by a `(superpowers)` description prefix; it labels by being a *plugin* (CC auto-namespaces plugin commands as `plugin:command`). dwarves-kit already ships a valid plugin manifest (`.claude-plugin/plugin.json` name `kit`, shortened from `dwarves-kit` so the namespace is the shorter `/kit:`, repo/project name unchanged; `marketplace.json` marketplace `dwarves-marketplace`, install `kit@dwarves-marketplace`). So **part (1) is obsolete: drop the description-prefix idea entirely.** A prefix is only a workaround for the no-namespace bash-installer path, and we are leaving that path (below).
    - **Canonical invocation = `/kit:<cmd>`** (plugin namespace). Bare `/<cmd>` was only the flat-installer artifact; `/user:<cmd>` is dead. Part (2)'s doc sweep should target `/kit:<cmd>` and remove BOTH `/user:` and bare `/<cmd>` forms.
    - **Part (3) meta-test** should assert: `plugin.json` + `marketplace.json` are valid; no `/user:` in shipped docs/install; docs use the `/kit:` form.
    - **Distribution gap (new, blocks naive plugin adoption):** CC 2.1.148 CANNOT install a local *directory*-source marketplace plugin (`/plugin install` fails: "source type your Claude Code version does not support"). 2.1.148 is the latest on npm, so "update Claude Code" is not a fix today. Two supported paths remain: **end users** install via a **github**-source marketplace (`dwarvesf/dwarves-kit`), frozen at the pushed commit; **the maintainer** runs `claude --plugin-dir <repo>` for the namespace PLUS live edits (`/reload-plugins` to refresh). This session: the bash installer was uninstalled and a `~/.config/fish/functions/claude.fish` wrapper now applies `--plugin-dir` globally.
    - **Net for the spec:** ID-031 shrinks to (a) sweep `/user:` and bare `/<cmd>` -> `/kit:<cmd>` across README/MANUAL/install.sh/cross-refs; (b) meta-test on manifest validity + invocation form; (c) document the github (users) vs `--plugin-dir` (maintainer) install split. Reference: `ops-toolkit/research/2026-05-22-cc-plugin-local-install.md`.
- ID-029 / ID-030 came out of the SPEC-028 retro (DEC-003's two inlined check copies drifted three times in one cycle, each caught at a different verification layer). ID-029 (identical-by-contract abstraction carve-out) is the substantive insight and relates to the kit's code-quality rules; ID-030 (worker-prompt symmetry) is tiny-lane polish and relates to ID-028. The third retro action item, resolving the release-state phantom cut (integrate the agents-md -> mid-flight -> release-hygiene stack and tag), is deliberately NOT a backlog row: it is a maintainer integration decision, recorded in the SPEC-028 retro.
- **2026-05-22 re-evaluation (maintainer pivot).** The queue was re-scored against three new north-star initiatives: **I1** fold `/goal` + the orchestration layer into one operating manual (ID-033); **I2** V-model SDLC mapping (ID-034) + first-class parallel execution (ID-035); **I3** the orchestration / hook / agent layering contract (ID-036). Lens: harness engineering (configure the process: triggers, when-the-human-jumps-in, lanes, loop design). Kept + elevated because they now serve an initiative: ID-012 P2 (-> I3), ID-020 (-> I2 verification arm), ID-021 (-> I2), ID-022 (-> I1 intake), ID-024 (-> I2; worktree isolation is the parallel-execution prerequisite), ID-027 (-> I3). Dropped as off-path or folded: ID-016 (folded out, dropped per maintainer), ID-017, ID-018, ID-019, ID-025, ID-028 (folded into ID-035), ID-029, ID-030. ID-002 stays parked. **Open conflicts to resolve before building I2:** C1 (parallel execution reverses four standing boundaries; needs a superseding ADR + PHILOSOPHY + kit-health update, ID-035's first step), C2 (the "8 phases" frame + rejection criterion #2 must be reworded for the V-model phases, ID-034). Each initiative is full/normal lane and should run `/kit:think` (brainstorming) before a spec; none is specced yet.
- **2026-05-22 sort (this branch, `docs/backlog-reeval`).** The Active queue was regrouped from strict ID-order into initiative blocks (**I1** operating manual / intake, **I2** V-model + parallel + verification arm, **I3** layering contract, then Parked, then a Terminal audit block), with dependency + readiness order inside each. Build-ready unblocked items float to the top of their block: **ID-022** (SPEC-026) and **ID-037** (SPEC-034) are validated and depend on nothing; **ID-035** (SPEC-032) is validated but blocked on **ID-024** (worktree isolation), so 024 precedes it. Three dispositions applied: **ID-034** (V-model lens, SPEC-031) shipped on this branch and moved out of live work into the Terminal block (CHANGELOG is the canonical shipped record; its SPEC header is still `VALIDATED` and should flip to `SHIPPED`). **ID-027** (autonomy-gate lens) folded into **ID-036** and dropped (precedent: ID-028 -> ID-035). **ID-012 P2** stays held with an explicit `(overlaps ID-036)` cross-link, per the maintainer's keep-distinct call. The Terminal block also holds the pre-existing shipped rows (ID-003/015/023/026/031/032), which technically violate "shipped drops off the queue"; they were relocated, not pruned (the "never delete without explicit ask" rule). Pruning them is a separate follow-up.
- **2026-05-23 re-evaluation (post-V-model + post-concurrent-dispatch).** Re-scored the eight live items against what shipped this cycle: the V-model lens (SPEC-031), cross-goal `/kit:dispatch` (SPEC-032), the multi-session goal registry (SPEC-036), `/kit:verify` (SPEC-035), and the `security-auditor` wiring (ID-039). **Two dropped as delivered:** ID-024 (its worktree-per-spec context-switch shipped via `/kit:dispatch`, and the explicit ABANDONED terminal is now a state in the `docs/architecture.md` SDLC state machine) and ID-033 (the orchestration docs it would fold, `docs/ORCHESTRATION.md` + `docs/operating-layer-vision.md`, no longer exist; their content is distributed across `MANUAL.md` + `WORKFLOW.md` + `docs/architecture.md`, and the "one manual" aim was overtaken by the deliberate AGENTS/WORKFLOW/MANUAL/architecture layering). **One flagged needs-maintainer:** ID-022 (the freeform front door is built, committed, and documented, but SPEC-026 still reads VALIDATED with no CHANGELOG entry, a status-drift call only the maintainer can settle: mark shipped or confirm pre-release). **Five kept:** ID-037 (`/kit:mega` still unbuilt, the sequential complement to the parallel dispatch, not superseded by it), ID-020 (the verifiers remain presence-only, the absence-check blind spot is still open), ID-021 (the execute worker-template gotchas are still unwarned), ID-036 (rescoped to the still-open hooks-as-fallback layer now that the command/agent half landed), and ID-012 P2 (verify-into-loops is now covered by `/kit:verify`; the residual loop QA-gate folds into ID-036, where P2 stays held). No spec, feature, or v2 candidate was touched; the Schema and charter sections were left as-is.

## Schema

The Active-queue contract (formalized per SPEC-005 / ID-006; the charter keeps the schema in this file, not a separate SCHEMAS file).

| Column | Meaning |
|---|---|
| `ID` | `ID-NNN`, zero-padded, stable, assigned on entry, NEVER reused. |
| `Title` | one-line description of the work. |
| `Source` | where the item came from (a braindump item, a research finding, a dogfooding signal). |
| `Target artifact` | the spec or doc this becomes (`SPEC-NNN`, or `(tiny, no spec)`). |
| `Lane` | the WORKFLOW.md risk tier: `tiny` / `normal` / `full`. |
| `Status` | the lifecycle state below. |

**Status lifecycle:** `queued` (committed, no spec) -> `claimed` (pulled via `/kit:assign --next`) -> `speccing` (spec drafting) -> `validated` (spec passed `/spec-validate`) -> `executing` (in build) -> `shipped`. Off-ramps: `parked` (a drafted spec deliberately set aside; the spec holds its own revisit note) or `dropped` (deliberately cleared from the active path during a re-evaluation; a one-line reason in the Status cell; the ID retires and is never reused). SHIPPED items drop off the queue: the CHANGELOG is the canonical shipped record and completed work is not mirrored back here.

**Consumer:** `/user:start` and `/user:next` render this queue when you ask "what's left?". That rendering is wired in SPEC-006, not here; SPEC-005 pins only the schema the spine reads.

This is **not** `TODOS.md`: that file is gitignored, transient, per-diff `/review` output, not the backlog. See the state model in `docs/architecture.md`.

## v2 candidates (pending real usage signal)

Items here are NOT committed; they become real only after a spec is written and `/spec-validate` passes. Listed for visibility.

- Prompt-type anti-rationalization hook (Haiku evaluation instead of grep patterns). Needs ~30 false-positive logs from `~/.claude/dwarves-kit/logs/anti-rationalization.log` to justify the latency cost.
- `/qa` command with headless browser testing (requires Playwright).
- ~~Agent Teams parallel task dispatch in `/execute`. Currently sequential by design.~~ **Promoted to ID-035 (2026-05-22 re-eval):** parallel execution is now committed work, not a someday-maybe. The "sequential by design" stance (PHILOSOPHY "Shallow and wide") is superseded by ID-035's first-step ADR.
- `SessionEnd` hook for automatic knowledge capture.
- AutoResearch optimization of the `task-verifier` prompt. Needs 30+ real verification transcripts as eval corpus.
- Multi-harness packaging (Codex / Cursor / Gemini / OpenCode). Defer until real cross-harness demand.
- `acceptance-verifier` read-only agent: extract the acceptance check from `/kit:ship`'s inline gate into a separate read-only agent (aligning with PHILOSOPHY "verify with a fresh context, not self-report"), reusable by `/kit:ship` and the proposed `/kit:verify`; completes the right-arm verifier set (unit/integration/acceptance). Not committed: the inline ship gate works today. (V-model coverage-gap analysis 2026-05-23.)

## Parking lot (revisit if a real signal arrives)

- L5 orchestration (Nimbalyst integration). (2026-05-23 re-scope per ADR-0022: the boundary no longer sits at "3+ concurrent sessions". One operator's N concurrent same-machine sessions over disjoint goals are now in-kit, coordinated by the passive running-goal registry, `lib/goal-registry.sh`, SPEC-036 / ID-040. L5 is reached only by coordination **across machines**, **3+ live human operators** sharing a pool, or **goal-ordering chains** (B waits for A to merge); those still hand off to Nimbalyst / GSD v2.)
- **In-kit DAG executor (the "build a runtime" option, Option 4).** A scheduler inside the kit that owns topological task ordering, wave execution, dependency resolution, and unattended cross-task progression. Deliberately NOT built: this is a runtime (crash recovery, state machine, per-provider error handling), which breaks PHILOSOPHY "Shallow and wide" + the bash-over-binaries thesis + the runtime-integration boundary. ID-035 (first-class parallel execution) deliberately stops short of this: it does disjoint-file fan-out + a pairwise interference gate + a wait-queue, NOT a DAG executor. **Tripwire to revisit:** when goals develop real ordering chains (C needs A+B merged, D needs C) that the interference-gate model cannot express, i.e. when "we actually need a DAG" becomes true. At that point the answer is to hand execution to GSD v2 (gsd-build/gsd-2, the external Pi-SDK runtime), not to build a scheduler in-kit. Full reasoning + the 7-option parallelism menu + the DAG analysis: `docs/research/2026-05-22-concurrent-goal-dispatch.md`.
- AutoResearch loop for command prompt optimization. Manual iteration is faster at current volume.
- Agent-type hooks for deep verification. Custom subagents handle this today; revisit only if subagents prove insufficient.
- **Kit self-improvement items from the SPEC-031 retro (deferred, not promoted).** Four candidates to revisit: (AI-1) `/kit:spec-validate` should dry-run the spec's own Verification + AC-proof commands, not just reason about them; (AI-2) a CLAUDE.md/test rule that a loop over an extracted list must assert non-empty; (AI-3) spec-template guidance to scope absence-check ACs to operating surfaces; (AI-4) backfill the ADR-0020/0017 architecture.md cross-refs. Full context: `docs/retro/RETRO-2026-05-22-v-model-lens.md`.

## Source

Backlog format matches `ops-toolkit/_meta/BACKLOG.md`, extended with the Active queue tier (ID-006 / SPEC-005). Completed work is NOT mirrored here, to avoid drift with CHANGELOG.md and `docs/handoff/`.
