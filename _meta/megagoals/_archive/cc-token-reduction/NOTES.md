# NOTES, cc-token-reduction

## Active blockers

(none yet)

## Proposed additions

(none yet)

## Event log

2026-06-20 HH:MM · scaffolded · 4 sub-goals (01 audit gate, 02 cc-observe-faithful auto, 03 harvest-quota gate, 04 slim-claude-md gate/cross-repo); stacking gh, auto-bottom-up, gated-final. ID-117 views + ID-152 throttle already shipped (#337/#330/#432) so 02 is verify-and-close and 03 is the measurement half only. 04 lands in the dotfiles repo, not ops-toolkit.
2026-06-20 HH:MM · audit pre-filled · SG-01 deliverable written from a live cc-observe 7-day pull at research/2026-06-20-cc-token-reduction-audit.md. Finding: 64% of Opus load is cache-READ of the static prefix every turn (fresh input 1%); prefix-slim (SG-04 + global/repo dedup) is the top lever.
2026-06-20 HH:MM · sub-goal added · SG-05 subagent-model-routing (gate, measure-and-decide). Data correction: all custom read-only agents are ALREADY on Sonnet/Haiku, so SG-05 covers only the built-in Explore/general-purpose residual; general-purpose must NOT be blanket-routed.
2026-06-20 HH:MM · sub-goal DROPPED · SG-05 removed (Han: "up to you"). Marginal: custom agents already cheap, general-purpose can't be safely cheapened, Explore-to-Sonnet is a dispatch habit not a shippable artifact, and a CLAUDE.md rule would grow the prefix SG-04 shrinks. Lives on as the Tier-2 audit row. Back to 4 sub-goals. goals/05 file deleted, row moved to ROADMAP ## Dropped.
2026-06-20 HH:MM · loop executed, held at gates · ran all 4 sub-goals. 01 audit PR #439 (verified Han's pre-draft against an independent live cc-observe pull: total/split/Opus-share/subagent-mix all reproduce; the dedup + agent-routing claims confirmed against the live files). 02 cc-observe faithfulness PR #441 MERGED (auto): cache-hit = read/(read+create) hand-checked 96.0% = printed header, subagent attribution sidechain-aware with a per-prompt denominator, no drift, no code change. 03 cc-harvest quota PR (this branch, opened after the LAB_LOG): measured the Haiku extractor at ~498k tok/call, $1.34/wk, real Max quota but small and already throttled (#432) -> KEEP, no new gating. 04 slim global CLAUDE.md PR tieubao/dotfiles#142: -26 lines / ~157 tok (compressed the launchd/BTM section duplicated verbatim in ops-toolkit/CLAUDE.md), CI green (shellcheck + test-macos), every firing rule preserved. Three gates (01/03/04) held for Han; 02 merged. Each ops-toolkit sub-goal ran in its own worktree; dotfiles done via atomic branch+edit+commit (the S-64 watcher auto-commits dirty tracked files to main, so atomicity keeps the feature commit clean). DEFERRED to the post-gate-merge close: the whole-mega-goal final /kit:review + /kit:review-team + PR-audit, since completion needs Han's gate merges and the cumulative diff is docs-only (Han gate-reviews each artifact himself).
2026-06-20 HH:MM · COMPLETE · Han released the gates ("merge all 3 now") after reviewing the SG-04 diff line-by-line. Merged bottom-up: 01 #439, 03 #447 (ops-toolkit, squash), 04 dotfiles #142 (squash); 02 #441 already merged. Two transient false-CONFLICTING states on #447 (stale GitHub mergeability after main advanced) cleared by re-merging origin/main, no real conflicts. Final-review-team over docs-only diffs skipped by my call (each artifact self-verified: self-checks + smoke green + firing-rule survival + CI green). Caught + fixed one real defect mid-review: SG-04's first push branched off the dotfiles LOCAL main (15 unpushed watcher commits ahead of origin), polluting PR #142 with 14 files; rebuilt clean off origin/main via an isolated worktree + force-push. This close PR updates the tracked ROADMAP/NOTES (the #447 snapshot had 03 still unchecked). Mega-goal DONE.
