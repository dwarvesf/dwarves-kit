Drive the cc-token-reduction mega-goal to completion. Source of truth: _meta/megagoals/cc-token-reduction/ROADMAP.md and the per-sub-goal files in that folder's goals/. Re-read them every turn; they hold the contract.

Shape: one PR per sub-goal, from its own branch, stacked per the roadmap Dependencies (01 is the keystone scoping 04; 02 and 03 run parallel off main). ops-toolkit is dwarves-kit-adopted and /goal runs at its root, so each ops-toolkit sub-goal routes through SDD: classify with lib/lane-classify.sh, then for a full-lane change run /kit:spec then /kit:spec-validate, implement, write the co-located proof-of-done, let the ship-gate pass. EXCEPTION: sub-goal 04 lands in the dotfiles repo, not ops-toolkit, /kit:* bind to the ops-toolkit cwd and cannot drive it; do that change directly, open its PR with plain gh from the dotfiles repo, and stage+commit in one shell call (the dotfiles watcher reverts uncommitted tracked changes).

Note: ID-117 views + ID-152 throttle already shipped, so 02 is verify-and-close and 03 is the measurement half only. Do not rebuild them. SG-01's audit is PRE-DRAFTED at research/2026-06-20-cc-token-reduction-audit.md; SG-01 verifies + PRs it rather than starting blank. Custom read-only agents are already on Sonnet/Haiku, so subagent routing is a Tier-2 audit row, not a sub-goal (there are 4 sub-goals).

Hard rules (commands, not suggestions):
- One PR per sub-goal. Local-only diff = unstarted. Record the PR # on its ROADMAP line the moment gh pr create returns ("- [ ] 0N-..., PR #N"); flip to "- [x]" only when its own Done is verified AND the PR is open + CI green (gh pr checks) + not CHANGES_REQUESTED. A checked box without a green PR # is invalid; the audit unchecks it.
- Merge: auto-bottom-up + gated-final. Merge an `auto` sub-goal's PR yourself once all gates hold (Done verified, gh pr checks green, not CHANGES_REQUESTED, proof-of-done committed WITH captured evidence, a run-table/screenshot/log, never a bare "passes"). Here only 02 is `auto`. NEVER merge a `gate` PR, a red PR, or a CHANGES_REQUESTED PR. Stop at every `gate` sub-goal (01, 03, 04) and at the final PR for Han.
- For a `gate` sub-goal: open the PR with its artifact + captured evidence, append the final summary to NOTES Event log on the first pause, then emit "🛑 NEEDS APPROVAL, sub-goal NN, STOP /goal MANUALLY". Do not merge it.
- Each sub-goal's close-the-loop has its OWN verification commands (in its file). Generic test runs are not verification.
- No new sub-goals mid-loop; discovered ones go to NOTES Proposed additions. Never rewrite any Done or Outcome. NOTES Active blockers updated in place (fingerprint: command · failure · prerequisite · last verified); Proposed additions + Event log append-only. Retry a blocked sub-goal only when its prerequisite changed. 04 stays blocked until 01's audit doc is PR'd.

Whole-mega-goal close (before marking complete):
- Audit: extract every "PR #N" from ROADMAP.md, gh pr view each for state + reviewDecision + statusCheckRollup; uncheck any invalid box.
- Final review pass over the cumulative diff: run /kit:review then /kit:review-team (security + architecture + test-coverage). Address findings on the affected PR's own branch; re-run until no blocking findings remain.
- Draft one _meta/LAB_LOG.md paragraph (slug, sub-goal count, PR range, key lessons) as the newest entry on the LAST ops-toolkit sub-goal's branch so it rides its PR (SPEC-005).

Stop conditions: all sub-goals checked-with-PR and merged-or-held per policy (success, only after the review pass + LAB_LOG); OR a `gate` sub-goal / the held final PR awaiting Han; OR every remaining sub-goal blocked with unchanged prerequisites; OR budget exhausted. On a repeat fast-stop emit only "🛑 LOOP BLOCKED, STOP /goal MANUALLY", do not re-audit. Anything else: keep moving.
