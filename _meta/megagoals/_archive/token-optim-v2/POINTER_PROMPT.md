You are running the `token-optim-v2` mega-goal. Source of truth on disk: `~/workspace/<owner>/ops-toolkit/_meta/megagoals/token-optim-v2/`. Read `ROADMAP.md` every turn, and the relevant `goals/NN-*.md` before working a sub-goal. Do not rederive scope.

PREREQUISITES SATISFIED: dwarvesf/dwarves-kit #80 (SPEC-087) + #81 (orchestrate.sh phase 1) are MERGED. The whole wave is unblocked. State (2026-06-29): SG-01/02/03/04/06/08/10/12 MERGED; SG-11 (#88) OPEN; remaining = SG-05, SG-07, SG-09.

Rules (commands, not suggestions):
- One PR per sub-goal. A sub-goal that is only a local diff is unstarted. Record its PR # on the ROADMAP line when opened; mark `- [x] , PR #N` only when complete and merged-or-held.
- Stacking: `gh` stacked PRs. SG-01..04 are independent kit changes off the post-#81 master, base master. SG-08 bases on SG-02/SG-03's branches if unmerged. Otherwise branch off the repo default.
- Merge = open-only. The remaining sub-goals are `gate-stacked` (Han 2026-06-29, "adjust the rest so they stack, I review at the end"): build them as open PRs, git-stacked within a repo, and DO NOT stop per-PR for review. See ROADMAP "## Stacking plan". Build order SG-05 -> SG-07 -> SG-09 (SG-09 LAST). ops-toolkit chain: SG-05 off main -> SG-09 stacks on SG-05; dotfiles SG-07 standalone; dwarves-kit #88 already open. Never merge a gate PR yourself; Han reviews the whole open set at the END of the wave.
- "CI green" = the open PR's checks, never local tests.
- Kit-adopted repos (dwarves-kit, ops-toolkit): each sub-goal reads AGENTS.md + WORKFLOW.md, runs its lane, records gates via lib/gate-ledger.sh so the ship-gate is the Done check. The `/kit:*` commands bind to cwd; for a sub-goal whose repo differs from cwd, drive the lane via lib/ + gate-ledger directly, or run it from that repo's checkout. Do not author kit specs from the ops-toolkit cwd.
- Each sub-goal verifies via its own close-the-loop commands (in its goal file), not a generic test run. Converge to its proof-of-done (captured evidence) before checking the box.
- Do not rewrite a sub-goal's Done=. No new sub-goals mid-loop (log them to NOTES.md `## Proposed additions`). ROADMAP.md is the source of truth for done + PR #s.
- Token hygiene (this mega-goal's own subject, so practice it): push big reads/searches to subagents, read narrow slices (offset/limit, grep -n), pipe big outputs to files. Keep the lead lean. `/compact` if it bloats; `/clear` + re-paste this pointer between sub-goals when you can.
- Worktrees: branch each sub-goal into a worktree. Cross-repo (dwarves-kit, dotfiles, the skill repo) cannot use the native EnterWorktree from an ops-toolkit cwd; create the worktree in the target repo with `git worktree add <repo>/.claude/worktrees/<name>` off its default branch, and never disturb other branches' uncommitted work.
- Before claiming the mega-goal complete: extract every PR # from ROADMAP, `gh pr view <N> --json state,reviewDecision,statusCheckRollup` each, confirm. Then run `/kit:review-team` plus one focused review lens across the merged set. Then, per ops-toolkit SPEC-005, draft a single-paragraph `_meta/LAB_LOG.md` arc entry (slug, sub-goal count, PR range, key lessons) as the newest entry on the LAST sub-goal's branch so it rides into the final PR. Then mark complete.
- Stop conditions: all sub-goals merged-or-held (success); a gate sub-goal or the held final PR awaiting Han; all remaining blocked with unchanged prerequisite (incl. #80/#81 still open); token budget exhausted. On any stop, append a summary to NOTES.md `## Event log` and emit a `🛑 LOOP BLOCKED , STOP /goal MANUALLY` banner. On repeated blocked-stops, emit ONLY the banner (do not re-audit).

Start: read ROADMAP.md (+ NOTES.md "## Event log" for current state). Next sub-goal = SG-05 (cheap-planner/expensive-executor split, ops-toolkit experiments/, branch off main). Then SG-07 (dotfiles static-floor trim, standalone). Then SG-09 LAST (eval ablation, stacks on SG-05's branch). One sub-goal per fresh context; /clear + re-paste this pointer between them. They all stay OPEN for Han's single end-of-wave review.
