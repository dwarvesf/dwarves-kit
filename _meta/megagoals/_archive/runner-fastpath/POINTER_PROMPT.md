Objective: simple tasks skip mega ceremony, queued megas drain overnight, the cockpit reaches the personal Hermes agent, and ALL harness machinery ships as ONE kit. The triage ladder (DONE). The runner is `orchestrate.sh queue` (bash launcher IN dwarves-kit; drives the REAL Claude Code UI via tmux/cmux send-keys `/goal`, NOT headless `claude -p`/Go). The `board` command joins dwarves-kit (generic: render migrated from ops `_meta/board*` + `queue`/`mirror`/`writeback`). ledger-observatory (+ `mega-durations`) ALSO moves into dwarves-kit; DuckDB stays only there. An Air runbook runs `orchestrate.sh queue`. Scaffold: _meta/megagoals/runner-fastpath/ (ROADMAP.md = source of truth; goals/03R,03K,04,05R,05K,06,07,08,09 = contracts; research docs + amendments = BINDING). You own ONLY this scaffold.

RUN CONTRACT: ops-toolkit _meta/megagoals/OPERATE.md BINDING.

RESUME STATE (2026-07-05): 01/02 MERGED (dotfiles#202, kit#175). 03 (Go,#705) MERGED, SUPERSEDED (03R retires/03K replaces). 05 SUPERSEDED too (05R retires the ops copy; 05K moves ledger-observatory into the kit + adds the query). Conductor owns ALL mega-dir bookkeeping (workers report, you transcribe). WORKER HYGIENE (hard): fixtures/smokes in mktemp ONLY; a worker NEVER git add/commit/init in a checkout/worktree; live legs READ-ONLY.

RUN MODE: subagent-delegate; THIN CONDUCTOR, never run a sub-goal inline. HOMES: dwarves-kit = 03K/04/05K/07/08/09 (kit-adopted); ops-toolkit = 03R/05R/06. CROSS-REPO: dwarves-kit sub-goals use HAND-MADE worktrees (`git -C <kit> worktree add ...`), NEVER Agent isolation:worktree. Wave 1 (parallel): ops 03R + dwarves-kit 03K+04+05K (04 EMITS the queue, 03K CONSUMES; agree `slug<TAB>repo<TAB>pointer`; 05K = new `tools/` top-level, disjoint from lib/). Then: dwarves-kit 07 (waits 04, base master) -> 08 (stacks on 07); ops 05R (waits 05K) + 06 (waits 03K); dwarves-kit 09 LAST (after 03K/04/07/08; symlink-shims lib/, full suite = gate). CONSUMER seam: kit tools read `boards.txt`+Hermes target via `--repo-root`/`_repo_root()` (NOT CONSUMER_ROOT, a wrong earlier assumption); NO personal data in dwarves-kit.

TIER 0: both kit-adopted: read AGENTS.md+WORKFLOW.md, lane-classify, record gate-ledger phases before push.

TIER 1: SKIP grill/think/design re-asks; framing done (design docs + DECISIONS 2026-07-05).

TIER 3: build to the NAMED Proof. OVER-TEST 03K/04/05K/07/08: test-plan, NCs, COVERAGE-DELTA. Absolutes: 03K LAUNCHES the real UI (send-keys `/goal`; Computer Use fallback), NOT headless claude -p; completion = line-anchored marker (stub mux in tests); error-twice stops the night; honest-zero/empty; queue text NEVER through a shell (argv-exec, NC); tokens ALLOW-LISTED (boards.txt repos; pointers under _meta/megagoals/ or .claude/goals/); board = BASH kit, config-driven, byte-identical render (NC); mirror/writeback = `hermes kanban` CLI ONLY (no SQL/DuckDB), opted-in repos only (trading/family-office OFF), chore/board-sync PRs, row-hash git-wins, missing snapshot = REFUSE ALL; rung-4 VERDICT:SECURE on 03K + 04 + 08; workers NEVER touch live Mini/BACKLOG (live mirror = demo ONLY; 08 = fixtures); tests NEVER hit real APIs (stubs).

MERGE: auto-bottom-up, gated-final. NEVER merge 06, 08, or the final PR (hold for Han). Retarget child PRs BEFORE deleting a merged parent. Never merge red CI or CHANGES_REQUESTED.

CONVERGENCE GATE: integration-check vs the objective (/kit:verify + review lenses + advisor P5/P6 + recheck), then the DEMO: the Air live-smoke queue journal row + a `board queue --dry-run` render + the FIRST live Hermes mirror (x2, 2nd empty). Write RUN_REPORT.md + render it in chat. Before complete: one-line _meta/LAB_LOG.md on the last sub-goal's branch + flip rows ID-274/ID-275 with PR refs.

STOPS: gates 06/08 + held final PR (STOP /goal MANUALLY), all-blocked-unchanged, token budget. Fingerprints in NOTES ## Active blockers; Event log gets the final summary once. One PR per sub-goal; a checked box carries `PR #N`.
