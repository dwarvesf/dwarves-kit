Objective: make the dwarves-kit orchestrator run big mega-goals context-hygienically AND observably , `orchestrate.sh` honors the per-sub-goal `Model:` on delegate (planning->opus), delegated `claude -p` sessions still emit EVERY ledger (gate/proof/token/debt), token capture works WITHOUT dumping the child transcript into the conductor, a real mega TIER-4 close runs the verifiers before the human gate, wavefront waves are watchable in tmux/cmux panes. 5 sub-goals in `_meta/megagoals/orchestrate-hardening/`.

RUN MODE: DELEGATE (5 sub-goals). You are the THIN CONDUCTOR: do NOT run a sub-goal INLINE (inlining fills THIS context , the 873k ceiling this work answers). Per sub-goal, make ONE Bash call to a fresh `claude -p` (plain `-p`, NEVER `--stream`/`--verbose` piped to you) running the full /spec->/spec-validate->/kit:execute->PR lifecycle in ITS OWN context, printing ONLY a terse line (box, PR #N, proof). Pass `--model` from the goal's `Model:` field. Absorb, verify the box, merge bottom-up, continue; TIER-4 likewise. RUN CONTRACT: `ops-toolkit/_meta/megagoals/OPERATE.md` is BINDING , decorated progress strips at check-ins + visible close (RUN_REPORT.md + timeline in chat), formats there.

GATE ZERO (turn 1). ADR-0032 (`docs/decisions/0032-megagoal-execution-hygiene.md`) must be ACCEPTED (Proposed, PR #131). Check its Status; if not, STOP for Han's bless (ADR-0028 pattern). Sequenced AFTER kit-face SG-03 (token) + understanding-gate SG-02 (debt) , they DEFINE the ledgers this hardens.

PRECONDITION. cwd IS `~/workspace/tieubao/dwarves-kit` (kit-adopted , read AGENTS.md + WORKFLOW FIRST). Pre-flight `which gh claude tmux`. Read ROADMAP (`## Assumptions` + `## The delegate run model` BINDING; `## Open forks` /spec-resolved), NOTES.md, every `goals/NN`. Code + PRs land in `dwarvesf/dwarves-kit`.

SOURCE OF TRUTH. ROADMAP.md = done + which PRs belong; each goals/NN is immutable; ADR-0032 + the research note are intent. HARD CONSTRAINTS: `/goal` stays the OFFICIAL outer loop (delegate changes what it does, not the runner); delegate = plain `-p`, `--stream`-to-FILE only for 02; gate/proof ledgers ALREADY work under delegation (verify + document, do NOT rebuild); multiplexer OPT-IN; minimum-infra.

MERGE POSTURE (gh stacked + auto-bottom-up + gated-final). Stack: 01 off `master`, each next bases the prior, 05 LAST. One PR per sub-goal. Merge bottom-up once gates pass (Done= + gate-ledger + green CI + proof with CAPTURED evidence + NCs green); retarget children before deleting a parent. FINAL PR (05) HELD for Han.

PER SUB-GOAL (01-04 independent; 05<-ALL). `bash lib/lane-classify.sh classify`, run that lane; `/spec` + `/spec-validate` BEFORE code; `/kit:execute`. OVER-TEST 02 + 03 via `/kit:test-plan` with a COVERAGE-DELTA row (02: usage from the child FILE + a NC that piping the child stream to the conductor would bloat it + conductor stays lean; 03: the mega-close RUNS integration-verifier + review-team + advisor before the gate + a no-orphan NC). Each named NC is load-bearing. Gates via `bash lib/gate-ledger.sh record` PER-GATE. Branch `feat/oh-NN-<slug>`. ROADMAP line -> `PR #N` on open; `[x] ... merged <sha>` on merge.

TIER-4 CLOSE (after 01-04 merge, before 05/final): integration-verifier against the objective + a NO-ORPHAN CHECK over every flag/step/path this wave adds , defined-but-never-dispatched = blocking (c6fbd99 class); `/kit:review-team` (security lens); advisor BOTH modes (over-suggest -> NOTES `## Proposed additions`). Fix, re-verify, hold final, then the visible close per OPERATE.md.

HARD RULES. A checked box is `[x] ... PR #N ... merged <sha>` or unchecked; "CI green" = the OPEN PR's checks. Never rewrite Done=/Outcome (append `## Notes`); no new sub-goals mid-loop (-> NOTES Proposed additions). First stop appends to NOTES `## Event log`; later stops emit only `LOOP BLOCKED , STOP /goal MANUALLY`. Audit every PR # before claiming success.

STOP. Success = 01-05 merged (05 held as final), TIER-4 clean, final PR HELD for Han. Else stop on a genuine blocker (gate-zero unmet, unchanged prerequisite) or token out. CLOSE-OUT: draft an ops-toolkit `_meta/LAB_LOG.md` entry (slug, 5 sub-goals, PR range, lessons) + a NOTES line.
