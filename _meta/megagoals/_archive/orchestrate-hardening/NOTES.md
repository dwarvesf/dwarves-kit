# NOTES , orchestrate-hardening

## Active blockers

- [GATE ZERO] ADR-0032 (dwarves-kit `docs/decisions/0032-megagoal-execution-hygiene.md`) is PROPOSED (PR #131 dwarvesf/dwarves-kit), not yet Accepted. The pointer's turn-1 checks its Status; if still Proposed it STOPS for Han's bless (ADR-0028 gate-zero pattern). This mega-goal EXECUTES the ADR; it does not re-decide it. Soft sequencing (non-blocking on the ADR itself): sequence AFTER kit-face SG-03 (token ledger) + understanding-gate SG-02 (debt ledger), because 02 reconciles token capture against kit-face's ledger and the delegated-ledger guarantee is stated over both , do not stack ahead of the ledgers whose delegation-survival is the thing being proven.

## Proposed additions

- 2026-07-03 (TIER-4 advisor over-suggest, wave 01-04): six follow-ups surfaced, NONE blocking (routed here, not implemented mid-loop per the no-new-sub-goals rule). Highest-leverage first:
  1. **TIER-4 verifier accumulation (from SG-03):** `_build_close_prompt` runs integration-verifier + review-team + advisor in ONE `claude -p` context , reintroduces the marathon-accumulation the per-sub-goal design avoids, at the highest-stakes boundary. Split into 3 fresh sessions + a terse aggregator. Medium. (Conductor note: this session dodged it by running the close lenses as separate fresh subagents; the ENGINE still has the single-prompt shape.)
  2. **Wave-path token-ledger hook (already a declared gap):** `_wave_run` has no per-sub-goal TOKENS extraction; if `WAVE_CAP` defaults to waves-on, most real runs fly blind on per-sub-goal cost. Highest-value gap-closer. Medium. VERIFY the WAVE_CAP default (advisor claimed 2/on; SG-04 comments say 1/off , reconcile).
  3. **`.orchestrate/*.stream.jsonl` retention:** capture files (--stream/--capture-tokens/DETERMINISTIC_HANDOFF) never pruned , unbounded local-disk transcript exposure (may include pasted secrets). Age/count cap swept at `next`/close time. Small-medium.
  4. **`Model:` allowlist validation (SG-01 follow-up):** `_route` passes the goal-file value straight to `--model` with no validation; a typo dies mid-wave as an opaque `claude` exit instead of a pre-flight error. Small.
  5. **Watchdog-path token capture:** the `WATCHDOG_STALL_SECS>0` branch bypasses `$slog`, so a flaky/long session (exactly the watchdog's case) loses token accounting under `CAPTURE_TOKENS=1`. Small-medium.
  6. **Happy-path tmux window cleanup (SG-04 follow-up):** the wave reap-success branch never `kill-window`s, so completed panes accumulate in the shared tmux session across a multi-wave run. Small.
  7. **Promote the 3 inline-comment declared gaps to real BACKLOG.md rows** (they live only as code comments today).
  8. **SG-04's worker recorded NO run ledger** (no `oh-04-*` rid exists) , its gate coverage is unauditable post-hoc (RUN_REPORT row = `?`). Workers must record under the expected rid even when the lane is light; consider a conductor-side check (dispatch fails the box-verify if no rid log appeared). Small-medium.
  9. **Auto-open viewer tabs (push model), OPERATOR-REQUESTED 2026-07-03** , SG-04 shipped the pull model (headless tmux windows; operator attaches). Add a `PANE_VIEWER` seam: on each `_pane_spawn`, also open a viewer surface attached to that window , `cmux` (`cmux new-surface ... 'tmux attach -t <sess>:<win>'`; NEVER `--focus true` into the controlling pane, known RPC-wedge), `kitty` (`kitty @ launch --type=tab`), `wezterm` (`wezterm cli spawn`), `ghostty` (`open -na Ghostty --args -e ...`), `iterm`/`terminal` (osascript). **Default = PUSH (operator decision 2026-07-03): `PANE_VIEWER=auto` detects the running viewer (cmux socket -> TERM_PROGRAM -> kitty/wezterm env) and auto-opens; `PANE_VIEWER=none` opts back to pull; headless/CI (no TTY, no GUI) auto-degrades to pull silently.** tmux stays the SUBSTRATE; viewers are disposable attachments, so the run never depends on the GUI. Noise controls: open per WAVE not per worker (or cap), reuse an existing viewer tab if attached. NOTE: iTerm2's `tmux -CC` control mode already gives one-native-tab-per-window with ZERO orchestrate.sh code , document it as the free path before building the seam. Small-medium.

- 2026-07-03: the delegated-ledger reconciliation (SG-02) is the SAME additive-marker discipline shared across the kit (kit-face TOKENS + understanding-gate debt + the gate/proof/run lines). SG-02 does NOT invent a new capture convention , it reconciles the EXISTING `TOKENS` marker with the plain-`-p` delegate rule via stream-to-FILE. If token capture and the ledger-observatory (ops-toolkit) reader disagree on the marker shape, defer to the SG-01 schema doc of ledger-observatory (the canonical ledger-event schema), do not fork a second shape.
- 2026-07-03: TIER-4 mega-close (SG-03) gives orchestrate.sh the close it lacks today (it prints "done" and returns). Open-fork 1 (step vs final auto sub-goal) is a /spec decision; if it lands as a final auto sub-goal, it REUSES the existing lifecycle + gate ledger rather than a bespoke close path , prefer reuse over a new engine (the ledger-observatory ETL lesson: a handful of existing pieces, not a custom engine).
- 2026-07-03: the multiplexer (SG-04) is the terminal analog of the browser-tab pattern , tmux/cmux is the control plane, ghostty is only the display. It ADDS visibility + intervention over the ALREADY-headless wavefront (ADR-0030) dispatch; it is NOT required for orchestration. Surface a possible FUTURE enhancement (a single cmux dashboard pane aggregating all wave panes) to the advisor over-suggest at TIER-4; Phase 1 spawns one pane per wave session only.
- 2026-07-03 19:20: SG-03 (TIER-4 mega-close) should EMIT A RUN REPORT as part of the close, not just verify-and-return (operator ask). One markdown artifact per run, landing IN the mega-goal dir (e.g. `RUN_REPORT.md`, newest-first sections like NOTES): waves x sub-goals table (model tier, tokens, duration, PR #, gate coverage), a callable-stack tree (conductor -> worker -> nested agents), and totals vs the token ledger. Evidence sources by mode: delegate = stream-to-file jsonl + TOKENS markers (SG-02); subagent-delegate = task-notification `subagent_tokens` + the subagent transcript jsonl (`~/.claude/projects/<slug>/<session>/subagents/agent-<id>.jsonl`, verified on disk). RENDERING reuses ledger-observatory's query+render primitives WHEN they exist; until then emit plain markdown (do not build a second renderer , the observatory's NOTES carries the mirror entry). ROADMAP/NOTES stay the narrative log; the run report is the machine-shaped summary next to them. DATA-VS-RENDER POLICY (operator, 2026-07-03): the committed artifact is MARKDOWN ONLY (small, diffs well); html/visual renders are DERIVED and never committed (repo-fat guard) , regenerate from the ledgers on demand (DuckDB reads the pipe-logs in place, proven live) or publish as a hosted Artifact and note the URL in the md. Diagrams INSIDE the md are ASCII/box-drawing in fenced blocks (operator preference: renders in terminal + GitHub + Obsidian alike, no mermaid/renderer dependency) , the md carries a text Gantt (1 col = N min, `█`/`▒` by model tier, wavefront-projection ghost lane), a gate-coverage matrix, and the callable-stack tree. Worked example: `_meta/megagoals/kit-face/RUN_REPORT.md`.
- 2026-07-03 19:55: SG-04 pane content, LIVE GATE/LANE PROGRESS (operator ask). Both data sources already exist: `gate-ledger.sh plan <lane>` = the ordered required phases; the worker's rid log under `runs/` = phases recorded so far. Their diff IS the progress strip: `spec✓ grill✓ sv✓ build▶ · review ship` (done / current / remaining). SG-04 should give each worker pane a one-line status strip (watch/tail over the rid log rendered through the plan), plus optionally one aggregate strip pane for the whole wave. The CHAT half is already wired: the conductor renders the same strip at wave check-ins (template, dotfiles a4fad84) and the TIER-4 close now emits RUN_REPORT.md + renders timeline/totals in chat. OPTIONAL follow-up (not SG-04 scope; advisor over-suggest material): a kit-aware Claude Code STATUSLINE showing `rid · lane · gate 5/9 build · next review` for interactive /kit sessions, same two sources.
- 2026-07-03 18:16: third run-mode candidate, SUBAGENT-DELEGATE (probe-grounded). Live capability probe from the Air session: a general-purpose subagent HAS the Agent tool and a nested dispatch succeeded cleanly (nesting works), so Agent-tool workers keep the kit's internal verifier/reviewer fan-out; parallel dispatch + `isolation: worktree` + per-agent `model` already exist. That makes "/goal conductor dispatches each ready sub-goal as a parallel background subagent, waits the wave, merges, next wave" a viable in-session wavefront (operator keeps the start-in-my-session habit). Bonus for SG-02: the harness task-notification returns per-worker `subagent_tokens` NATIVELY (probe: 84075), an alternative token-capture channel needing no stream-to-file parse , reconcile it against the TOKENS marker schema, do not fork a second shape. Honest deltas vs claude -p workers: workers die with the conductor session (PR/roadmap state recovers), scheduling is model-driven not deterministic, and the native usage number is a total (no per-phase detail). Route to SG-01/SG-02 /spec + SG-05 docs as an OPTION, not a replacement.

## Event log

2026-07-03 HH:MM · scaffold · orchestrate-hardening mega-goal created from dwarves-kit ADR-0032 + `ops-toolkit/research/2026-07-03-megagoal-execution-hygiene.md` (the execution-economics half of the 873k-context session). Builds the four gaps ADR-0032 section 5 obliges the kit to close: model-routing enforcement (01), token-capture stream-to-file under delegation (02), a first-class mega TIER-4 close (03), multiplexer panes (04), plus honest docs-wiring (05). 5 sub-goals, gh-stacked (linear 01<-02<-03<-04<-05 for merge ordering + shared-file `orchestrate.sh` conflict avoidance) + auto-bottom-up + gated-final, RUN MODE = delegate (thin-conductor per the plan-for-mega-goal run-mode option this mega-goal itself hardens). Work repo = dwarves-kit (roadmap here in ops-toolkit; any pointer-template touch is a dotfiles half). GATE ZERO: ADR-0032 must be ACCEPTED (Proposed, PR #131); sequenced after kit-face SG-03 + understanding-gate SG-02 (they define the token + debt ledgers this hardens). Over-test 02 (usage from file + conductor-stays-lean NC) + 03 (mega-close runs the verifiers before the gate + no-orphan NC). 3 forks surfaced non-blocking (TIER-4 placement, pane driver tmux/cmux, model-routing enforcement site) for /spec.

2026-07-03 17:54 · gate-zero STOP (run 1) · /goal loop started in delegate mode. Turn-1 gate-zero check: ADR-0032 Status = Proposed (PR #131 dwarvesf/dwarves-kit is OPEN, mergedAt=null, no reviewDecision). Per the pointer's ADR-0028 gate-zero pattern the loop STOPS for Han's bless before any sub-goal dispatch , this mega-goal EXECUTES ADR-0032, it does not re-decide it. Preconditions otherwise green (cwd=dwarves-kit, gh+claude+tmux present, on master, clean tree). No sub-goal, branch, or PR created. Unblock = accept ADR-0032 (flip Status to Accepted + merge #131), then re-run /goal.

2026-07-03 18:25 · operator decision · subagent-delegate is now the DEFAULT run-mode in the plan-for-mega-goal template (dotfiles f64def8; probe-grounded per the Proposed-additions entry above). claude -p delegate stays the pick for unattended runs + tmux-pane visibility (SG-04 attaches to OS processes, which only that mode has); inline stays for <= 4. SG-05 docs must reflect default status. SG-04 tmux scope, refined: FULL panes (watch + send-keys intervene) = delegate-mode `claude -p` workers only (they are OS processes). Subagent workers CAN still get READ-ONLY panes: each background subagent writes a live JSONL transcript to `~/.claude/projects/<slug>/<session>/subagents/agent-<id>.jsonl` (verified on the 2026-07-03 probe), so a pane tailing that file (through a small jq formatter) gives watch-only visibility; intervention for subagents routes through the conductor (SendMessage), never the pane. Driver stays an open /spec fork (tmux vs cmux); note tmux-inside-a-cmux-pane composes (control plane tmux, display cmux/ghostty).

2026-07-03 18:02 · gate-zero CLEARED (run 2) · Han blessed ADR-0032 via "proceed". Discovered the pointer's premise was stale: PR #131 is NOT the ADR PR , its branch (docs/adr-execution-hygiene) fell 7 commits behind master and no longer contains the ADR (the ADR had already landed on master as Proposed via #132); #131's live diff is unrelated persona-lens SPEC-109 work. Merging #131 would NOT have cleared gate-zero. Instead recorded the bless with a clean one-line flip Status: Proposed -> Accepted on a fresh branch (docs/accept-exec-hygiene) -> PR #138 -> squash-merged to master (257999f) after required CI (test macos/ubuntu) went green. ADR-0032 now Accepted on master. FOLLOW-UP (not blocking): stale PR #131 should be closed/retitled (it is mislabeled as the ADR PR). Proceeding to delegate dispatch of SG-01.

2026-07-03 20:30 · wave 01-04 MERGED + TIER-4 close underway · SG-01 #139 (44d2b8f) model-routing; SG-02 #140 (a5393de) lean token capture; SG-03 #141 (346fe1e) TIER-4 mega-close; SG-04 #142 (d3e63ef) opt-in tmux multiplexer. DELEGATE KILL PATTERN: SG-02's delegate finished + committed then auth expired pre-push (recovered commit 8b1db9b -> #140); SG-03's delegate killed but had committed (recovered 6041737 -> #141); SG-04's delegate killed twice, 2nd left a stash + a worktree with the full uncommitted work (spec+tests+orchestrate.sh), tests green, so conductor wrote the missing proof-of-done + committed on its behalf (3e373e7 -> #142). Lesson: the "commit before push" delegate instruction + worktree recovery saved 3 of 4 sub-goals from repeated background- kills. TIER-4 CLOSE: mechanical NO-ORPHAN sweep CLEAN , every new surface reaches a live dispatch (--model @923/1455; CAPTURE_TOKENS branch @666; _tier4_close called from the real run terminal @1421 not just the dry-run preview; MULTIPLEXER/TMUX_CMD @943/783). Integration trace coherent (4 disjoint orchestrate.sh concerns, objective met). Security-lens + advisor(over-suggest) dispatched as fresh-context review subagents over the wave diff 257999f..HEAD (avoiding the delegate-kill exposure); findings pending.

2026-07-03 20:42 · TIER-4 close CLEAN · no-orphan sweep passed (all 4 surfaces reach live dispatch). Security lens found + this session FIXED a CRITICAL command injection in SG-04 _pane_spawn (tmux new-window joined-string -> $SHELL -c re-parse; route_flags from unsanitized Model: header; hostile mega-goal PR runs host cmd under MULTIPLEXER=1). Fix = pass argv after -- (exec-direct, no re-parse), PR #143 merged 30a7f96, with a faithful injection NC (D1 positive control injects, D2 fixed path inert). Other 3 surfaces clean. Advisor over-suggest: 6 follow-ups routed to ## Proposed additions (none blocking). Proceeding to SG-05 docs (final, HELD for Han).

2026-07-03 21:01 · SG-05 done + mega-goal at terminus · SG-05 docs (WORKFLOW.md + AGENTS.md delegate run model + ledger-under-delegation guarantee + opt-in multiplexer) PR #144 OPEN, CI green, HELD for Han (gated-final). test-docs-wiring.sh 25/25: no-orphan sweep confirms each documented capability has a LIVE call site (Model->--model @923/1455, token-records via gate-ledger, TIER-4 invoked from run terminal @1421, pane spawns under MULTIPLEXER=1), AC10 over-claim NC catches a planted default-on claim. MEGA-GOAL COMPLETE pending Han: 01-04 + the #143 security fix MERGED; TIER-4 close clean; ONLY remaining step = Han merges #144.
