# WARM decisions ledger (append-only)

- **01 (RUN CONTRACT resolution order, settled/live):** a project with a local OPERATE.md OVERLAY next to its scaffolds (e.g. ops-toolkit `_meta/megagoals/OPERATE.md`) references THAT; ANY OTHER project references the skill's PORTABLE copy `~/.claude/skills/plan-for-mega-goal/references/OPERATE.md`. Confirmed live on dotfiles `main` (shipped in #194 after the thin+GUIDE split #193); the "re-apply against thin+GUIDE" premise did not hold, so PR #195 is verification-record-only (zero functional diff on the three in-scope files).
- **03 (deviations, non-blocking):** `References:` implemented as a top-metadata field (peer to `Generated:`/`Status:`/`Lane:`), not a `## References` section, matching the colon-terminated framing the grep proof targets. `Post-condition:` added only to meta-agent Mode C (ephemeral immediate-dispatch, where caller-side verification matters most; avoids retrofitting hand-written `agents/*.md`). New `tests/test-references-field.sh` + 2 fixtures NOT wired into CI, consistent with ~16 other unwired test files in the repo (the sweep test in 05 is where the no-orphan invariant gets locked). `research/2026-07-04-fable-unknowns-absorption.md` was absent in the worktree (ops-toolkit-only); SPEC-137 written self-contained.
- **02 (item-3 skipped, boundary not scale):** the session-age nudge (ID-254) hook lives in `ops-toolkit/tools/cc-context-hooks/` (binary `cc-context`), NOT dotfiles; dotfiles only manages the settings.json entry that CALLS it. Editing the elapsed/compaction logic would breach the sub-goal's "do not touch ops-toolkit" fence, so it was skipped and routed to Proposed additions as a small ops-toolkit follow-up. Other 4 items PASS, chezmoi-clean (source==applied sha256 on all 4 files).
- **02 (INCIDENT, resolved):** the worker's first `chezmoi apply` was NOT path-scoped; chezmoi's `run_onchange_*` scripts write real `$HOME` regardless of `--destination`, so it briefly reinstalled a pinned pkg, added 3 stray hook entries to LIVE `~/.claude/settings.json`, and regenerated the dotfiles-watcher plist (bootstrap failed harmlessly). Worker restored settings.json from the installer `.backup` and verified byte-identical; conductor independently re-verified (valid JSON, zero duplicate UserPromptSubmit hooks). PROCESS LESSON for plan-for-mega-goal: any dotfiles verification via chezmoi must scope to explicit target paths (`chezmoi apply --source <wt>/home --force <paths>`), never an untargeted apply, in this shared-live-config repo.
- **08 (Consolidate-mode refinements from real archives):** (1) the design's steps 2/3 assume a `## Touches` section, but real archived sub-goals encode scope in a single `**Where:**` line (older ones omit it, scope only inferrable from directional-outcome prose). Step 1 must tolerate BOTH shapes; step 2 intersects on whichever exists. (2) The highest-value re-slice signal was NOT module file-overlap but the **shared serializer file** (`gate.js` touched by 6 of 12 SGs): pulling one file's wiring into a single owner SG is what creates wave parallelism, more than module splits. Call this out as the primary re-slice heuristic. (3) Supersede is operator-confirmed, never auto.
- **08 (mirror knobs for 09):** the Consolidate-mode section lives in GUIDE.md `## Consolidate mode (remega)`: input `consolidate=<slugA>,<slugB>[,...]`; the 7 steps (GUIDE L289-300); when-NOT table (L302, >=80% done / different postures-tenants / under ~2x3 overlap); never-diverge = kit-native projection, pointer not fork. SKILL.md pointer L13 + `consolidate=` arg-hint L4.
- **04 (grill precheck thresholds, SETTLED, 05 reuses):** fire the interview when **>=2 of {S1,S2,S3} OR S3 alone**; else AUTO-SKIP. S1 territory: `git log --oneline -5 -- <paths>` empty OR newest commit >90d old. S2 domain: task nouns absent from repo code + CONTEXT.md/ADRs + specs. S3 declared: operator's own words. Unresolvable signal → treat as FIRED (fail open).
- **04 (reason= grammar, SETTLED, matches sibling kit_gates parser `line.split(" | ")[4]`):** `reason=<home-turf|density-low|operator-wave>`, exact token OR token immediately followed by `:` (e.g. `reason=home-turf: <why>`). 0 signals→`home-turf`; 1 signal→`density-low`; operator override of a fired precheck→`operator-wave` (absorbs the SPEC-058 "banks already resolved" carve-out). Enforced as a CLOSED enum in `lib/gate-ledger.sh` `record()` (prefix-glob bypass was a review-caught bug, fixed), scoped to `phase==grill && state==skipped`. Documented in `commands/grill.md` Step 4. 05's emit-sweep reuses this closed-enum-or-colon pattern.
- **06 (team-shared detection rule):** `lib/pitch.sh team-shared` runs one `gh api repos/{owner}/{repo} --jq '.owner.type'`; `Organization`→team-shared (exit 0), anything else or any gh failure/unauth→NOT team-shared (exit 1), fail-safe (a missed offer costs nothing; a spurious solo-repo nag is the exact fatigue ADR-0031 guards). No hardcoded org/username → portable.
- **06→07 (ship Step 8 ordering):** 06's pitch-offer bullet reads the significance verdict via `gate-ledger.sh show <rid> | grep '| DEBT |' | tail -1`. 07's de-escalation nudge shares NO mutable state (both only READ the DEBT line), so 07 may append after 06 safely; if 07 writes a new DEBT/ledger line it must do so BEFORE 06's read-back or as its own separate bullet after 06's.
- **07 (size floor + action format, SETTLED, 09 mirrors):** `LANE_DEESCALATE_FLOOR`=20 changed (added+deleted) lines, default, overridable via env or `--floor N`; computed `base..HEAD` + working-tree (2-source sum, not the 3-source union, `--cached` would double-count). Ledger action line: `TS | ACTION | lane-deescalate chosen=<lane> lines=<N> floor=<F> verdict=misroute-tiny` (space-separated key=value, mirrors significance-classify's debt line; uses the pre-existing `gate-ledger.sh action` verb, no new subcommand). No-block guaranteed by a `|| true`-guarded action call (verified exit 0 even on an unwritable ledger dir).
- **07→09 (mega mirror caveat):** the de-escalation nudge lives in `lib/lane-classify.sh` `deescalate()` + `commands/ship.md` Step 8, fires ONLY from the single-run `/kit:ship` path. It is NOT auto-mirrored into `/kit:mega`. 09 must check whether `commands/mega.md` delegates to ship.md per sub-goal (then it's covered) or runs its own ship/consolidate path (then 09 adds an equivalent `lane-classify.sh deescalate "$LANE" --rid <rid>` call).
- **01 grep-NC (stricter than bar):** `rg 'ops-toolkit|~' references/OPERATE.md` → zero matches; portable copy is fully generic, not even the optional-overlay mention remains in that file.
