# NOTES: kit-absorptions

## Active blockers

- (none active, both STOP-#1 blockers cleared 2026-07-04, see Event log)

### Resolved
- ENV BRICK (secret-guard hook), RESOLVED 2026-07-04: Han repaired the hook (`bash -n` clean, 1518 lines restored, Bash alive). Root cause was 02's untargeted `chezmoi apply`; the process fix (scope every dotfiles apply to explicit paths) is logged in DECISIONS.md as a skill-edit follow-up.
- CROSS-MEGA HOLD, RESOLVED 2026-07-04: harness-observatory SG-01 (kit_gates reader) merged as ops-toolkit PR #683 (`feat(ledger-observatory): kit_gates per-gate table + gate-yield`). 04+05 unblocked.

## Proposed additions

- (append-only; discovered sub-goals + skill/tooling friction land here, never inline)
- (from 09, TIER-4 to re-verify) mega.md's new `deescalate` call is dead prose until a real `/kit:mega` run invokes it (mega.md is a command SPEC, not executable code; no test exercises the call site). Integration check should confirm a live mega run actually prints the nudge when a sub-goal's diff lands under the floor, not merely that the text exists.
- (from 06) CI-PORTABILITY LESSON (orchestration): a proof test that renders a command against a LIVE just-shipped rid passes on the worker's machine (has `~/.local/state/dwarves-kit` ledger) but FAILS in CI's fresh checkout (no local state). Watch for green->red CI flips when `gh pr checks --watch` catches an early checkpoint-commit's run; always re-read `mergeStateStatus`+`statusCheckRollup` at merge time, never trust a stale watch. Fix pattern: over-test proofs must render against COMMITTED fixture rids, not live state. Worker prompts for future live-render sub-goals should require a scrubbed-HOME (`env HOME=$(mktemp -d)`) test run to catch this pre-push.
- (from 05) PRE-EXISTING GATE-EMIT GAP: `Build` and `design-record` are REQUIRED matrix gates that NO command records (execute.md narrates build escalation but never calls `record` for build; design-record is enforced statically by spec-validate Reviewer 6, never separately recorded). Named in dwarves-kit WORKFLOW.md as a known gap; out of 05's 9-command scope (no gate-requirement change allowed). Backlog candidate for a future kit sub-goal.
- (from 02) SESSION-NUDGE FOLLOW-UP: ID-254's elapsed>6h / compactions>=2 nudge could not land in dotfiles (the logic lives in `ops-toolkit/tools/cc-context-hooks/` binary `cc-context`). Proposed small ops-toolkit sub-goal: add the nudge inside that binary. Not part of this mega (kit/dotfiles only); file as a board row candidate.
- (from 01) `invocation-template.md:159` names a private path `ops-toolkit/_meta/megagoals/kit-face/RUN_REPORT.md` as a RUN_REPORT calibration example. Out of 01's scope (grep-NC was OPERATE.md-only, portability-only). Candidate for a future full-bundle private-path sweep; not blocking.
- ID-258 (portfolio scheduler v1) adopted as PRACTICE, not built: the runbook is `research/2026-07-04-megagoal-portfolio-scheduling.md` §1 and THIS run's parallel launch with harness-observatory is its first exercise. If friction appears (conductor collision, gate pile-up), log it here as input for a build decision.

## Event log

- 2026-07-04 COMPLETE. 9/9 built. dwarves-kit MERGED bottom-up: #165 #166 #168 #169 #170 #171. dotfiles HELD for Han (gate): #195 #196 #197. TIER-4 verifier PASS on all 7 objective deliverables, no cross-sub-goal seam mismatch, demo captures real (grill fixtures live, /kit:pitch on kit-emit-sweep rid, remega dry-run over safari-extension-unlock + safari-ext-enhancements). One unrelated pre-existing test failure flagged (test-classify-md-inert.sh, not a regression). Cockpit rows flipped (ID-254 parked = deferred session-nudge; rest shipped/annotated). RUN_REPORT.md written + rendered in chat. Close-out on branch chore/kit-absorptions-close (final PR HELD for Han alongside the 3 dotfiles gates).
- 2026-07-04 RESUMED. Both STOP-#1 blockers cleared: hook repaired by Han (Bash alive), sibling kit_gates reader merged (#683). WAVE 3 dispatched: Lane A re-runs 08-remega-consolidate (opus, existing worktree feat/remega-mode, prior worker died pre-commit) + Lane B starts 04-grill-conditioning (sonnet over-test, dwarves-kit, base master since 03 merged, SPEC-138), parallel.
- 2026-07-04 STOP #1 (env brick + all-blocked). WAVE 1-2 results: 03-kit-templates-batch MERGED (dwarves-kit #165, c57d5c5, SPEC-137). 01-operate-portability PR #195 HELD gate (CI green; outcome was pre-shipped in dotfiles #194, PR is verification-record-only). 02-contracts-batch PR #196 (4/5 items PASS+CI green, item-3 session-nudge skipped=wrong repo; held behind gate 01). 08-remega-consolidate BLOCKED, zero commits: the live secret-guard hook was corrupted by 02's untargeted chezmoi apply and now blocks all Bash for every worker. dwarves-kit lane 04-09 remain cross-mega-held (harness-observatory SG-01 unmerged). Two independent blockers now: (a) env brick needs Han's hook repair; (b) cross-mega hold needs the sibling's kit_gates reader to merge. Nothing the conductor can dispatch until (a) is fixed AND (b) clears. Handed to Han.
- 2026-07-04 LAUNCHED. SPEC block SPEC-137..142 reserved in dwarves-kit (03/04/05/06/07/09). WAVE 1 dispatched: Lane A 01-operate-portability (dotfiles, base main) + Lane B 03-kit-templates-batch (dwarves-kit, base master, SPEC-137), both sonnet, parallel subagents in hand-made worktrees. Cross-mega HOLD active: 04+05 blocked on harness-observatory SG-01 (kit_gates reader, unmerged, no PR#); dwarves-kit lane stalls at 03 until it merges.
- 2026-07-04 drafted (9 sub-goals, two per-repo stacks) on branch worktree-benchmark-followup; restructured from the original 8-sub-goal single mega after Han's completeness challenge (kitchen-sink batches un-batched; scope cut reversed for 250/257/259). Launch pending Han's go + the family PR merge.
