# DECISIONS, kit-foldin (WARM: durable invariants, append-only)

## 2026-07-05 SCAFFOLD (drafted from the design note; supersedes runner-fastpath SG-09)

**Origin.** This mega executes `research/2026-07-05-cc-elevation-kit-foldin-design.md`, which is BINDING: naming convention (Decision 1), kit structure (Decision 2 + the `lib/` subsystem map), the per-tool disposition table, and the 5 resolved open questions all live there. The loop does not re-decide them; it implements them.

**Rehome, not duplicate.** runner-fastpath's SG-09 (`lib/` regroup) becomes THIS mega's SG-01. runner-fastpath closes at 12/13 with SG-09 marked `[REHOMED -> kit-foldin SG-01]`. Rationale: SG-09 and the cc-* fold-in both rewrite `lib/`; doing the regroup once here (then landing `lib/session/parse-transcript.sh` on top) is one pass, not two. The taxonomy is decided ONCE (design note) and both the existing-file regroup (SG-01) and the new session parser (SG-03) follow it.

**Naming rule (invariant).** Kit artifacts are named by FUNCTION, never by host-agent. Drop `cc-` on entry. `session-` prefix ONLY for tools whose object is the agent session/transcript (agent-agnostic). Scope = kit only; ops-toolkit personal cc- config (`cc-money-gate`) keeps its name because there the prefix is accurate.

**Structure rule (invariant).** Top level = artifact TYPE, loader-mandated (`agents/*.md`, `commands/*.md` namespaced `kit:`, `skills/*/SKILL.md`, `hooks/` wired by explicit settings.json path). Subsystem grouping is possible ONLY in `lib/` (call-by-path, any depth) and `tools/` (one subtree per tool). `hooks/`/`agents/`/`commands/` stay FLAT , subdividing them = config churn for zero navigation win (ponytail).

**Consumer seam (invariant, carried from runner-fastpath).** `--repo-root`/`REPO_ROOT` flag + `_repo_root()` helper. There is NO `CONSUMER_ROOT` env var anywhere in the kit; that phrase was a wrong earlier assumption, confirmed three times. Any moved tool reading tenant config uses this seam.

**Adapter-default two-class split (carried from SG-05K).** Kit-internal sources default repo-relative (via the kit's own root); tenant-specific sources are required-explicit , missing config raises a clean error at the call site, never a silent wrong write.

**Deploy correction (invariant).** `install.sh:167-321` wires kit hooks into a consumer's `~/.claude/settings.json` at the fixed path `~/.claude/dwarves-kit/hooks/<name>.sh`. So once the 4 guards move to `dwarves-kit/hooks/`, re-running the kit installer replaces ops-toolkit's `redeploy.sh` git-archive-snapshot-symlink dance FOR THOSE FOUR. `redeploy.sh` is stripped of them in SG-07. It may still matter for whatever stays a standalone ops-toolkit tool.

**Reused runner-fastpath patterns (do not re-derive).** engine-in-kit/data-in-ops-toolkit split; NR-retire/NK-migrate paired-subgoal shape (here: N kit-landings + ONE batched ops retire-sweep, SG-07, to avoid a PR-per-tool explosion); hand-made worktrees for dwarves-kit sub-goals (`git -C <kit> worktree add`, NEVER Agent isolation:worktree , that cuts from the SESSION repo not the work repo); conductor-transcribes bookkeeping (workers report PR#/SHA/proof/decisions, conductor writes the mega dir); OPERATE.md run contract; ship-gate proof-of-done; over-test with named NCs on behavioral sub-goals.

**Merge posture.** gh-sequential (cross-repo, each branch off its repo default), auto-bottom-up, gated-final (SG-07 held for Han). SG-06 (claim-verifier) tagged `auto` but FLAGGED for a possible `gate` flip , the fan-out agent's behavior is a taste call Han may want to eyeball before merge.

## 2026-07-05 ADVISOR PRE-LAUNCH PASS (P5 critique + P6 over-suggest) , findings applied

**P5 CRITICAL 1 , the shim mechanism was a false-green (FIXED in SG-01).** The kit's lib scripts self-resolve siblings from their OWN `BASH_SOURCE` dir (`orchestrate.sh` -> `$ORCH_DIR/gate-ledger.sh`; `mega-merge.sh` `source "$MM_DIR/kit-log-dir.sh"` FATAL-on-miss). So once a CALLER moves into a subsystem dir, `$DIR/<callee>.sh` looks in the NEW dir , a shim at the old flat root is never consulted, and the busiest engines break. The obvious NC (rm the root shim) does NOT falsify it. FIX: SG-01 is now `Design: bearing` + `Model: opus`; the spec picks (a) a shared lib-root resolver or (b) per-(caller-subsystem, callee) shims; the GATE is the full suite + actually RUNNING `orchestrate.sh`/`mega-merge.sh` post-move + a resolver backstop RED->GREEN. This is the invariant: NEVER regroup `lib/` with only old-flat-root shims.

**P5 CRITICAL 2 , SG-04 scope self-contradiction (FIXED).** `cc-self-improve` is 57 files (not 22 , that was the code-file count), including `deploy/` (stays ops per Assumption 7) AND `RUNBOOK.md`/`MANUAL.md` which hardcode `~/workspace/tieubao/...` and are NOT under `deploy/`. A literal "move the whole subtree" fails SG-04's own `grep workspace/tieubao == empty` Done gate. FIX: SG-04 names the exclusion set (deploy/ stays; rewrite RUNBOOK/MANUAL paths).

**P5 MAJOR , the real hook-registration file is root `settings.json` (FIXED in SG-02).** `install.sh` COPIES hook scripts + MERGES root `dwarves-kit/settings.json`; it does NOT generate event registrations. A hook only dropped in `hooks/` is never wired. SG-02 now edits root `settings.json` (the event->command entries) + keeps it in parity with `hooks/hooks.json` (the kit's `test-meta.sh` parity check). Assumption 3 corrected.

**P5 MINORs (FIXED):** `handoff-gen` (a 271B launcher) moves WITH `handoff/` into `goal/` (added to the design-note map + SG-01); the design-note inventory's cc-observe "2 bins" typo -> "3 bins".

**P6 APPLY-NOW (FIXED):** SG-02 wires TWO independent hook manifests (root settings.json + hooks/hooks.json) + generalizes install.sh's hardcoded single-skill copy to a `skills/*` glob loop; SG-04 promotes the embedded `skills/skill-review/` to top-level `skills/` (loader-mandated, else it silently never installs). P6 RIDE-LATERs (out-of-scope tools, cross-repo history technique, multi-agent hook-event gap, SG-04+05 merge option, CI gap) -> NOTES ## Proposed additions.

## 2026-07-05 HAN FOLLOW-UP (6 items) , decisions

1. **Hard-remove, not retire-stub (reverses the retire convention for confirmed-moved tools).** SG-07 now `git rm`s each moved code dir down to a `MOVED.md` tombstone + MANIFEST row, and ALSO removes the already-moved ledger-observatory + mega-runner stubs. Safe: `git log --follow` never spans repos, so the stub preserved nothing git history doesn't already hold. See ROADMAP Assumption 6.
2. **prose-rag STAYS in ops-toolkit** (Han asked "check whether to absorb"; RESOLVED no, 2026-07-05). The RAG engine is generic (`--corpus`), but the TOOL is semantic search over Han's PERSONAL WRITING: `DEFAULT_CORPUS` = til + ops-toolkit/research + learned-ledger (`bin/prose-rag:30-32`), `consumers=[]`, tagged personal/standalone, docstring self-distinguishes from cc-recall. It fails the kit-purpose test (generic harness/agent machinery only) the same way `cc-money-gate` does , a personal-knowledge tool, not harness operation. A generic engine does not make the tool harness-shaped (grep is generic). Absorbing = dragging a personal corpus + a Python pilot into the agent-agnostic kit for speculative benefit (ponytail: no build-for-later). NOTE: two forks wrongly added it as SG-08 off a misread of "check whether" as "absorb"; reverted. If a repo-docs-RAG kit feature is ever wanted, it is a fresh clearly-scoped kit tool, not this pilot.
3-4-6. **The "kit-modularity" layer is a SEPARATE design note + a later mega, sequenced AFTER kit-foldin** (kit-foldin's lib/ regroup is its structural precondition). RE-FRAMED 2026-07-05 to the MIDDLE LEVEL (Han rejected "kit as product": full consolidation makes the kit feel abnormally big; a consumer should install + wire a-la-carte). Covers: (3) per-subsystem STANDALONE installable commands (the `board`/`orchestrate` shape) over the SG-01 dirs , NOT a `kit` uber-binary; an optional thin `kit` dispatcher is discovery-sugar only. bash-first AFFIRMED (shallow-and-wide; Python stays the DuckDB/embeddings exception); (4) enablement by INSTALL/WIRE per-module (shell) + a light `kit.toml [modules]` MANIFEST that records+tiers what's on (essential spine always wired; optional add-ons each an opt-in wire step) , NOT a monolithic runtime feature-registry every hook reads; generalizes the existing `bridge=on`/`heartbeat.every` opt-in; (6) a philosophy/README/onboarding refresh (toolbox-not-appliance framing; layered install) as the capstone once 3/4 land. Design note: `research/2026-07-05-kit-modularity-design.md`.
5. **Team-mode stays PARKED but config-aware.** [see below for RUN-time worker judgment calls]

## 2026-07-05 RUN, worker judgment calls (conductor-transcribed from worker reports)

**SG-06 fan-out contract (durable design; PR #184).** N = default 3 (odd → unambiguous majority), overridable via dispatch prompt. Threshold = majority-refute: HOLDS iff `refuted*2 <= N`, else REFUTED (identical to the predecessor CLI's `refuted*2 <= n`). Dispatch style = SINGLE dispatch, N in-context independent skeptic passes aggregated in-agent, NOT nested subagent dispatch (harness disallows subagent recursion, so the agent carries no Agent/Task tool) and NOT `claude -p` subprocesses (the replaced mechanism). Independence via distinct attack angles per skeptic (factual / logical-scope / steelman; +provenance +counterexample for N>3), fail-closed per skeptic. Cross-model: honored where the harness allows, boundary stated honestly, a single subagent runs one model so true cross-vendor is unreachable from one dispatch; substitutes are cross-angle independence in-context + cross-tier by default (sonnet vs the lead's opus); true cross-vendor is a caller-side escalation (dispatch this agent + a different-vendor skeptic, compare), out of one dispatch's scope. Tools = read-only verifier set (Read, Grep, Glob, Bash(git diff*), Bash(git log*)), no Write/Edit (ADR-0005 judge). Model = sonnet.

**SG-04 deploy-split judgment (PR #185).** Assumption 7 / P5-CRIT-2 named the PERSONAL deploy artifacts as `deploy/macos/{mini.cc-curator.plist,cc-curator-runbook.md}`; those STAYED in ops-toolkit (untouched). BUT `deploy/{install,uninstall}.sh` are generic, tested, and carry no personal hardcode, a literal "the whole `deploy/` dir stays" reading would break `tests/test-install.sh` (6 of 11 assertions), contradicting SG-04's own "11 tests pass at identical count" Done bar. Resolution: the generic install scripts MOVED with the tool; only the personal launchd plist + runbook remain in ops-toolkit. **SG-07 IMPLICATION (do not delete personal deploy):** SG-07 hard-removes the MOVED source under `ops-toolkit/tools/cc-self-improve/` but MUST PRESERVE `deploy/macos/{mini.cc-curator.plist,cc-curator-runbook.md}` (deploy-follows-source keeps the personal Mini deploy artifacts ops-side). Env-default flip: `lib/surface.sh:9` `CC_SI_MEMORY_LEDGER` default → empty; new `memory_ledger_count()` is the explicit clean-error call site. Rename map: user-facing prose/identity + runtime state dir `~/.claude/cc-self-improve` → `~/.claude/skill-curator`; INTERNAL `CC_SI_*` env names + `bin/cc-improve`/`bin/skill-review` binary names left unchanged (surgical, renaming churns tests). History preserved via `git format-patch --relative` + `git am --directory` (real per-commit, not squash).

**SG-03 parse-transcript interface + session-intel deploy (PR #188).** `lib/session/parse_transcript.py` exposes the RAW parsed JSON dict, not a wrapper type: `iter_entries(path) -> Iterator[dict]` streams one parsed object per valid line in file order (blank lines skipped; a line failing `json.loads` skipped SILENTLY, untrusted input, never raised; empty file → nothing = honest-zero; `FileNotFoundError`/`OSError` on unopenable path so the caller decides per-file-skip vs reported error). `load(path) -> list[dict]` = `list(iter_entries)` for random-access callers. A parsed "turn" is just the transcript's own entry dict, NO role/ts/text abstraction imposed (only the file-parse loop was duplicated; role/ts/text extraction differs per caller, so a `Turn` wrapper would force an abstraction neither needs). `lib/session/parse-transcript.sh` is a thin `exec python3` launcher (same shape as `lib/gate/proof-table-gen.sh`) for standalone testability; both CLIs `import` the `.py` in-process (no per-file subprocess). Extraction folded in 2 MORE duplicate loops beyond the design-note's two (cc-semantic bin, session-intel's repeat_detect), "no duplicate turn-parser remains" is genuinely true (grep: `JSONDecodeError` = exactly 2 sites, one being cc-semantic's UNRELATED LLM-response parser). De-tenanted `OPS_TOOLKIT` → required-explicit (unset → merge-proposals degrades to `_none_`). **SG-07 IMPLICATION:** retire the 3 ops copies (cc-observe/recall/intel) but PRESERVE `ops-toolkit/tools/cc-intel/deploy/` (personal launchd cron, plist hardcodes `/Users/tieubao/...`, not a generic installer; same judgment as SG-04's cc-self-improve plist).

**SG-07 retire-sweep (HELD ops PR #720).** 14 dirs hard-removed → `MOVED.md` tombstones + MANIFEST rows (10 `moved`, meta-agent/cc-workflows/mega-runner `abandoned`; incl. the already-moved ledger-observatory + mega-runner runner-fastpath stubs). Preserved: `cc-money-gate/` (untouched), `cc-intel/deploy/`, `cc-self-improve/deploy/macos/{plist,runbook}`, `cc-worktree-provision/` (deferred). **Ship-gate resolution (durable):** the `proof-ledger.sh override` escape hatch (cited from 03R/05R precedent) is now BLOCKED for source-deletion diffs by a newer guard `cc-hyg-04` (03R/05R only flipped a status flag, never deleted source). Resolution: superseded the premature override entry (annotated, not deleted, audit trail intact) and satisfied the branch's `stateful` classification via the NORMAL fresh-proof path, added a Rollback section to `docs/verification/kit-foldin-retire-sweep.md` with a dry-run-tested `git revert` (clean apply + clean abort, 0 leftover diff); `proof-ledger.sh check` passes on merits, no override. Lesson: for a hard-remove sub-goal, plan the fresh-proof+rollback path, NOT the override. **Ambiguous grep-hits (judgment):** fixed 3 stale live-pointers in `_meta/BACKLOG.md` (ID-269/ID-202/ID-117 queued rows naming dead ops paths), 2 preserved runbooks (cc-intel/cc-curator, repointed to the kit checkout), 1 auto-loaded repo-memory file. LEFT AS-IS: BACKLOG ID-266 (parked, "cc-backlog buffer" could mean the concept not the path); `_meta/INVENTORY.md` (doc-completeness tracking table, 3 hits, not a live pointer). `redeploy.sh` is host-local (see NOTES redeploy reconciliation).

**SG-01 lib-regroup resolution strategy (PR #187).** Chose (b) per-subsystem shims + root compat shims (keeps zero call-site edits, the goal's quality bar). Pure `git mv` + symlinks, committed as proper git symlinks (mode 120000) so CI on Linux resolves them. TWO shim classes: 33 root compat shims `lib/<name>.sh -> <sub>/<name>.sh` (external flat-path callers) + 21 cross-subsystem shims inside each caller's subsystem dir (defeats the false-green). KEY INSIGHT beyond the design note: the internal shims need a per-subsystem TRANSITIVE CLOSURE, when a moved caller invokes a callee via its subsystem shim, the callee runs with `$DIR`=that subsystem dir, so the callee's OWN downstream calls also need shims THERE (e.g. `queue/gate-ledger.sh` also requires `queue/kit-log-dir.sh`). Closure computed per subsystem; full suite (40/40 identical pre==post) + both engine NCs + backstop RED→GREEN confirm none missed. Proof-path deviation: goal named `docs/proof/...` but the kit ship-gate (SPEC-016) only reads `docs/verification/<slug>.md` (greps literal `NEGATIVE CONTROL` + `Verdict: PASS`); canonical proof placed at `docs/verification/kit-foldin-01-lib-regroup.md`, no duplicate. `lib/session/` exists (empty `.gitkeep`), SG-03 unblocked.

**SG-02 hooks de-tenanting (PR #186).** Env-var renames on entry (old cc- → kit function name): `CC_BACKLOG_*`→`BACKLOG_STAGE_*`, `CC_CITATION_*`→`CITATION_GUARD_*`, `CC_CONTEXT_*`→`CONTEXT_HINTS_*`, `CC_HARVEST_*`→`HARVEST_*`. The `OPS_TOOLKIT` default-path var (in cc-backlog/cc-harvest) was REMOVED entirely, replaced by the `REPO_ROOT`/`_repo_root()` seam + repo-relative `_meta/` defaults (a behavioral de-tenant, not just a rename). Shape note: the design-note inventory labeled these "bash" but they are Python, landed as `.sh` shims + `.py` logic. Out of scope (hooks only, not ported): cc-backlog's `add-backlog` promote-CLI, cc-harvest's `--stop-trigger`. citation-guard default is log-only exit 0 (`CITATION_GUARD_STRICT=1` → exit 2 on bad ref). SG-07 must strip these 4 from ops-toolkit `redeploy.sh` on merge.

**SG-05 (PR #183).** No hardcoded ops path existed to make opt-in, the source already read `CC_PLUGINS_DIR` defaulting to `$HOME/.claude/plugins` (standard CC dir); only two doc-only worked-example mentions of Han's marketplace checkout were generalized. `tool.toml` aligned to the kit's `summary` field (not ops-toolkit's `description`) to match `ledger-observatory/tool.toml`. lane-classify returned `normal` (goal file guessed `small`). Already fully designed 2026-07-04 ("attestation, not sync", kit BACKLOG L185: actor= on gate rows, Owner on board rows, ship emits run-table onto the branch, consumer CI re-checks, git-only shared medium). Tripwire = a named second user on a real repo , NOT fired. The kit-modularity note reserves a `[modules] team_mode=false` slot (a not-yet-installable add-on) + names it in the philosophy doc; no build until the tripwire fires. Full design: ops-toolkit `research/2026-07-04-pxpipe-plannotator-improve-absorption.md` §5.
