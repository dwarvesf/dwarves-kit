# NOTES , kit-hardening

## Active blockers

<none yet>

## Proposed additions

- 2026-07-02: seed , VERIFY at execution whether the kit ship-layer actually auto-merges a sub-goal PR once its ship-gate passes (P3). `lib/orchestrate.sh` + `lib/gate-ledger.sh` exist, but the auto-merge-past-gate action was not confirmed present during scaffolding. If missing, it becomes a real sub-goal 08; do not silently rebuild. **RESOLVED same day:** a second pass over ADR-0028's placement table (prompted by Han) confirmed `/kit:mega` and ship-layer auto-merge are KIT-side and unbuilt , now sub-goal 08-mega-reconcile.
- 2026-07-02: seed , SPEC-089 (dynamic agent synthesis, same-run specialists) is DRAFT and token-optim-v3-adjacent. It overlaps 04's "specialist reviewer" idea but is a SEPARATE effort (per-task worker synthesis, not review). Keep out of this mega-goal; revisit if 04's meta-agent scaffolding surfaces reuse.
- 2026-07-02: audit R1 (candidate sub-goal 09) , **RE-HOMED to dwarves-kit board ID-082** , gate-ledger durability + blanket-override ban: per-gate override reasons required (one pasted reason across 11 gates defeats the audit trail), ledger storage moved out of the plugin-reinstall blast zone (the 2026-07-01 reinstall wiped all prior records) , from `research/2026-07-02-process-effectiveness-audit.md`; untrustable gate telemetry is the same failure class this mega-goal targets.
- 2026-07-02: audit R2 (run instrumentation) , run THIS mega-goal as the handoff-layer benchmark: per-sub-goal `claude -p` sessions give clean per-unit token/turn/outcome data; A/B `DETERMINISTIC_HANDOFF` on/off if feasible , the HANDOFF/DECISIONS contract has ZERO production runs to date, so this run is its first real exercise; capture the data or the technique stays unmeasurable.
- 2026-07-02: audit R3 (ops-toolkit process trim, Han sign-off) , **RE-HOMED to ops-toolkit board ID-234** , LAB_LOG entries to one-liners (the session-closer hook checks diff presence only, never content), drop impl-notes for non-spec work, fold FEEDBACK.md into NOTES Proposed-additions , audit: LAB_LOG/impl-notes are write-only; FEEDBACK logged the same worktree friction 3x over 9 days with zero fixes while Proposed-additions demonstrably get read.
- 2026-07-02: audit R4 (ops-toolkit process trim) , **RE-HOMED to ops-toolkit board ID-235** , stop minting bookkeeping-only PRs; bookkeeping rides inside the feature PR (dwarves-kit model: 34.9% vs 83.8% median process lines) , 5 of the last 10 merged ops-toolkit PRs were pure bookkeeping about other PRs.
- 2026-07-02: audit R5 (session hygiene, global) , **RE-HOMED to ops-toolkit board ID-236** , cache-read is the real cost center (772M cache-read vs 1.59M uncached, 485:1): split mega-sessions, watch same-file re-read churn (one file read 21x), avoid uncached router models for long sessions; fresh-context subagent dispatch is CHEAP (1-11% of session tokens), keep dispatching.

## Event log

2026-07-02 HH:MM · scaffold · kit-hardening mega-goal created from ADR-0028 (team-blessed 2026-07-01) + ADR-0029. 7 sub-goals, integration-branch + gated-final, cross-repo (roadmap in ops-toolkit, PRs in dwarves-kit).
2026-07-02 HH:MM · audit · process-effectiveness audit filed (`research/2026-07-02-process-effectiveness-audit.md`); independently validates SG-04 (task-verifier is the proven weakest review link) and SG-07 (live-environment behavior is the biggest recorded miss class); 5 ranked actions R1-R5 filed under Proposed additions.
2026-07-02 HH:MM · correction · scaffold grown 7 -> 8 sub-goals: added 08-mega-reconcile (`/kit:mega` mirror + ship-layer auto-merge enforcement + deploy/UAT terminus). The initial "P3 out of scope" assumption under-read ADR-0028's placement table; caught on Han's prompt to re-check the kit-side records.
2026-07-02 HH:MM · routing · audit actions re-homed to the boards where they will actually be sequenced: R1 -> dwarves-kit ID-082, R3/R4/R5 -> ops-toolkit ID-234/235/236. Only R2 stays here: it is instrumentation OF this run, not separate work.

2026-07-02 · COMPLETE (first stop, success) · All 8 sub-goals built + merged into `mega/kit-hardening` past their own ship-gates, dwarves-kit PRs #101-#108 (merge SHAs: 01 f67d441, 02 5f73b8a, 03 5e6e80d, 04 e937c03, 05 bf1a930, 06 b77e1e0, 07 e89b957, 08 0f5b451). Each shipped a captured-evidence proof-of-done (green + real NEGATIVE CONTROL) + gate-ledger records; every sub-goal PR CI green. TIER-4 close on the assembled branch: full suite green (test-meta 578, test-hooks 438, + all 8 suites); integration-verifier caught 3 orphaned right-arm agents (defined but no command dispatched them) + a WORKFLOW over-claim, fixed in `c6fbd99` (acceptance/system-verifier -> /kit:verify, brief-reviewer -> /kit:think, no-orphan guard, v-model.svg rename, eval->bash -c); security review SHIP (2 LOW; 1 filed dwarves-kit ID-083). Final PR **#109** `mega/kit-hardening -> master` OPEN + HELD for Han, CI green, MERGEABLE , NOT merged (gated-final). Loop stop condition met: success = all 8 merged + final PR opened + held. R2 instrumentation: this was the HANDOFF/DECISIONS contract's first real multi-sub-goal production run; the per-sub-goal subagent-build + lead-verify + lead-ship split held; the ship-gate's spec-resolution-by-branch-slug quirk (proof-gate is the real per-sub-goal guard) is the main reusable lesson.
