Objective: split every learning-shaped loop across the three kits by ONE axis, who changes when the loop runs, so the engine knows no consumer by name. Reflect (the system changes) stays in dwarves-kit. Understand splits: the gate (that a human owes understanding on a shipped change) stays in dwarves-kit; the teacher (how the human pays it) moves to learning-kit behind one seam, `understand.teach`. Study (a human expanding) is learning-kit. Knowledge writers go to context-kit. One concept store, one flush skill. Six sub-goals live in `dwarves-kit/_meta/megagoals/learning-boundary/`.

GATE ZERO is satisfied: ADR-0036 (`docs/decisions/0036-loops-split-by-who-changes.md`) reads `Status: Accepted`. Turn 1: confirm that line. If it does not read Accepted, write `.planning/BLOCKER-adr.md` and STOP; the loop never self-blesses.

SETUP (turn 1). cwd IS `~/workspace/dwarvesf/dwarves-kit`. Read ROADMAP.md fully; its `## Names` table and `## Decisions already made` are BINDING. Read `docs/implementation-notes/learning-boundary.md` and append to it (the delta from the goal file, never a restatement). Read every `goals/NN-*.md`; each is an immutable contract. Kit-adopted: AGENTS.md + WORKFLOW.md first.

NAMES ARE THE QUALITY BAR. One word per loop wherever it appears (stage, subsystem, seam key, lane, skill directory, doc heading): Reflect, Understand, Study. The `## Names` table says which spelling goes where. A retired word (`dev-learner`, `learn` as a subsystem, `learning-ledger` as a skill name, `session-closeout`, `session-distill`) reappearing anywhere in a sub-goal's diff is a failing proof for that sub-goal.

CROSS-REPO MECHANICS. learning-kit is `~/workspace/tieubao/learning-kit`, context-kit is `~/workspace/tieubao/context-kit`, dotfiles is `~/workspace/tieubao/dotfiles` (chezmoi SOURCE under `home/dot_claude/skills/`; two skills, `knowledge-capture` and `learn-skill`, are `symlink_*` entries into `~/workspace/claude-skills/skills/`, so their bodies move from that repo and the dotfiles half is removing the symlink entry). A foreign repo gets a hand-made worktree at `<repo>/.claude/worktrees/<slug>` via `git -C <repo> worktree add`; every read and write uses an absolute path. A move across repos is a copy with a `Moved-from: <repo>/<path>@<sha>` trailer in the new file's frontmatter and a `git rm` in the source, same sub-goal, linked PRs. Each PR's body links its siblings. dotfiles after merge: `chezmoi apply <target path>` (never bare `chezmoi apply`; an unrelated file prompts and blocks).

PER SUB-GOAL. `bash lib/classify/lane-classify.sh classify "<sub-goal>"`, run that lane. `/kit:spec` then `/kit:spec-validate` before code on 01 and 03; 02, 04, 05 are moves and docs, spec only if the lane demands it. Build with `/kit:execute`. Converge to the named Proof; the negative control in each goal file is load-bearing and a proof without it is failing. Behavioral change in dwarves-kit: `docs/verification/<branch-slug>.md` with a run table and a `negctl.sh` block, committed BEFORE the push (the ship-gate reads it). Docs-only change: `bash lib/gate/proof-ledger.sh override '<branch-slug>' "<reason>"` run alone, then push. Commit subjects under 72 chars, Conventional Commits, no spec or ADR ids in the subject. Never `--no-verify`, never force-push, never merge a PR the run did not open. ROADMAP line -> `PR #N` on open; `[x] ... merged <sha>` on merge; the orchestrator advances only on the box.

MERGE POSTURE. Sequential, bottom-up, one sub-goal fully merged (every repo it touched) before the next starts. 01 dwarves-kit only. 02 learning-kit first, then dwarves-kit thinning, then dotfiles. 03 learning-kit, then dotfiles and claude-skills. 04 context-kit, then dotfiles and claude-skills. 05 one docs PR per kit, then the terminus check and the archive move. `gh pr merge --squash` alone in its own command, never chained with a branch delete.

TIER-4 CLOSE (after 05): `bash bin/config seams --check` on this machine must exit 0 with `understand.teach`, `wrap.after`, `knowledge.root` all `filled`; a fresh-context integration-verifier against the objective; a no-orphan sweep (a command or skill defined but reachable from nowhere is blocking); the boundary lint green in dwarves-kit; then hold the final archive PR for the operator.

STOP. Success = 01-05 merged and the terminus check green. Also stop: gate-zero unmet, a blocker whose prerequisite has not changed, token out. Write HANDOFF.md for the next session on every stop.
