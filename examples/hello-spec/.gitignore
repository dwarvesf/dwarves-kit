# Local Claude Code user state and per-machine settings
.claude/

# Kit workflow artifacts: generated per-diff by /review and /review-team,
# consumed by /ship and /start. Transient, regenerated each run, stale after
# ship. Needed on disk during the lifecycle, never in version control.
REVIEW.md
TODOS.md
