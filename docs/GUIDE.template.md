<!--
Copy this file to GUIDE.md at the root of the shipped product (or its docs
folder) whenever the change has an end user who is not the builder. Fill in
each section below, then delete this comment block. Keep it to one page.

ELI10 register: plain everyday words, no internals, no jargon, no code. Write
it for the person who will use the thing, not the person who built it.
-->

# [Product or feature name]

## What this does

<!-- 2-3 sentences. The problem this solves for the person using it, in
     their words. No architecture, no tech names, no "under the hood". -->

## How to use it

<!-- A short numbered list: the exact actions the end user takes, in order.
     A screenshot or a one-line example is welcome. Skip any step only the
     builder needs. -->

## What to do when it breaks

<!-- 2-3 lines. The most likely thing that goes wrong from the end user's
     side, what it looks like, and the one action that fixes it or the
     person to ask. Not a debugging guide, a "try this, then tell them"
     note. -->
