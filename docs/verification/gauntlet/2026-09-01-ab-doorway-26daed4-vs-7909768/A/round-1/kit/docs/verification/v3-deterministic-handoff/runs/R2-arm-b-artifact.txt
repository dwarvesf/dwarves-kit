# R2: deterministic handoff artifact (arm B) generated from the SG-01 backoff transcript
--- HANDOFF.md (HOT) ---
# HOT HANDOFF (overwritten each sub-goal transition)

Next sub-goal: SG-02 wire retry config into CLI.
First action: Still need to wire the retry config into the CLI flags before this ships.

Read-pointers (files this run actually touched):
- `src/fetch_client.py` -- touched (2 edits)
- `tests/test_fetch_client.py` -- touched (1 edit)

Outstanding hit this run: TODO: document the backoff envvar in the README.. See DECISIONS.md for the full ledger.
--- DECISIONS.md (WARM) ---
# WARM LEDGER (append-only: invariants + dead-ends, read on demand)

Never inlined into the prompt; the orchestrator injects only a pointer to this file.
Blocks below are generated deterministically from each sub-goal's transcript.

<!-- handoff-gen:0fa456486860 -->
## 2026-06-30 SG-02 (deterministic handoff)

### What this run was
- Add an exponential-backoff retry wrapper to the HTTP fetch client so transient 5xx errors are retried automatically.

### Invariants / decisions (from this run)
- Going with a manual backoff loop because we avoid adding a new dependency.
- Decision: cap retries at 5 with a 0.5s base delay doubling each attempt.
- I'll use a small helper rather than wrapping every call site.

### Commits this run
- feat(client): add backoff retry wrapper

### Dead-ends / outstanding
- Still need to wire the retry config into the CLI flags before this ships.
- TODO: document the backoff envvar in the README.
- Next step is to expose --max-retries on the CLI.
