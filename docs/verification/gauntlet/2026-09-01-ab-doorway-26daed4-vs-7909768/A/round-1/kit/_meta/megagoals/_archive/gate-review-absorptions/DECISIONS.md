# DECISIONS: gate-review-absorptions

(append-only; sub-goals record load-bearing decisions here on completion: grammar, formats, thresholds, verdicts.)

- 2026-07-04 draft: plannotator integration is ONE wrapper deep (`pl-gate`), fail-open surface + fail-visible log, never a fabricated ledger line; the kit/mega flow never learns the binary's name. Rationale: bus-factor-1 upstream + commercial trajectory (research doc §2).
- 2026-07-04 draft: rejected findings are surfaced-not-suppressed ("previously rejected <date>: <reason>"), and the memory keys on finding-key match, never file alone; novel findings must always fire (02's load-bearing NC).
- 2026-07-04 draft: pxpipe NOT adopted (silent confabulation of exact strings + Max-OAuth proxy ban-risk); only its measurement methodology absorbed (05). Parked as ID-265.
- 2026-07-04 04-review-yield-lens (PR #701 ops-toolkit, HELD/final, tool-local SPEC-137 ledger-observatory namespace): FP-rate = `fp_rate_approx = n_rejected / raised`; n_rejected per-(repo,lens) from the new `rejected_findings` table, raised = GLOBAL sum of `findings=`+`rejected=` regex-extracted from `kit_gates.reason` across every `gate='review'` row (kit_gates has no lens/repo column, so per-lens is a deliberate APPROXIMATION, `approx=true` on every output row; `suppressed=` never contributes). Multi-repo source = `LEDGER_OBS_REPOS` env var (NOT boards.txt: its paths nest inconsistently). Thresholds: `--min-n` default 5; anomaly `review_fp_min_n=5.0`, `review_fp_rate_max=0.5`, propose-never-autofile. `fp_rate_approx` is NULL (never 0.0) when raised=0; rate>1.0 reported exactly, never clamped. Honest-zero NC proven by literal source-edit break (reversed JOIN + NULL->0.0 fabricated a bogus row -> RED, restore -> GREEN, source byte-identical). Real run: ops-toolkit 0 rows (honest absent), dwarves-kit/architecture n_rejected=1 raised=4 fp_rate_approx=0.25 low_n=true.
- 2026-07-04 06-advisor-visibility (PR #174 dwarves-kit, SPEC-145, merged b628549): advisor emit grammar `bash lib/gate-ledger.sh record <rid> advisor ran "mode=P5|P6 findings=N actor=<git config user.name>"` (one row per mode, fail-open with a stderr WARNING if the ledger dir is unwritable). RID CONVENTION (pinned): standalone `/kit:review-team` -> the run's own rid; mega/convergence-gate context -> the FINAL sub-goal's rid, as a STATIC value (the final sub-goal's `**Branch:**` header, `type/` prefix stripped), NEVER a re-derived shell var (the advisor-critique lens caught that `$RID` from a per-sub-goal loop does not survive to the convergence-gate step under subagent/delegate run modes). Never-diverge mirror NOT needed: the ops-toolkit plan-for-mega-goal skill (dotfiles) already carries this beat + grammar verbatim (GUIDE.md 6a, OPERATE.md, invocation-template.md; landed 2026-07-04 ahead of the kit). kit_gates parse verified: `advisor | ran=2` with `mode=P5 findings=2` + `mode=P6 findings=3` rows, zero reader-code change. NC1 honest-zero + NC2 emit-failure-never-blocks both proven with real output.
- 2026-07-04 03-plannotator-gate-trial (PR #698 ops-toolkit, HELD/gate): install = REAL verified binary plannotator v0.21.4 darwin-arm64, checksum + SLSA attestation verified (`gh attestation verify --repo backnotprop/plannotator`), binary gitignored (111MB). LEDGER GRAMMAR: `<TS> | GATE | human-gate | ran | reason=<approved|denied> findings=<N> actor=<git user.name> [digest=<text>]` (parses with kit_gates). Real plannotator decision schema is 3-WAY (approved/annotated/dismissed), NOT the contract's sketched 2-way: annotated->denied (deny-with-feedback), dismissed->fail-open (Close = no real decision, never a fabricated denial). FAIL-OPEN table: missing binary / non-zero exit / malformed JSON / unrecognized decision / dismissed -> visible WARNING + manual fallback + NO ledger write, every case. NC proven load-bearing: removing the `jq -e .` guard turned exactly bats test 5 RED, restore -> 11/11 green. Verdict PENDING (Han's live trial). Wrapper is `experiments/plannotator-gate/`; the kit/mega flow only ever calls `pl-gate`, never plannotator's name (swappable, bus-factor-1 upstream).
- 2026-07-04 02-review-findings-memory (PR #173 dwarves-kit, SPEC-144, merged 0904e74): ledger file `docs/verification/rejected-findings.md`, table `| date | lens | finding-key | verdict | reason |`, finding-key = `<slug>:<path>` (e.g. `bare-except:notify.py`). EMIT GRAMMAR (parses with kit_gates reader #683, verified live via ledger-observatory query): `bash lib/gate-ledger.sh record <rid> review ran "<verdict> findings=<K> suppressed=<S> rejected=<M> actor=$(git config user.name)"` where findings=FRESH unsuppressed count, suppressed=SPEC-081 confidence-gate auto-suppression (DIFFERENT axis), rejected=human previously-rejected matches. Pre-flag consult uses `grep -F "| <finding-key> |"` PIPE-ANCHORED (a real substring-collision bug found+fixed mid-build: bare `except:notify.py` wrongly matched `bare-except:notify.py`). Reconciliation with 03: review-finding-key (`<slug>:<path>`) and 03's gate-decision `reason=<approved|denied>` are ORTHOGONAL axes, no collision. Load-bearing NC proven: Run 2 file-path-only match wrongly suppressed a novel sql-injection (RED), whole-key match restores it (GREEN).
- 2026-07-04 01-stale-adr-inversion (PR #172 dwarves-kit, merged e54397b, SPEC-143): identical rule text in agents/advisor.md:37, commands/review.md:26, commands/review-team.md:81 (inside the dispatch fence, since dispatched lenses don't inherit). Rule wording: "Stale-ADR inversion. Behavior that matches what a spec/ADR/intent doc claims is BY DESIGN, not a finding... Code that has DRIFTED from what a spec/ADR/intent doc claims IS itself a finding: report the drift naming the doc's line and the code's line. A doc can never blanket-mute observed behavior." FINDING-KEY CONVENTION (advisor P6, needed by 02's pre-flag grep + 04's adapter): stale-ADR-drift findings carry a `stale-adr:` prefix (e.g. `stale-adr: <doc>:<line> claims X, <code>:<line> does Y`). Fixture proof used real kit:code-reviewer dispatches + a rule-stripped negative control (reviewer never opens the ADR without the rule).
- 2026-07-04 05-ops-contracts-batch (PR #697, merged 4400fce): gate-deny triage-first contract landed in `_meta/megagoals/OPERATE.md` under `## Merge posture` (after "FINAL PR is HELD for Han"): when a human gate denies, worker verdicts EVERY finding (Confirmed/Partly/Not-a-bug/Intended, each file:line evidence) BEFORE changing anything; never resubmit unchanged; keep branch + PR title stable so re-review diffs against the denial. This is the anchor 03's pl-gate deny path renders. n-rule under `## Hard rules`. Counterfactual-in-same-row + honest-negative = §5.2 items 6-7 of `research/2026-07-04-scaling-the-harness-audit.md`. Items 1-2 mirrored byte-identical (shasum 52b6f1e6) into dotfiles portable OPERATE copy (dotfiles commit d1652d2, scoped chezmoi apply on the single target only).
- 2026-07-04 pre-launch (advisor P6 suggestion, adopted): every NEW emit grammar this mega introduces (02 `findings=/rejected=`, 03 pl-gate line, 06 advisor rows) carries `actor=<git user.name>` from birth. Solo it is a constant; it is team-mode Tier-1 identity plumbing shipped while the grammars are green-field (research/2026-07-04-team-collab-workflow-proposal.md).
