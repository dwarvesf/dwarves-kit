Objective: make dwarves-kit production-facing and cost-measured , current README + mermaid hero, docs map, token-efficiency metrics in the run ledger, cheap-tier defaults, meta-agent provenance + efficacy metric, operator-persona design lens, UI done-modes with a bounded quiescence loop, and a PREPARED v2.0.0 release. 8 sub-goals live in `~/workspace/<owner>/ops-toolkit/_meta/megagoals/kit-face/`.

SETUP (turn 1). Run from a session whose cwd IS `~/workspace/<owner>/dwarves-kit`. Pre-flight `which gh` (missing = stop). Read ROADMAP.md (its `## Assumptions` block carries 25 harvest-resolved decisions , they are BINDING), NOTES.md, every `goals/NN-*.md`. Kit-adopted: read AGENTS.md + WORKFLOW.md FIRST. Sub-goals 04 + 07 carry a dotfiles half: edit the chezmoi SOURCE, `chezmoi apply`, stage+commit in ONE shell call (the watcher reverts uncommitted tracked changes).

SOURCE OF TRUTH. ROADMAP.md = what-is-done + which PRs belong. Each goals/NN file is immutable contract (Done=, Proof, deps, Model/Effort). Prior decisions the wave touches (DEC-003, meta-agent Mode B stance, SPEC-073, SPEC-087's default-path pin) are amended EXPLICITLY per the assumptions, never silently.

MERGE POSTURE (gh stacked + auto-bottom-up + gated-final). EXECUTION ORDER (docs run LAST): machinery {03, 04, 05, 06, 09} first (independent, off `master`); then 07 (bases 06's branch); then docs {01, 02} off `master` (they reflect FINAL state , diagram, agents count, index); then 08 release off `master`, created LAST. One PR per sub-goal. Once a sub-goal's gates pass (Done= verified + gate-ledger recorded with PER-GATE reasons + `gh pr checks` green + proof committed with CAPTURED evidence), the loop merges it bottom-up , before merging-and-deleting a parent branch, retarget child PRs (`gh pr edit <child> --base master`). EXCEPTION: 08 is a `gate` AND the final , open it, HOLD it, never merge/tag/publish. NEVER merge red-CI or CHANGES_REQUESTED.

PER SUB-GOAL (deps: 01+02 need ALL machinery incl. 09, run late; 07 needs 06; 08 needs ALL; 03/04/05/06/09 independent). 09 generates 8 role agents (per role-classify domain, mixed reviewer/worker), each gated by agent-effectiveness, and MUST reconcile SPEC-089 (static roster vs dynamic long tail):
- `bash lib/lane-classify.sh classify "<sub-goal>"`, run that lane. `/spec` then `/spec-validate` BEFORE code.
- Build with `/kit:execute` (V-model fires: task-verifier + integration + re-audit). Converge verify->fix->re-verify to the named Proof, bounded ~a few attempts; never check a box on a failing proof.
- Over-test substantial sub-goals: `/kit:test-plan` matrix from the spec ACs, test modes matched to the risk surface, the COVERAGE DELTA recorded as a named proof-of-done row; trivial keeps its Done-line.
- Every named negative control is load-bearing (03's usage=?, 06's 5-lens byte-compat, 07's no-false-quiescence, 08's three-surface pin) , a proof missing its NC is a failing proof.
- Branch `feat/kit-face-NN-<slug>`. ROADMAP line -> `PR #N` on open; `[x] ... merged <sha>` on merge.

TIER-4 CLOSE (after 01-07 + 09 merge, before 08 is handed to Han): integration-verifier against the objective + a NO-ORPHAN CHECK over every artifact this wave added (each new agent / lens / subcommand / flag must have a live dispatch path; defined-but-never-dispatched = blocking; a WORKFLOW.md claim with no dispatch path = blocking , this is the kit-hardening c6fbd99 bug class, do not repeat it); `/kit:review-team` with the deep `security-reviewer` lens; the `advisor` in BOTH modes (over-suggest -> NOTES `## Proposed additions`, do not build them). Fix findings, re-verify, then run 08.

HARD RULES. A checked box is `[x] ... PR #N ... merged <sha>` or unchecked. "CI green" = the OPEN PR's checks. Never rewrite Done=/Outcome (append `## Notes` only). No new sub-goals mid-loop (-> NOTES Proposed additions). Retry blocked work only when its prerequisite changed. First stop appends the final summary to NOTES `## Event log`; later stops emit only a `LOOP BLOCKED , STOP /goal MANUALLY` banner. Before claiming success audit every ROADMAP PR # via `gh pr view <n> --repo dwarvesf/dwarves-kit --json state,reviewDecision,statusCheckRollup`; unmark any box that fails.

STOP CONDITIONS. Success = 01-07 + 09 merged, TIER-4 clean, 08's PR open + HELD with the tag/Release text prepared. Also stop: genuine blocker with unchanged prerequisite, or token budget out. Else keep moving.

CLOSE-OUT. Draft a one-paragraph ops-toolkit `_meta/LAB_LOG.md` entry (slug, 8 sub-goals, PR range, lessons) + a NOTES line for the ops-side close. Then mark complete.
