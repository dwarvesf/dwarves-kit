# NOTES, kit-foldin

## Active blockers

(none; BUILD COMPLETE. All 6 kit sub-goals merged to dwarves-kit master; SG-07 held final PR #720 OPEN for Han. Convergence gate clean. STOP is the held PR.)

## Proposed additions

- 2026-07-05: DEFERRED tool , `cc-worktree-provision` , Done = it lands as `lib/goal/worktree-provision.sh` OR `tools/worktree-provision/` , off the critical path (already flagged kit-shaped in the design note); pick the shape when a session actually needs it.
- 2026-07-05: DEFERRED tool , `review-findings-memory` , Done = shape decided once it is built , it is spec-only today (`dwarves-kit docs/specs/SPEC-144`, unmerged), so its landing shape waits for its implementation, not this mega.
- 2026-07-05: `cc-money-gate` STAYS in ops-toolkit (tenant/personal-path bound); not a fold-in candidate. Recorded so a later audit does not re-flag it as "unmoved".
- 2026-07-05 (Han asked "check whether to absorb prose-rag" , RESOLVED: STAYS OPS): two forks misread the question as "Han chose absorb" and added an SG-08; the conductor REVERTED it. Evidence for stays-ops: `DEFAULT_CORPUS` = Han's personal writing (til + research + learned-ledger, `bin/prose-rag:30-32`), `consumers=[]`, tagged personal/standalone, and its own docstring self-distinguishes from cc-recall. It is semantic search over Han's PROSE, not agent/session data , fails the kit-purpose test the SAME way `cc-money-gate` does. A `--corpus` flag makes the ENGINE generic; it does NOT make the TOOL harness machinery (grep is generic too). Absorbing would drag a personal corpus + a Python pilot (consumers=[]) into the agent-agnostic kit for speculative benefit (ponytail: don't build-for-later). If Han ever wants a repo-docs-RAG kit feature, that is a fresh clearly-scoped kit tool, NOT this personal pilot. Mega stays 7 sub-goals.
- 2026-07-05 (advisor P6 #3): `mini-cc-session` + `oc-go-cc-deploy` are ALSO out of scope , cc-namespace-adjacent but personal SSH/tmux + endpoint-switch infra, NOT elevation guards/tools (absent from the design-note inventory on purpose). Recorded so SG-07's grep sweep + any future audit does not re-flag them as forgotten cc- tools.
- 2026-07-05 (advisor P6 #4, RIDE-LATER): cross-repo history preservation for SG-04/SG-07 is stated "best-effort", but `git format-patch`/`git subtree split`/`git filter-repo --path` replayed via `git am` actually preserves per-commit history in the destination. Worth ONE attempt in SG-04 before falling back to a single-commit move; log the outcome either way.
- 2026-07-05 (advisor P6 #5, RIDE-LATER): the multi-agent-future gap , the 4 new `hooks/` are wired to Claude-Code-specific event names (SessionEnd/Stop/UserPromptSubmit/PreCompact). If the kit is to serve pi/opencode too (Han's flag), this fold-in hard-bakes CC event names into the "generic" kit a second time. Known gap for whoever designs the multi-agent hook-translation layer; not a blocker for this mega.
- 2026-07-05 (advisor P6 #6, RIDE-LATER): SG-04 + SG-05 could merge into ONE PR (both auto, self-contained, disjoint tools/ subtrees, same move+env-fix shape) , same batching rationale already used for SG-07. Marginal; only if the loop has slack. Kept separate for now (clean per-tool proof).
- 2026-07-05 (Omnigent absorption, RIDE-LATER for SG-06): default the claim-verifier skeptic panel to CROSS-MODEL with the claim's producer when known (Omnigent's Polly routes each diff to a reviewer "from a different vendor than the one that wrote it"). verify-claim's CLI panel is already multi-model; carry that property into the in-harness redesign rather than N same-model skeptics. Source: `research/2026-07-05-omnigent-team-harness-absorption.md` row 9.
- 2026-07-05 (advisor P6 #7 folds into the earlier CI-gap note): SG-06's agent smoke-dispatch proof is ALSO not bash-suite-shaped , add it to the SAME `uv`/non-bash CI follow-up (SG-03 + SG-04 + SG-06), not a second one.
- 2026-07-05: coverage gap carried from SG-05K , ledger-observatory's Python/uv tests do NOT run in kit CI (`.github/workflows/test.yml` has no uv step). The session-tools (SG-03) + skill-curator (SG-04) moves ADD more non-bash tests; if the loop has budget, add ONE `uv`-based CI job covering `tools/*/tests` so these do not rot. Not a blocker; a hardening follow-up.
- 2026-07-05: SG-06 open design detail , the claim-verifier fan-out's exact contract (how many skeptics, how the majority threshold is expressed, whether it reuses `kit:code-reviewer`-style dispatch or a bespoke panel). Resolve in SG-06's spec (Design: bearing), do NOT pre-guess here.
- 2026-07-05 (convergence advisor P5, FIXED in kit PR #189): 2 tenant-path leaks that evaded the sub-goal `workspace/<owner>` greps (dash-slug + prose forms): `tools/skill-curator/MANUAL.md:104` documented `CC_SI_MEMORY_LEDGER`'s default as the ops learned-ledger path (but SG-04 made it required-explicit) → `(unset; required)`; `tools/session-observe/README.md:42` hardcoded Han's `-Users-tieubao-workspace-tieubao-ops-toolkit` project slug → `<project-slug>`. Broader dash-slug sweep confirmed these were the only live-source leaks. Lesson: the no-ops-path grep gate should also match the DASH-SLUG form (`-Users-tieubao-workspace`), not just `workspace/<owner>`.
- 2026-07-05 (advisor P6 #1, RIDE-LATER): add a copy-mode lib-install symmetry test. The `lib/` compat symlinks are RELATIVE so `cp -R` install preserves them today, but nothing pins the invariant; an absolute symlink or a shim added outside `lib/` would break moved callers ONLY in copied-lib installs (Han's local setup symlinks lib, so it passes locally and fails only for real consumers). One assertion in the existing fixture-install harness closes it.
- 2026-07-05 (advisor P6 #2, RIDE-LATER): `install.sh` installs hooks/skills/agents/lib but NOT `tools/`, so session-*/skill-curator/plugin-check are only runnable from a repo checkout. Undocumented invocation contract for a plugin-cache consumer with no checkout , add a README paragraph OR decide to symlink `tools/`. The cross-sub-goal seam no per-sub-goal review owned (SG-02 owns install.sh, SG-03/04/05 own the tools).
- 2026-07-05 (advisor P6 #5, RIDE-LATER): the deferred cost-per-merged-PR signal (session-observe/SPEC.md:58) is now one adapter away , the shared `lib/session/parse_transcript.py` + a `gh pr list --merged` join is the missing half the fold-in just built.
- 2026-07-05 (SG-07 finding, RIDE-LATER): **`redeploy.sh` full reconciliation.** `redeploy.sh` is HOST-LOCAL (`~/.local/share/cc-elevation/redeploy.sh`), not repo-tracked, so it is outside PR #720's diff by construction. SG-07 stripped the 4 kit-wired hooks host-side, but 4 MORE now-tombstoned tools (cc-intel, meta-agent, cc-workflows, cc-observe) still linger in its `TOOLS=` list → a future `redeploy.sh` run would create dangling symlinks for them. Follow-up: strip those 4 host-side too (host cleanup, low-risk, not a merge blocker). Deeper: the snapshot-deploy mechanism itself is now obsolete for everything kit-landed; a later pass could retire redeploy.sh down to only what legitimately stays ops (cc-money-gate).
- 2026-07-05 (convergence finding, RIDE-LATER): **session-observe bin-naming debt.** `tools/session-observe/bin/` files kept `cc-` prefixes (`cc-observe`/`cc-semantic`/`cc-vps-report`) vs Decision 1's drop-cc- rule. SG-03's contract explicitly protected the bin verbs ("Not: renaming the 3 bins' user-facing verbs"), and `cc-intel-weekly` inside cc-vps-report is a LIVE vps-mon heartbeat id (renaming it breaks the registration). So this was correct-per-contract, but leaves a cc- name inside a shipped kit tool. Follow-up: a dedicated rename pass (bin files → observe/semantic/vps-report, with the heartbeat id left as an external-identifier exception) if the naming purity is wanted.
- 2026-07-05 (convergence finding, pre-existing NOT this mega): `tools/ledger-observatory/` (moved in runner-fastpath, not kit-foldin) hardcodes `~/workspace/<owner>/...` in its `skill/SKILL.md` + a `config.py` example default. Recorded so a future kit-hygiene audit does not attribute it to kit-foldin; the adapter-default flip that SG-05K applied to the CODE path did not reach the SKILL.md/example strings.
- 2026-07-05 (Han, ledger/observatory design , Decision E in the kit-modularity note): SG-03's `tools/session-*` placements are PROVISIONAL. The modularity mega retires the `lib/`-vs-`tools/` split (Han: "they're the same") into self-contained per-subsystem modules, and renames `ledger-observatory` -> `stats` (Han's pick; read-side never carries "-ledger"). kit-foldin does NOT need to change , land session-* in `tools/` as planned; the modularity mega collapses `tools/` (cheap within-repo `git mv`). Recorded so a future reader knows `tools/` is not the final home.

## Event log

2026-07-05 14:00 · drafted · kit-foldin scaffolded from research/2026-07-05-cc-elevation-kit-foldin-design.md; 7 sub-goals (01 lib-regroup [rehomed runner-fastpath SG-09], 02 hooks-batch, 03 session-tools, 04 skill-curator, 05 plugin-check, 06 claim-verifier, 07 ops retire-sweep). gh-sequential, auto-bottom-up, gated-final. NOT launched; awaiting Han's go + confirmation that runner-fastpath SG-09 is marked rehomed.
2026-07-05 14:05 · rehome bookkeeping · runner-fastpath ROADMAP SG-09 -> [~] rehomed; ID-274 -> shipped (closes 12/13); new backlog row ID-276; runner-fastpath NOTES event line added.
2026-07-05 14:30 · advisor P6 (over-suggest) · 7 suggestions. APPLY-NOW ×2 folded into scaffold: (#1) SG-02 wires TWO independent hook manifests (settings.json + hooks/hooks.json) + proof greps both; (#2) SG-04 promotes embedded skills/skill-review to top-level skills/ + SG-02 generalizes install.sh's hardcoded single-skill copy to a skills/* glob loop (else the nested skill silently never installs). #3 (out-of-scope tools) + #4-#7 (RIDE-LATER) routed to Proposed additions. P5 (critique) pending.
2026-07-05 15:00 · Han follow-up (6 items) · ① SG-07 -> HARD-REMOVE + MOVED.md tombstone (reverses retire-stub; git-log-follow never spans repos so stub was moot), incl. cleanup of already-moved ledger-observatory + mega-runner. ② prose-rag -> STAYS OPS (Han asked "check whether"; evidence says personal-prose RAG, fails the kit-purpose test like cc-money-gate). Mega stays 7 sub-goals. ③④⑤⑥ -> the "kit-as-product" layer captured in research/2026-07-05-kit-modularity-design.md (sub-command facade `kit <group> <verb>` over the SG-01 subsystems [bash-first AFFIRMED, not rewritten]; `kit.toml [features]` essential-vs-optional config generalizing the bridge=on pattern; team-mode stays PARKED but config-aware, reserve `team_mode=false` slot; philosophy/README/onboarding refresh as capstone) , a SEPARATE mega AFTER kit-foldin (its lib/ regroup is the precondition).
2026-07-05 15:40 · PROCESS + REVERT · three read-only forks (prose-rag/team-mode/CLI-map) each OVERSTEPPED and edited the scaffold concurrently (a fork inherits full context + "continue the work"; two synthesized all 6 items, reconciling with each other; one was killed). Net edits were high-quality but TWO wrongly absorbed prose-rag off a misread of "check whether". Conductor VERIFIED the on-disk state (scaffold intact, no corruption), then REVERTED prose-rag to stays-ops (removed goals/08, fixed ROADMAP/pointer/NOTES/DECISIONS/backlog/kit-as-product note). Lesson: a `fork` subagent is NOT a safe read-only investigator , it will edit; use `Explore`/`general-purpose` with no write tools for read-only fan-out, or accept it will act. kit-foldin back to 7 sub-goals, launch-ready; kit-as-product note KEPT (sound).
2026-07-05 16:10 · reframe (Han) · "kit as product" REJECTED , full consolidation makes the kit feel abnormally big. Successor note re-framed to the MIDDLE LEVEL (toolbox you install a-la-carte, not an appliance you switch on) + renamed research/2026-07-05-kit-modularity-design.md. Decision A: per-subsystem STANDALONE commands (board/orchestrate shape), NOT a `kit` uber-binary (optional thin dispatcher = discovery-sugar only). Decision B: enablement by INSTALL/WIRE per-module (shell) + a light `[modules]` manifest, NOT a monolithic runtime feature-registry; install wires ONLY opted-in hooks. D: layered install + toolbox-not-appliance philosophy. Does NOT touch kit-foldin (the move/regroup is model-agnostic); successor-note only.
2026-07-05 · LAUNCHED · runner-fastpath SG-09 confirmed [REHOMED -> kit-foldin SG-01]. Wave 1 dispatched as 5 parallel subagent-delegate workers in hand-made dwarves-kit worktrees (.claude/worktrees/kf-0{1,2,4,5,6} off master@ba5e1dd): SG-01 lib-regroup (opus), SG-02 hooks-batch (sonnet), SG-04 skill-curator (sonnet), SG-05 plugin-check (sonnet), SG-06 claim-verifier (opus). Wave 2 = SG-03 (waits 01 merged). Fan-in = SG-07 ops retire-sweep (waits 01-06 merged; HELD final PR).

2026-07-05 · RUN COMPLETE (BUILD terminus) · 7 sub-goals, 8 PRs. dwarves-kit: #187 (01 lib-regroup, strategy-b shims + transitive closure), #186 (02 hooks, dual-manifest), #188 (03 session-tools, shared parser folded in 4 dup loops), #185 (04 skill-curator), #183 (05 plugin-check), #184 (06 claim-verifier) ALL MERGED; #189 gate-fix (2 tenant-path leaks the advisor caught) MERGED. ops-toolkit: #720 (07 retire-sweep, 14 dirs → tombstones) OPEN + HELD for Han. Convergence gate clean: temp-HOME install wires 4 hooks + both skills; kit suite 40/40 + all moved tool suites green on merged master; both-repo dangling-ref grep clean. Wall-clock ~85m, 133 worker-min across 7 builders + advisor. Every kit PR re-verified against the integrated tree before its bottom-up merge; zero red-CI merges. RUN_REPORT.md written + rendered. STOP = the held #720 (manual /goal stop). Post-merge: co-locate scaffold → _meta/megagoals/_archive/.

2026-07-05 14:35 · advisor P5 (critique) · 2 CRITICAL + 1 MAJOR + 2 MINOR, ALL applied to the scaffold. CRIT-1: the SG-01 shim mechanism was a false-green (kit self-resolves siblings via BASH_SOURCE, so a moved caller never consults an old-flat-root shim; NC couldn't catch it) -> SG-01 now Design:bearing + opus, spec picks a lib-root resolver or per-subsystem shims, gate = run orchestrate.sh/mega-merge.sh post-move. CRIT-2: SG-04 "move whole subtree (22 files)" is actually 57 files incl. deploy/ (stays ops) + RUNBOOK/MANUAL personal-path hardcodes -> named the exclusion set. MAJOR: root settings.json is the real hook-registration file (install.sh only copies+merges) -> SG-02 edits it + hooks.json parity. MINORs: handoff-gen moves with handoff/; cc-observe "2 bins"->"3 bins" typo. Verdict was "not sound as drafted"; now fixed. Scaffold is launch-ready.
