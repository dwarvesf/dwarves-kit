# Spec: agents/research-pitfalls.md + agents/research-stack.md contract (backfill)
Generated: 2026-07-31
Status: DRAFT (reverse-engineered)

Backfill items 4+5/6 of the ID-452 campaign, combined per the item-3 rationale: both targets share the identical skeleton AND the same `commands/spec.md` Step 2 dispatcher that SPEC-210 already pins, so this spec covers only the per-agent DELTAS (tool-roster diffs, model tiers, each body's question list, output template, rules) plus each agent's own dispatcher lines. The shared dispatcher rows (brownfield gate, parallel-4-dispatch sentence, Mode A/B selection gate) are SPEC-210 rows 7/9 and its coverage notes; they are referenced here, never re-pinned. Process deviation, stated honestly: the `/kit:test-plan` Step 0 research-agent dispatch was skipped per the SPEC-208/209/210 precedent; the surface is three small files, read directly. Neither agent file is hard-wrapped, so pins are line-stable. Three side findings, recorded not fixed, each pinned from both sides: (a) BOTH agent bodies instruct writing `docs/research/{pitfalls,stack}.md` but neither frontmatter grants a Write tool (same class as SPEC-210 side finding a); (b) the Mode B pitfalls fallback prompt asks 6 of the agent's 7 categories (omits stale dependencies) and drops the mcp routing; (c) the Mode B stack fallback prompt names `get_architecture()` while the agent body names `get_structure()` (an mcp call-name divergence) and asks 4 of the agent's 5 categories (omits Infrastructure).

## Acceptance Criteria
- [ ] AC-1: Identity per agent: frontmatter names `research-pitfalls` / `research-stack`, each description carries both clauses (its auto-delegation trigger and the shared `Dispatched by /spec for brownfield projects. Read-only.`), the model tiers are pinned as the per-agent delta (`sonnet` for pitfalls, `haiku` for stack, the roster's only haiku-tier research agent), and each body carries its single-job mission.
- [ ] AC-2: Read-only tool grants, exact per agent: pitfalls is exactly Read, Grep, Glob, `Bash(git log *)`, `Bash(find *)`, `Bash(wc *)`; stack is exactly Read, Grep, Glob, `Bash(cat *)`, `Bash(head *)`, `Bash(wc *)`; no write-capable tool (Write/Edit/NotebookEdit) in either grant.
- [ ] AC-3: Dispatch deltas, pinned from both sides: each agent's Mode A dispatch line names the agent and its exact write target; each agent's Mode B inline fallback prompt is pinned verbatim (including divergences b and c as they exist today); each write target appears exactly twice in the Step 2 region (once per mode) and once in the owning agent body.
- [ ] AC-4: Research scope per agent: pitfalls carries its seven bolded category labels and its full conditional codebase-memory-mcp routing sentence (`find_dead_code()` + `trace_call_path()`); stack carries its five bolded category labels and its full routing sentence (`get_structure()`).
- [ ] AC-5: Output contracts: pitfalls targets `docs/research/pitfalls.md` with its five template section headings; stack targets `docs/research/stack.md` with its five template section headings.
- [ ] AC-6: Rules per agent: pitfalls has the 40-line budget, real-risks-not-style rule, the critical-definition sentence, the no-padding honesty rule, and the git-blame 6+-months smell; stack has the 50-line budget with be-concise, verify-from-files with don't-guess, and the not-found-instead-of-assuming honesty rule.
- [ ] AC-7: Distilled return (SPEC-087 Mechanism C) in BOTH agents: heading, the four bounded fields, the bounded-summary/no-re-paste/token-bound fragments.

## Test plan
Date: 2026-07-31 (revised once after the round-1 critique)
Source: this spec's ## Acceptance Criteria. Dialect: doc/prompt-file contract (pin the claims with grep assertions, prove falsifiability with negative controls). All proofs run from the repo root; `P=agents/research-pitfalls.md`, `S=agents/research-stack.md`, `C=commands/spec.md`. Proof cells escape a literal shell pipe as `\|` (markdown table convention, per SPEC-204); rows 4/17's regex alternation pipes are escaped the same way, and the materialized script uses bare `|` in both roles. Rows 1-32 are deterministic reads on the three tracked files; rows 33-34 are the permanent in-suite negative controls (one per agent, mktemp scratch copies, never a tracked file); rows 35-36 are the two deliberate exceptions, one-time recorded live mutations with byte-identity restores, run one at a time. Every pinned string was occurrence-counted 1 in its file at authoring time (each dispatch target deliberately counted 2 within `$C`'s Step 2 region: once per mode; counts are section-anchored via `sed -n '/^### Step 2: Research/,/^### Step 3/p'`, never whole-file).

| # | Case | Category | Covers (AC) | Expected | Proof |
|---|------|----------|-------------|----------|-------|
| 1 | [pitfalls] name + model tier | happy-path | AC-1 | frontmatter has `name: research-pitfalls` and `model: sonnet` (cheap-worker choice; `tests/test-meta.sh` validates the alias set, this row pins the CHOICE) | `awk '/^---$/{c++;next} c==1' $P \| grep -qx 'name: research-pitfalls' && awk '/^---$/{c++;next} c==1' $P \| grep -qx 'model: sonnet'` |
| 2 | [pitfalls] description, both clauses | happy-path | AC-1 | auto-delegation clause AND dispatch-trigger clause | `grep -qF 'Finds landmines and risks in a codebase area before new work begins.' $P && grep -qF 'Dispatched by /spec for brownfield projects. Read-only.' $P` |
| 3 | [pitfalls] single-job mission | happy-path | AC-1 | `Your single job: find landmines that will blow up during implementation.` (presence, not position; accepted) | `grep -qF 'Your single job: find landmines that will blow up during implementation.' $P` |
| 4 | [pitfalls] exact tool roster | security/safety | AC-2 | frontmatter tool list is byte-for-byte the 6 read-only entries, in order (`git log` BEFORE `find`, reversed vs research-architecture; an addition, removal, or reorder breaks the row deliberately) | `diff <(awk '/^---$/{c++;next} c==1' $P \| grep '^  - ') <(printf '  - Read\n  - Grep\n  - Glob\n  - Bash(git log *)\n  - Bash(find *)\n  - Bash(wc *)\n') && ! awk '/^---$/{c++;next} c==1' $P \| grep -qE '^[[:space:]]*-[[:space:]]*(Write\|Edit\|NotebookEdit)'` |
| 5 | [pitfalls] seven category labels | happy-path | AC-4 | all seven bolded what-to-find labels present | `grep -qF '**Deprecated code still referenced**' $P && grep -qF '**TODO/FIXME/HACK in the area**' $P && grep -qF '**Test gaps**' $P && grep -qF '**Large files**' $P && grep -qF '**Circular dependencies**' $P && grep -qF '**Missing config/env values**' $P && grep -qF '**Stale dependencies**' $P` |
| 6 | [pitfalls] codebase-memory conditional | boundary | AC-4 | the FULL routing sentence as one pin (guard + both calls co-located) | `` grep -qF 'If codebase-memory-mcp is available, use `find_dead_code()` and `trace_call_path()`.' $P `` |
| 7 | [pitfalls] output template sections | happy-path | AC-5 | the report title + all five `##` headings of the pitfall-report template (title added in round 1 for parity with row 20) | `grep -qF '# Pitfall Report' $P && grep -qF '## Critical (will block implementation)' $P && grep -qF '## Warnings (will cause problems if ignored)' $P && grep -qF '## Noted (cosmetic, low risk)' $P && grep -qF '## Missing prerequisites' $P && grep -qF '## Files over 500 lines (split candidates)' $P` |
| 8 | [pitfalls] 40-line budget | boundary | AC-6 | `Max 40 lines.` (first sentence of the budget rule; shares its physical line with row 9's pin, see NC note; `$C`'s Mode B copy is out of blast radius, this row greps `$P` only) | `grep -qF 'Max 40 lines.' $P` |
| 9 | [pitfalls] real-risks-not-style rule | boundary | AC-6 | `Only report real risks, not style preferences.` (second sentence, same physical line as row 8; the pitfalls NC target) | `grep -qF 'Only report real risks, not style preferences.' $P` |
| 10 | [pitfalls] critical-definition sentence | boundary | AC-6 | the full definition sentence on the adjacent physical line (NC survivor) | `grep -qF "Critical means \"implementation will fail or produce bugs if this isn't addressed first.\"" $P` |
| 11 | [pitfalls] no-padding honesty | boundary | AC-6 | the nothing-found sentence + don't-pad rule, one physical line | `grep -qF "If you find nothing, say \"No significant pitfalls found.\" Don't pad the report." $P` |
| 12 | [pitfalls] git-blame smell rule | happy-path | AC-6 | both sentences of the git-blame rule | `` grep -qF 'Check `git blame` on suspicious code to see when it last changed. Code untouched for 6+ months in an active repo is a smell.' $P `` |
| 13 | [pitfalls] distilled return contract | happy-path | AC-7 | heading + four bolded fields + the three bound fragments | `grep -qF '## Return contract (distilled return, SPEC-087 Mechanism C)' $P && grep -qF '**verdict**' $P && grep -qF '**key findings**' $P && grep -qF '**artifacts**' $P && grep -qF '**read-next**' $P && grep -qF 'BOUNDED summary, not a dump' $P && grep -qF 'not as a re-paste of diffs, full test logs, or whole files' $P && grep -qF 'hundreds of tokens per dispatch instead of tens of thousands' $P` |
| 14 | [stack] name + model tier | happy-path | AC-1 | frontmatter has `name: research-stack` and `model: haiku` (the roster's only haiku-tier research agent, the campaign's first non-sonnet worker pin) | `awk '/^---$/{c++;next} c==1' $S \| grep -qx 'name: research-stack' && awk '/^---$/{c++;next} c==1' $S \| grep -qx 'model: haiku'` |
| 15 | [stack] description, both clauses | happy-path | AC-1 | auto-delegation clause AND dispatch-trigger clause | `grep -qF 'Maps the technology stack of an existing codebase.' $S && grep -qF 'Dispatched by /spec for brownfield projects. Read-only.' $S` |
| 16 | [stack] single-job mission | happy-path | AC-1 | `Your single job: map the technology stack.` (presence, not position; accepted) | `grep -qF 'Your single job: map the technology stack.' $S` |
| 17 | [stack] exact tool roster | security/safety | AC-2 | frontmatter tool list is byte-for-byte the 6 read-only entries, in order (`cat`/`head` replace `find`/`git log`: stack is the only research agent with NO git access) | `diff <(awk '/^---$/{c++;next} c==1' $S \| grep '^  - ') <(printf '  - Read\n  - Grep\n  - Glob\n  - Bash(cat *)\n  - Bash(head *)\n  - Bash(wc *)\n') && ! awk '/^---$/{c++;next} c==1' $S \| grep -qE '^[[:space:]]*-[[:space:]]*(Write\|Edit\|NotebookEdit)'` |
| 18 | [stack] five category labels | happy-path | AC-4 | all five bolded what-to-find labels present | `grep -qF '**Languages**' $S && grep -qF '**Frameworks**' $S && grep -qF '**Key dependencies**' $S && grep -qF '**Infrastructure**' $S && grep -qF '**Build/deploy**' $S` |
| 19 | [stack] codebase-memory conditional | boundary | AC-4 | the FULL routing sentence as one pin; pins the `get_structure()` side of divergence c | `` grep -qF 'If codebase-memory-mcp is available, use `get_structure()` instead of reading files one by one.' $S `` |
| 20 | [stack] output template sections | happy-path | AC-5 | the report title + all five `##` headings of the stack-report template | `grep -qF '# Stack Report' $S && grep -qF '## Languages' $S && grep -qF '## Frameworks' $S && grep -qF '## Key dependencies' $S && grep -qF '## Infrastructure' $S && grep -qF '## Build & deploy' $S` |
| 21 | [stack] 50-line budget + be-concise | boundary | AC-6 | `Max 50 lines of output. Be concise.` (full physical line, adjacent to the NC line; NC survivor) | `grep -qF 'Max 50 lines of output. Be concise.' $S` |
| 22 | [stack] verify-from-files rule | boundary | AC-6 | `Only report what you can verify from files.` (first sentence, same physical line as row 23; NC survivor) | `grep -qF 'Only report what you can verify from files.' $S` |
| 23 | [stack] don't-guess rule | boundary | AC-6 | `Don't guess.` (second sentence, same physical line as row 22; the stack NC target; occurrence-counted 1 in `$S` and 0 in `$C`) | `grep -qF "Don't guess." $S` |
| 24 | [stack] not-found honesty | boundary | AC-6 | the config-file honesty sentence | `grep -qF "If a config file doesn't exist, say \"not found\" instead of assuming." $S` |
| 25 | [stack] distilled return contract | happy-path | AC-7 | heading + four bolded fields + the three bound fragments | `grep -qF '## Return contract (distilled return, SPEC-087 Mechanism C)' $S && grep -qF '**verdict**' $S && grep -qF '**key findings**' $S && grep -qF '**artifacts**' $S && grep -qF '**read-next**' $S && grep -qF 'BOUNDED summary, not a dump' $S && grep -qF 'not as a re-paste of diffs, full test logs, or whole files' $S && grep -qF 'hundreds of tokens per dispatch instead of tens of thousands' $S` |
| 26 | [dispatcher] Mode A pitfalls line | happy-path | AC-3 | the dispatch sentence names the agent and the exact write target (pinned without the list numeral, renumber-tolerant) | `` grep -qF '**research-pitfalls** agent: "Find landmines in [target area / target files]. Write to `docs/research/pitfalls.md`."' $C `` |
| 27 | [dispatcher] Mode A stack line + gate example filename | happy-path | AC-3 | the research-stack dispatch line, AND the Mode A gate's illustrative filename `` `.claude/agents/research-stack.md` `` (the gate SENTENCE stays SPEC-210-deferred, but its example filename names THIS spec's agent, so the fragment is claimed here; occurrence-counted 1 in `$C`) | `` grep -qF '**research-stack** agent: "Map the technology stack. Write to `docs/research/stack.md`."' $C && grep -qF '`.claude/agents/research-stack.md`' $C `` |
| 28 | [dispatcher] Mode B pitfalls prompt | boundary | AC-3 | the full inline pitfall-research prompt line, verbatim (pins today's 6-of-7 divergence and missing mcp routing as-is; side finding b) | `grep -qF 'Find landmines in [target area]. Look for: deprecated code still referenced, TODO/FIXME comments, test gaps, circular dependencies, files over 500 lines, missing env/config values the new feature will need. Max 40 lines. Write to docs/research/pitfalls.md.' $C` |
| 29 | [dispatcher] Mode B stack prompt | boundary | AC-3 | the full inline stack-research prompt line, verbatim (pins today's `get_architecture()` naming and 4-of-5 divergence as-is; the dispatcher side of side finding c) | `grep -qF 'Map the technology stack. Read package.json / go.mod / Cargo.toml / pyproject.toml and config files. Report: languages, frameworks, versions, key dependencies (top 5-10), build/test/deploy commands. If codebase-memory-mcp is available, use get_architecture(). Max 50 lines. Write to docs/research/stack.md.' $C` |
| 30 | [dispatcher] pitfalls write target agrees | boundary | AC-3, AC-5 | `docs/research/pitfalls.md` exactly twice within `$C`'s Step 2 region (Mode A + Mode B; region-anchored) and in `$P`'s output contract | `` test "$(sed -n '/^### Step 2: Research/,/^### Step 3/p' $C \| grep -cF 'docs/research/pitfalls.md')" -eq 2 && grep -qF 'Write to `docs/research/pitfalls.md`:' $P `` |
| 31 | [dispatcher] stack write target agrees | boundary | AC-3, AC-5 | `docs/research/stack.md` exactly twice within `$C`'s Step 2 region and in `$S`'s output contract (the Mode A gate's `.claude/agents/research-stack.md` mention does not contain this path, verified at authoring) | `` test "$(sed -n '/^### Step 2: Research/,/^### Step 3/p' $C \| grep -cF 'docs/research/stack.md')" -eq 2 && grep -qF 'Write to `docs/research/stack.md`:' $S `` |
| 32 | read-only claim consistency, both | regression | AC-1, AC-2 | `Read-only.` in BOTH descriptions AND no write grant in either frontmatter (the pair that catches a future grant widening that forgets a description, or vice versa) | `grep -qF 'Read-only.' $P && grep -qF 'Read-only.' $S && ! awk '/^---$/{c++;next} c==1' $P \| grep -qE '^[[:space:]]*-[[:space:]]*(Write\|Edit\|NotebookEdit)' && ! awk '/^---$/{c++;next} c==1' $S \| grep -qE '^[[:space:]]*-[[:space:]]*(Write\|Edit\|NotebookEdit)'` |
| 33 | [pitfalls] in-suite negative control (permanent, scratch copy) | falsifiability | AC-6 | on a mktemp COPY of `$P` with ` Only report real risks, not style preferences.` stripped (substring removal, tracked file untouched), the NC-SETUP GUARD `! cmp -s $P copy` proves the strip actually changed the copy (this guard, not the absence check, is what goes RED in row 35: sed is a silent no-op when the substring is already gone), then row 9's pin FAILS on the copy while two named survivors still PASS: row 8's `Max 40 lines.` (same physical line, discriminates substring-strip from whole-line delete) and row 10's critical-definition sentence (adjacent physical line, discriminates a line-scoped over-strip); EXIT-trap cleanup | inside the test script: mktemp scratch copy + sed substring strip + `! cmp -s` setup guard + `! grep -qF` on the stripped sentence + `grep -qF` on the two survivors |
| 34 | [stack] in-suite negative control (permanent, scratch copy) | falsifiability | AC-6 | on a mktemp COPY of `$S` with ` Don't guess.` stripped, the same `! cmp -s` setup guard, then row 23's pin FAILS while two named survivors still PASS on the copy: row 22's first sentence (same physical line) and row 21's budget line (adjacent physical line); EXIT-trap cleanup | inside the test script: same shape as row 33 |
| 35 | [pitfalls] live negative control (one-time, recorded) | falsifiability | AC-6 | strip ONLY ` Only report real risks, not style preferences.` from the tracked `$P` via `sed -i.bak` (BSD/macOS-safe; substring removal, never the whole line: row 8's pin shares the physical line), run the suite, expect EXACTLY this flip set: row 9 RED plus row 33's `! cmp -s` NC-setup guard RED (nothing left to strip on the copy it derives, a named, predicted side effect); meaningful survivors: row 8 (same physical line) and row 10 (adjacent line) stay GREEN, every `$S` row and every `$C` row stays GREEN (untouched files), and row 34's stack NC stays fully GREEN; restore from the `.bak`, prove byte-identity via truncated sha256 + re-derive command, re-run green; if interrupted mid-mutation, emergency recovery is `mv -f $P.bak $P` (or `git checkout -- $P`) | recorded with both transcripts in `docs/verification/backfill-research-pair.md` |
| 36 | [stack] live negative control (one-time, recorded) | falsifiability | AC-6 | after row 35's restore is byte-verified (the two live NCs NEVER overlap: one tracked file mutated at a time), strip ONLY ` Don't guess.` from the tracked `$S` via `sed -i.bak`, run the suite, expect EXACTLY: row 23 RED plus row 34's `! cmp -s` NC-setup guard RED; meaningful survivors: row 22 (same physical line) and row 21 (adjacent line) stay GREEN, every `$P` row and every `$C` row stays GREEN, row 33's pitfalls NC fully GREEN; restore, byte-identity, re-run green; same emergency recovery | recorded in `docs/verification/backfill-research-pair.md` |

### Coverage notes
- Every AC maps to at least one row per agent and every row back to an AC; no orphans. Failure-injection is honestly thin: the subjects are static prompt files, so the only injectable failure is content drift, which IS rows 33-36 plus the regression row 32.
- Shared dispatcher rows are NOT re-pinned: `### Step 2: Research (if brownfield)`, `dispatch all 4 via the Task tool in parallel`, and the Mode A/B selection gate belong to SPEC-210 (rows 7/9 and its coverage notes). This spec pins only the per-agent dispatcher lines and region-anchored target counts.
- Agent frontmatter structure and the MANUAL.md inventory cross-ref are owned by `tests/test-meta.sh`; rows 1/14 pin the model CHOICE, which test-meta deliberately does not.
- Exact-string pins are by design: the tested contract is the prose itself; a legitimate rewording is EXPECTED to break the matching row and prompt a re-verify (same stance as SPEC-208/209/210). Neither agent file is hard-wrapped, so no pin spans a wrap boundary today; if any file is re-wrapped, the longest pins (rows 6, 12, 19, 26-29) break first. Snapshot, not exhaustive.
- Adjacency hazards, named per the SPEC-208 precedent: rows 8-9 pin two sentences of one physical line of `$P` (`- Max 40 lines. Only report real risks, not style preferences.`); rows 22-23 pin two sentences of one physical line of `$S` (`- Only report what you can verify from files. Don't guess.`). Any future line-deletion-style negative control on AC-6 must strip a substring, never the whole line. `Max 40 lines.` also exists once in `$C`'s Mode B pitfalls prompt (row 28's verbatim pin); rows 8-9 grep `$P` only, so neither NC's blast radius reaches it.
- The Write-without-grant conflict (side finding a) is deliberately NOT asserted either way: rows 4/17 pin the grants and rows 30/31 pin the write instructions, so whichever side a future fix changes, exactly one row per agent breaks and surfaces this spec. Same stance for divergences b and c, both sides named: for b, rows 28 (Mode B verbatim) vs 5 (`**Stale dependencies**` among the seven labels) and 6 (the mcp routing Mode B drops); for c, rows 29 (Mode B's `get_architecture()`) vs 19 (the body's `get_structure()`). Closing any divergence breaks exactly one row and surfaces the note.
- Deliberately unpinned, accepted LOW: both templates' bracketed placeholders; pitfalls' input section; the numbered order of the category lists (labels pinned, numerals not, renumber-tolerant); rows 3/16's position ("opens with") proven by presence only; the shared boilerplate opener `You are a codebase researcher.` and the Step 2 preamble `` Create `docs/research/` directory first. `` (both identical/shared across all 4 sibling agents, no per-agent discriminating power, campaign-wide gap noted); rows 4/17's write-tool alternation is not end-anchored (a hypothetical `Editorial` would false-RED, over-conservative), the same accepted LOW as SPEC-210, carried forward explicitly; rows 30/31's region anchor does not discriminate against today's file (whole-file count also 2), it is forward-looking defense verified at authoring.

## Test plan critique
Date: 2026-07-31 (revised once after the round-1 critique)
Spec: SPEC-211
Lenses run: Coverage completeness, Oracle & falsifiability, Determinism & maintainability (3 of the standard 6, dispatched as parallel read-only subagents). Skipped, honestly: Feasibility & reproducibility (every proof is a pasteable one-liner; the determinism lens in fact pasted-and-ran all 32), Test-ladder & boundary depth (the ladder collapses for static-file subjects), Tiering & floor (N/A: no Tier column, no model call in scope). Same ID-452 calibrated-cost triage as SPEC-208/209/210, stated rather than hidden.

Rounds:
- [[QL-VERDICT round=1 clean=false findings=10]]
- [[QL-VERDICT round=2 clean=false findings=5]]

Round 1 (full 3-lens dispatch) returned 10 deduplicated findings: 1 CRITICAL, 2 HIGH, 2 MEDIUM, 5 LOW. One revision was applied (the matrix above). Round 2 was NOT a fresh lens dispatch: it was a narrow mechanical re-check by the coordinator, every new pin run live against the real files with occurrence counts (all 1). K fell 10 to 5 and max severity fell CRITICAL to LOW; the 5 remaining are accepted LOW trade-offs, so the loop stops.

### Critical findings
1. Rows 33/34's Proof column never defined the NC-setup guard that rows 35/36's flip-set prediction depends on: sed substring removal is a silent no-op when the substring is already gone, so without a `! cmp -s` precondition every literal assertion in the drafted rows would stay GREEN under the live mutation and the predicted "NC-setup RED" referred to nothing in THIS spec (it existed only in SPEC-210's materialized script, a dangling cross-reference inherited from the precedent's own prose gap; the lens proved it by simulation) (oracle lens) -- fix: rows 33/34 now name the `! cmp -s` setup guard explicitly and rows 35/36 reference it -- resolved in round 1.

### High findings
1. The Mode A gate's illustrative filename `.claude/agents/research-stack.md` names one of THIS spec's agents but was orphaned campaign-wide: SPEC-210 defers only the generic gate clause, and the draft claimed the shared rows belong to SPEC-210 without SPEC-210 ever pinning this string (coverage lens) -- fix: row 27 now pins the filename fragment and states the ownership split (gate sentence deferred, example filename claimed here) -- resolved in round 1.
2. `# Pitfall Report` was unpinned while the sibling row 20 pins `# Stack Report`, an asymmetry contradicting the no-orphans claim for a load-bearing string (coverage lens HIGH, oracle lens filed the same as LOW, merged at max severity) -- fix: row 7 pins the title -- resolved in round 1.

### Medium findings
1. The coverage note named row 19 as divergence c's body-side counterpart but named nothing for divergence b's two sub-claims, leaving the both-sides pairing undocumented for b (coverage lens) -- fix: the note now names rows 5/6 as b's body-side pins -- resolved in round 1.
2. The two-file live-NC sequence doubled the mutation exposure window vs the single-file precedent with no recovery path named (determinism lens) -- fix: rows 35/36 now state never-overlap sequencing plus the `mv -f .bak` / `git checkout --` emergency recovery -- resolved in round 1.

### Low findings
1. Rows 30/31's region anchor does not discriminate today (whole-file count also 2); forward-looking defense -- accepted, documented in coverage notes -- OPEN as accepted.
2. Rows 4/17's write-tool alternation not end-anchored (inherited SPEC-210 accepted LOW, previously uncarried) -- accepted, now documented in coverage notes -- OPEN as accepted.
3. Unquoted `$P`/`$S`/`$C` in proof cells vs the quoted convention of the materialized precedent -- accepted for the doc; the materialized script quotes them -- OPEN as accepted.
4. Shared boilerplate opener and the `Create docs/research/ directory first.` preamble line unpinned campaign-wide -- accepted, added to the deliberately-unpinned list -- OPEN as accepted.
5. Rows 33-36's artifacts did not exist at critique time -- true by pipeline position (critique precedes materialization in SPEC-203); resolved at the materialization + verification steps.

### Scores (round 1, pre-revision)
- Coverage completeness: 7/10
- Oracle & falsifiability: 8/10
- Determinism & maintainability: 9/10

### Verdict: SOLID (after the round-1 revision resolved the CRITICAL and both HIGHs; remaining findings are accepted LOW)
