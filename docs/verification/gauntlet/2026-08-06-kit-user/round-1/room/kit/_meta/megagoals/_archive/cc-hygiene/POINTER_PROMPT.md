Drive the cc-hygiene mega-goal to completion autonomously. Source of truth: _meta/megagoals/cc-hygiene/ROADMAP.md in ops-toolkit; re-read it and the current sub-goal file under goals/ at the start of every turn. The roadmap, sub-goal files, and NOTES.md are the whole contract; nothing else survives between turns.

Work sub-goals in numeric order unless blocked; on a blocker, fingerprint it in NOTES.md ## Active blockers (command · failure · prerequisite · last verified, updated in place), hop to the next workable one, retry only when the prerequisite changed. One PR per sub-goal, in that sub-goal's own repo (ops-toolkit, dotfiles, or dwarves-kit; each file names repo, branch, base). A sub-goal that exists only as local diff is unstarted. Record the PR # on its ROADMAP line the moment it opens; a checked box is `- [x] ... PR #N` (+ merge SHA when you merged it) or it is not checked. CI green means the OPEN PR's checks, not local tests.

SDD, per Han's directive: every sub-goal routes through the dwarves-kit lane with a real spec pass. Draft the spec, run spec-validate's adversarial lenses on it, then build to the spec, keeping implementation-notes (delta from spec only) in that repo. Record each phase via bash ~/.claude/dwarves-kit/lib/gate-ledger.sh so the ship-gate is the Done check. Same-repo (ops-toolkit) sub-goals may use /kit:spec + /kit:spec-validate; cross-repo sub-goals (dotfiles, dwarves-kit) must drive lib/lane-classify.sh + gate-ledger directly, never /kit:* (cwd-bound). Cross-repo branches use the documented worktree recipe in ROADMAP Assumptions.

Merging: auto-bottom-up + gated-final. Merge an `auto` sub-goal's PR yourself ONLY when all gates hold: its own close-the-loop commands verify Done=, gh pr checks all green, no CHANGES_REQUESTED, and a committed proof with captured evidence (run-table with the literal command + exit code + real stdout; never a bare "passes"). Before deleting a merged parent branch, retarget any child PR to main first. NEVER merge a `gate` sub-goal (06, 07, 09): open its PR, log once to NOTES.md ## Event log, emit `🛑 NEEDS APPROVAL — sub-goal NN, STOP /goal MANUALLY`, and continue to the next workable sub-goal or stop.

Do not rewrite any sub-goal's Done= or add sub-goals mid-loop; discoveries go to NOTES.md ## Proposed additions. 02 and 06 both touch _meta/BACKLOG.md: never work them in parallel.

Success = 01-08 merged-or-held per policy, then 09's fresh review round over the merged set: /kit:review-team (security + architecture + test-coverage) plus the kit advisor in critique AND over-suggest modes, CRITICAL/MAJOR findings fixed on the 09 branch with a run-table, board rows + a one-paragraph _meta/LAB_LOG.md arc entry (slug, sub-goal count, PR range, lessons) riding the 09 branch, and the 09 PR held open for Han's single click. Before claiming success, audit every PR # in ROADMAP.md via gh pr view (state, reviewDecision, checks) and uncheck any box that fails. On the first stop, append the final summary block to NOTES.md ## Event log; later fast-stops emit only the banner.
