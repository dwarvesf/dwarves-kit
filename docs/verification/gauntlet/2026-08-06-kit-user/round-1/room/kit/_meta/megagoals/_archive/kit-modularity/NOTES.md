# NOTES, kit-modularity

## Active blockers

(none), PRECONDITION MET 2026-07-05: kit-foldin (ID-276) SHIPPED + archived (`_meta/megagoals/_archive/kit-foldin/`; dwarves-kit #183-189, ops #720/#721). Real merged state verified: `lib/` subsystem dirs EXIST (board/classify/gate/goal/queue/session/spec/telemetry) + 34 flat shim/helper files at lib root; kit `tools/` has ledger-observatory + session-observe/recall/intel + skill-curator + plugin-check; hooks renamed backlog-stage/citation-guard/context-hints/harvest. Ready to launch.

## Proposed additions

- 2026-07-05 (convergence-gate advisor P6 + P5, FOLLOW-UP backlog row, NOT executed at close): the mega found test-docs-wiring absent from CI (fixed in #198); the advisor found MORE load-bearing tests absent from `.github/workflows/test.yml` , `test-mega-reconcile.sh` (35 assertions on the SG-08 drift reconciler), `test-install-compat.sh` + `test-install-contract.sh` (SG-04 layering back-compat), plus ship-gate-fail-closed/profiles, adopt, agent-effectiveness (18 of 63 test files run in CI). PLUS: a standing no-stale-path lint (grep asserting no LIVE file names a `tools/<x>/` path, excluding docs/proof/session-state) would fold the last retirement residue , the P5 MINOR: `lib/stats/tests/lib/conform.sh:3` header comment still names `tools/ledger-observatory/docs/ledger-event-schema.md` (doc now at `lib/stats/docs/`), comment-only, no runtime effect. PLUS: a substrate-append lint documenting which side-logs (`completeness.log`, `mega-merge.log`) are deliberately OUTSIDE the run-line event stream. Bundle these into ONE substantive follow-up (wire tests + 2 lints + fix the comment) , a real piece of work, not a bookkeeping PR. File as an ops-toolkit backlog row.



- 2026-07-05: the kit-foldin-precondition rot risk is CONTAINED BY SG-01's re-read (advisor P5 clarification). SG-01 absorbs kit-foldin's REAL merged structure; 02-06 all build on 01's OUTPUT, so they inherit the real surface transitively , the risk is NOT limited to 03/07. In particular 04's optional-module list (board/queue/orchestrate/stats/bridge/quiz-gate/weekend-batch/advisor/session-*/cosmetic-hooks) is a BEST-GUESS to reconcile against 01's actual output at execution (some are pre-existing hooks, not kit-foldin sub-goal names). Mandatory first step of 01 (and a re-check in 02-06): re-read kit-foldin's merged output; do NOT execute off the guessed names. 03 (wraps `stats`) + 07 (final surface) are the MOST exposed, but not the only ones.
- 2026-07-05: Decision H (the Hermes-cockpit , wire backlog + megagoal + learning-ledger into the Air's Hermes) is a SEPARATE mega, NOT scaffolded here. It sequences AFTER this mega and has an open Air-vs-Mini topology question. Draft it when this ships.
- 2026-07-05: the optional thin `kit` dispatcher (SG-03) may not earn its keep , standalone commands are the surface regardless. Build it only if `kit list`/forwarding is genuinely useful; skipping it is a valid outcome.
- 2026-07-05 (advisor P6#1, RIDE-LATER -> now in HANDOFF first-action): BASELINE the never-diverge mirror check right after kit-foldin ships, BEFORE SG-01 launches, and log the result here , so SG-07's diff is attributable to THIS mega, not pre-existing kit-foldin drift.
- 2026-07-05 (advisor P6#7, RIDE-LATER for SG-06): the philosophy should name the multi-agent-future distinction , the standalone `<subsystem> <verb>` SHELL surface is runtime-agnostic (any shell-capable agent: pi/opencode/CC), but the agent-authoring surface (`agents/`/`commands/`/`skills/`) is Claude-Code-loader-specific and would need its own loader for another runtime. One honest paragraph in SG-06.
- 2026-07-05 (advisor P6#8, RIDE-LATER, low): SG-05 + SG-06 could merge into one PR (both small, parallel, auto, docs-describing-the-settled-surface). Saves a review round; not worth forcing. Kept separate for clean per-artifact proof.
- 2026-07-05 (advisor P6#9, QUESTION FOR HAN before launch): should SG-01 ABSORB the `ledger-observatory`->`stats` git-mv (shrinking SG-02 to naming/extraction-only)? Doing both structural moves in one pass = cleaner `git log --follow` + no second wave of path updates against files 01 just moved. A scope-boundary call, not silently folded. Default keeps them split (01 collapse, 02 rename+extract).

## Event log

2026-07-05 · SG-07 MERGED , MEGA SHIPPED · Han waived the gated-final hold (explicit "merge them" after reviewing options A/B) , merged dwarves-kit #197 (303096b) then dotfiles #207 (MERGED 13:49Z). ALL 8 SUB-GOALS MERGED. Both re-verified MERGEABLE/CLEAN + CI green right before merge (#197 briefly showed UNKNOWN while GitHub recomputed mergeability against the #198-moved master; resolved CLEAN on poll). Worktrees removed, branches deleted. Co-located the mega folder to `_meta/megagoals/_archive/kit-modularity/` (kit-* cross-repo owner per megagoal-lifecycle-rule) + committed ops-toolkit records + flipped ID-277 shipped. Follow-up (advisor P6) filed for CI-test-coverage + 2 lints.

2026-07-05 · CONVERGENCE GATE PASSED · #198 merged 175557c (test-docs-wiring fix + wired into CI), master fully green. Composed adversarial gate (in-harness advisor, opus, P5+P6, MACHINE-VERIFIED , ran 20 suites itself, not report-trusted): ALL 7 cross-sub-goal invariants HOLD (event-sourcing/no-persist, no-alias/0-symlink, tools/-retired, install-layering + anti-registry, middle-level/no-uber-binary, module-completeness). P5 = 1 MINOR (comment-only stale tools/ path in conform.sh:3, non-blocking) + 2 candidate seams INVESTIGATED-AND-CLEARED (completeness.log/mega-merge.log write to ledger ROOT not runs/, so no stats projection contamination; append_run_line is a thin ledger_append wrapper). VERDICT: SOUND TO CLOSE. Advisor ledger row recorded (mode=P5|P6 findings=1|5 actor=convergence-gate). recheck = the advisor's own 20-suite machine-verification IS the fresh-context re-audit of the integration PASS. P5-MINOR + P6-CI-gaps -> Proposed additions (one substantive follow-up backlog row, NOT executed at close per OPERATE no-new-work rule). Gate matrix: full-suite-green ✓ · spine-only-install ✓ · per-module-doc/firing ✓ · mirror-check ✓ · advisor P5/P6 ✓ · recheck ✓. Proceeding to RUN_REPORT + close-out.

2026-07-05 · SG-07 BUILT + HELD · dwarves-kit PR #197 (CI green, CLEAN) + dotfiles PR #207 (CI green: shellcheck + test-macos, CLEAN). NOT MERGED , held for Han (edits his authoring skills). Mirror check: the runner-fastpath `<!-- BEGIN/END triage-ladder -->` fence between plan-for-goal/SKILL.md (canonical) and commands/mega.md (mirror) is byte-identical, shasum a42939f37e91e61eadfa0a7e4de7034a3309a22c unchanged before/after (edits landed OUTSIDE the fence); negative control verified (corrupt one word -> shasum mismatch -> restore -> match). grep-audit 0 live across all 3 surfaces (ledger-observatory / flat lib/<x>.sh / tools/ / old-install). Only 3 stale refs total (plan-for-goal/SKILL.md 1 + GUIDE.md 2); **mega.md needed ZERO edits , SG-08 (#194) had already authored it against the final subsystem paths**, so the baseline's 33+9 mega.md flat-path count was resolved upstream of SG-07. Pure stale-ref fix, no scaffolder flow change. dotfiles skills live at home/dot_claude/skills/ (chezmoi source == deployed, non-templated).

2026-07-05 · CONVERGENCE GATE (in progress) · full suite on merged master (0460c36) = 58 pass / 4 fail. 3 fails are the KNOWN pre-existing local-env ones (test-board.sh stale installed lib/board.sh, test-classify-md-inert /tmp copy, test-ship-gate-profiles needs installed hook , all SKIP/pass in CI). The 4th, **test-docs-wiring.sh AC7, is a REAL cross-sub-goal regression the gate caught**: SG-02's ledger-substrate extraction routed the TOKENS marker through `append_run_line` (no inline `\n`), so the emission became `printf '%s | TOKENS | %s'` while the test's fixed-string grep still pinned the old `printf '%s | TOKENS | %s\n'`. Marker still emitted (functional), only the assertion stale. It slipped EVERY per-PR CI because test-docs-wiring was NOT in the CI list (grep -c=0). Fixed via conductor PR #<fix> (fix/docs-wiring-token-marker): update the assertion to match the substrate emission + ADD test-docs-wiring to .github/workflows/test.yml so the no-orphan sweep runs on every push. 22/22 local post-fix. (Spine-only install NC on master = PASS, exactly 6 spine hooks, no optional; per-module doc/firing = SG-06's 35-row audit.) LESSON: a "no-orphan sweep" test that greps for an EXACT byte form is brittle across a substrate refactor; and a test absent from CI is a silent coverage hole , the convergence gate (full local suite on merged master) is what caught both. SG-07 still building; /kit:verify + review-team + advisor P5/P6 + recheck run after 07 lands.

2026-07-05 · SG-05 DONE · PR #196 merged 0460c36. Operate-contract refresh. ALL 7 BUILD SUB-GOALS NOW MERGED (01-06, 08); only SG-07 (held) + the convergence gate remain. Grep-audit reconfirmed by conductor: 0 `ledger-observatory` / 0 `bash tools/` live refs in AGENTS.md + WORKFLOW.md. Scope clean (AGENTS/WORKFLOW + 2 proofs, no SG-06 overlap). FINDING: the operate-contract was NOT stale , SG-01..04's call-site sweeps had already scrubbed every retired token + repointed every lib/ path as a side effect. SG-05's real delta was the inverse: the files POSITIVELY DESCRIBED none of the new surface, so it ADDED one composition note to each (AGENTS.md "How the kit composes" + WORKFLOW.md "Subsystem modules and install layering"), pointing at PHILOSOPHY/README for the full model rather than duplicating. No lane/gate/state-machine semantics changed. -> DECISIONS. Next: convergence gate on merged master, then RUN_REPORT, then dispatch held SG-07.

2026-07-05 · SG-06 DONE · PR #195 merged c6e1ad6. Docs refresh to toolbox-not-appliance. Worker exited without a full report (left CI pollers running), so conductor verified the branch directly: REAL scope in-bounds (README/PHILOSOPHY + 2 proof docs only; the `master..HEAD` diff FALSELY showed SG-08 files , diff-direction artifact because the branch forked pre-#194; the true `merge-base..HEAD` diff is clean, no board.sh/test.yml/mega overlap). F-bar audit = 35 rows, 0 unjustified gaps, 9/9 ACs PASS; both required philosophy paragraphs (multi-agent-future P6#7 + team-mode-parked C) landed; ledger substrate inline-doc justified (not papered with a stub README); delete-a-doc NC + stray-`ledger-observatory`-label re-grep both green. CI green, CLEAN merge. LESSON: when a worktree branch forks before a sibling merges, `git diff master..HEAD` conflates the sibling's landed files , always diff against `merge-base..HEAD` for a worker's REAL scope. Only SG-05 remains before the convergence gate + held SG-07.

2026-07-05 · SG-08 DONE · PR #194 merged 5e7ca71. `mega status <slug>` reconciler shipped. Verified: 16/16 NCs, CI green, and despite both #193+#194 editing .github/workflows/test.yml the merge was CLEAN (merge-tree no conflict; both test-install-modules + test-mega present on master post-merge, grep=3, no silent drop , the feared conflict didn't materialize). Design: `mega` = bare orphan `lib/mega.sh` (single verb `status`, per SG-03's 2+-verb rule), NOT a board subcommand (it reconciles a code-repo's git truth against a THIRD location where megagoals live, wider than board's single-repo domain). Drift taxonomy: OK/CLAIM-UNVERIFIED/MERGED-UNCHECKED/STALLED/WIP/PENDING/INFO , and it correctly classifies 0-commits+open-PR as WIP not STALLED (the in-flight nuance I injected). `--with-mega` opt-in on board render (never default: protects the board byte-identical non-regression NC + avoids unconditional gh network calls). Firing point = the --with-mega rollup section. Real-corpus self-test: `mega status kit-modularity` flagged SG-01/02/03 merged-OK, SG-04/08 STALLED at that instant (0 commits then, correct for the snapshot). Judgment calls -> DECISIONS. Only SG-05 + SG-06 remain building, then SG-07 (held) + convergence gate.

2026-07-05 · SG-04 DONE · PR #193 merged bc5bf56. Layered install shipped. Worker exited mid-CI-wait (incomplete report), so conductor verified from the branch proof directly: 21/21 install-module NCs pass, NEGATIVE CONTROL is load-bearing (revert install.sh -> 20 hooks wire unconditionally + 15/21 NCs fail), anti-registry lint (`grep -rl kit.toml hooks/` empty) wired into CI, CI green both OS. SPINE (unconditional, 6 hooks): safety-gate, ship-gate, spec-drift-guard, secrets-guard, commit-format, anti-rationalization. OPT-IN modules: board/session/advisor/cosmetic (have hooks) + hookless queue/stats/quiz_gate/weekend_batch/bridge. `kit.toml [modules]` is a per-CONSUMER install artifact (written by install.sh, NOT committed to the repo). Re-run is additive (never un-wires); `--prune --with` is the explicit trim; team_mode=false reserved. Modified .github/workflows/test.yml (+ .gitignore) , NOTE: SG-08 also touches test.yml, expect a merge conflict when 08 lands (resolve at merge). Now dispatching SG-05 + SG-06 (install surface settled). SG-08 still building.

2026-07-05 · SG-03 DONE · PR #192 merged b2e8717. 5 grouped standalone entries (gate/classify/spec/goal/session); board+stats already had the shape. `kit` dispatcher SKIPPED (7 cmds too few to earn it; no MANIFEST convention to back `kit list`; building it risks the uber-binary seam the Middle-level invariant rejects , each entry's `--help` is the discovery surface). Orphans stayed bare. Verified: 14/14 call-sites resolve, worktree clean, and the pre-existing `maintainer-wip-marketplace-backlog-id042` stash was UNTOUCHED (the worker's `git stash -u` suite-compare did NOT blind-pop it , the exact hazard checked). CI green. DECISIONS gets the ship-gate proof-of-done-path lesson. SEQUENCING DECISION: 05+06 goal files say `Depends on: 04` (they describe the install surface) even though Han's ROADMAP loosened them to "parallel to 04" , honoring the tighter sub-goal contract to avoid doc-drift. Fan-out = **SG-04 + SG-08 now** (pure-code, depend only on 03, mutually independent), then SG-05 + SG-06 after 04 merges.

2026-07-05 · SG-02 DONE · PR #191 merged 053c381. Event-sourcing plane split shipped AND SG-01's deferred tools/ fold completed , **tools/ is now empty/gone on master, lib-vs-tools split fully retired.** VERIFIED against git (the HANDOFF "SG-02 not started / empty worktree" edit was a stale mid-flight snapshot , at merge time the branch had 7 commits, PR CLEAN, CI green; a good SG-08 datum: drift-check must not flag in-flight workers as stalled). ledger substrate `lib/ledger/ledger.sh`: `ledger append <stream> <text>` / `ledger read <stream>` / `ledger root`; KIT_LEDGER_DIR precedence = KIT_LEDGER_DIR (wins) -> DWARVES_KIT_LOG_DIR (back-compat) -> XDG_STATE_HOME/dwarves-kit/logs; set-but-EMPTY = fatal. no-persist NC: full-tree snapshot byte-identical across two `stats` runs + fresh-context recheck HOLDS (stats materializes `:memory:` per invocation, persistent DuckDB cache REMOVED). Homeless tools: skill-curator+plugin-check -> bare lib/ orphan dirs (extends SG-01 orphan precedent); session-observe/recall/intel -> lib/session/. CONDUCTOR CATCH: worker left one read-side `ledger-observatory` output label in anomalies.py:689 (violates Done="no read-side name survives"); no test pinned it, swept in-place (commit 1b3138e) rather than re-dispatch. F-bar residual: ledger substrate has inline-header doc + firing point but no standalone README -> SG-06 doc-audit. Judgment calls -> DECISIONS. Dispatching SG-03.

2026-07-05 · SG-01 DONE · PR #190 merged cb64f15. Kept `lib/` dir name (renaming re-touches every call-site for zero behavioral gain; design-note default). NO-ALIAS load-bearing NC verified INDEPENDENTLY: 0 symlinks in whole lib tree via /usr/bin/find + python (the machine's `find` is `rtk find` shim which mis-parses `-type l` when not piped , use /usr/bin/find or python for symlink checks the rest of this run). Suite 56/2->56/2 identical (the 2 fails pre-existing env: classify-md-inert /tmp copy + ship-gate-profiles needs installed hook). mega-merge+orchestrate resolve via $LIB_ROOT. CI green ubuntu+macos. Judgment calls -> DECISIONS. **SCOPE REASSIGNMENT: the tools/ physical fold (ledger-observatory, session-observe/recall/intel, skill-curator, plugin-check) was DEFERRED by SG-01 and moved into SG-02's scope** , SG-01's crisp Done= is the 4 NCs + F-bar (all met), the tools/ fold was an Outcome-step not a Done= criterion. ledger-observatory folds naturally with the SG-02 stats rename (avoids double-churn); SG-02 must ALSO decide a home for the 2 homeless tools (skill-curator, plugin-check) + the 3 self-contained session-* tools. Recorded in SG-02 goal ## Notes.

2026-07-05 · LAUNCH · confirmed no drift vs precondition (33 lib-root symlinks, 8 subsystem dirs board/classify/gate/goal/queue/session/spec/telemetry, tools/=ledger-observatory/session-observe/recall/intel/skill-curator/plugin-check). MIRROR BASELINE captured (P6#1, pre-SG-01, so SG-07's diff is attributable to THIS mega): scaffolders already ZERO `ledger-observatory` refs (the 67 repo-wide refs are SG-02's rename target, not a scaffolder concern); `lib/<x>.sh` path refs = plan-for-goal/SKILL.md 1, plan-for-mega-goal/SKILL.md 0, GUIDE.md 2, commands/mega.md 33 (+9 `bash lib/` calls). SG-07 target = drive these to the collapsed-module surface; any residual NOT in this baseline = pre-existing kit-foldin drift, not this mega. Dispatched SG-01 (module-collapse, opus, hand-made worktree).

2026-07-05 · Han directive: NO ALIAS SHIMS · kit-foldin left ~34 lib-root SYMLINK aliases (lib/board.sh -> board/board.sh etc.). Han rejects scattered aliases , SG-01 now does the PROPER restructure: remove every alias + update every call-site via one LIB_ROOT anchor (no per-file wrapper). Gate: `find lib -maxdepth 1 -type l` EMPTY. SG-01 is now a HIGH-blast-radius call-site refactor (opus, over-tested). Propagated to SG-01/ROADMAP/pointer/DECISIONS + design-note E3.
2026-07-05 · GROUNDED against real merged state (Han caught it) · kit-foldin had ALREADY shipped+archived on origin (dwarves-kit #183-189, ops #720/#721) while this session designed off a stale "kit-foldin drafted" snapshot , so the kit-foldin re-scaffold + its advisor passes this session were REDUNDANT. Verified the real kit: lib/ subsystem dirs EXIST + ~34 flat shims; tools/ has ledger-observatory/session-*/skill-curator/plugin-check; hooks backlog-stage/citation-guard/context-hints/harvest. Precondition MET; SG-01 reframed (dirs exist, fold tools/ in + resolve shims, don't create); "03/07 provisional / 04 best-guess" hedges RESOLVED. Updated ROADMAP/pointer/HANDOFF/DECISIONS/NOTES + backlog ID-277. LESSON: verify git/real state before executing, never a scaffold snapshot.
2026-07-05 · advisor P5+P6 pre-launch pass · P5: 1 CRITICAL (02/03 framed parallel in ROADMAP/POINTER but 03 wraps `stats` so needs 02 , FIXED to linear 01->02->03) + 1 MAJOR (02 no-persist NC only checked the ledger dir; a cache elsewhere passes , FIXED to a full-tree snapshot diff) + 1 MINOR (rot risk is contained by 01's re-read, not just 03/07 , clarified in NOTES+DECISIONS). P6: 6 APPLY-NOW gate-hardening folded in (01 CI-path NC; 02 standing no-persist lint; 04 migration-idempotence sentence + standing manifest-lint + post-install `--help` smoke; P6#1 mirror-baseline -> HANDOFF) + 4 RIDE-LATER -> Proposed additions (multi-agent philosophy para, 05+06 merge, 01-absorbs-stats-mv question for Han). Scaffold sound; verdict-blocking findings fixed.
2026-07-05 · drafted · kit-modularity scaffolded from research/2026-07-05-kit-modularity-design.md (decisions A-G; F=per-module gate, C=folded, H=separate mega). 7 sub-goals (01 module-collapse, 02 stats-plane, 03 subsystem-commands, 04 install-wire, 05 operate-contract, 06 docs, 07 reconcile). gh-sequential, auto-bottom-up, gated-final. NOT launched; BLOCKED on kit-foldin (ID-276) shipping first. Han directed full-scaffold ahead of the precondition (accepted rot risk on 03/07).
