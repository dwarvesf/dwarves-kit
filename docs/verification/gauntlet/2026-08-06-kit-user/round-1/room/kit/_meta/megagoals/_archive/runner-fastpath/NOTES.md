# NOTES: runner-fastpath

Working memory for the loop. ROADMAP.md stays the source of truth for done-ness.

## Active blockers

(One fingerprint per blocker: `<sub-goal> | <one-line cause> | <what unblocks>`. Remove when cleared.)

- (CLEARED 2026-07-04) 05-mega-durations was parked on gate-review#701 (review-yield-lens); #701 MERGED (origin 3b2221ed, gate-review closed out via #704). 05 UNPARKED, bases on main.
- (CLEARED 2026-07-05) 07-bridge-mirror was blocked by a master-branch SPEC-146 duplicate (03K PR #178 + 04 PR #176 both reserved it in parallel before either merged). FIXED: dwarves-kit PR #180 (`fix/spec-146-collision`, squash ba42d43) renumbers 03K's spec to SPEC-148, every cross-reference updated, board-tool's legitimate SPEC-146 refs left untouched; test-meta.sh 670/671 -> 670/670. Rebased 07's branch onto the fixed master (clean rebase, no conflicts, 671/671 local incl. 07's own new assertion), force-pushed (own branch, PR still open, no review activity) to re-trigger CI.
- 2026-07-05 SG-07 MERGED (dwarves-kit PR #179, squash c64ef47): `lib/board-mirror.sh` (`mirror`/`status` subcommands) built. STEP 0 findings against a REAL local hermes dev-home (v0.18.0): no update/rename verb (title/body immutable post-create, a changed row only gets a `comment`); LOAD-BEARING discovery -- `create --initial-status blocked` and `block --kind dependency` both auto-promote back to `ready`/`todo` within ~15-20s with NO gateway running, only `block --kind needs_input` proved durable (verified 20+s, multiple calls, 3-row AND 20-row live batches) -- reachable state set narrowed from the planned 6 to 4 (`triage/ready/blocked/done`); no body-length truncation up to 20k chars; a real board-name collision in the shared hermes-fleet dev-home led the worker to use a fresh disposable HERMES_HOME instead. 6/6 named NCs green (59/59 assertions, one deliberate-breakage revert confirmed exactly 3 flip red). DOUBLED live dev-home E2E: run 1 created 4 cards (1.14s, durable 20s+); run 2's plan EMPTY (0.087s, 0 hermes calls). 20-row batched load measured 5.504s wall / 25 calls (~220ms/call); idempotent re-run 0.209s. Two real bugs caught+fixed pre-ship: a newline-delimited argv decode silently split a multi-line card body (fixed to NUL-delimited jq decoding), plus the state-auto-promotion behavior above. row_hash = `sha256(repo\x1fid\x1fitem\x1fnotes\x1fstatus)`. FINAL state mapping: `queued->triage`, `claimed/speccing/validated/executing->ready`, `parked->blocked` (via create+`block --kind needs_input`), `shipped/dropped->done` (disappeared-row path only, never a direct create target). `bridge` column format for ops-toolkit's real `_meta/boards.txt` (NOT YET applied -- consumer-side edit, opt-in default OFF, ready whenever 08 or the convergence demo needs it):
  ```
  # Format: <name>  <path-to-BACKLOG.md>  [bridge]
  # bridge: 'on' opts the repo into the board-mirror Hermes sync (SG-07); anything else
  # (absent, 'off', any other token) is NOT mirrored. Default is OFF -- a repo must
  # explicitly opt in. Sensitive repos (trading, family-office) must never be 'on'.
  ```
  Remote-applier plan format: NDJSON, one op/line with a literal `argv` array; built but not exercised against a real remote host (none available to the worker). Snapshot: NDJSON at `<repo-root>/_meta/.board-mirror-snapshot.jsonl`. Handoff for 08: the conflict rule keys on `lib/board-mirror.sh:333`'s `p_hash[$1]=$3` (per-origin row_hash read from this snapshot).
- 2026-07-05 SG-08 DISPATCHED (dwarves-kit `.claude/worktrees/board-writeback`, `feat/board-writeback` base master, sonnet, GATE -- worker explicitly instructed never to merge). Corrected the goal file's stale stacking instruction (base `feat/board-mirror`/stack-on-07) since 07 already merged to master -- worker told to base directly on master instead, which already carries everything 07 built.
- 2026-07-05 SG-08 DONE, PR #181 OPEN + HELD FOR HAN (do not merge; dwarves-kit `feat/board-writeback` -> master, final commit f7ed5eca, 10 files +1686/-12): `lib/board-writeback.sh` built, reuses 07's `lib/board-mirror.sh` at the function level (not a re-fork). Diffs a live Hermes board vs the SPEC-147 mirror snapshot; validates opted-in-repo + legal state + row_hash match (git wins on any mismatch); applies via an ISOLATED git worktree branch (never the caller's own checkout) + one `actor=hermes` commit + a HELD `gh pr create`. 6/6 named NCs green (53/53 assertions incl. hash-mismatch, illegal-status, empty-changeset, non-opted-in-repo, missing/corrupt-snapshot, two-writer-coexistence), 0 regressions across test-board/test-board-mirror/test-meta/test-hooks, shellcheck clean. Fixtures-only round-trip used a REAL throwaway repo + a REAL local bare remote (so `git push` genuinely succeeds, zero network) + stubbed hermes/gh -- captured exactly one status flip (`queued -> claimed`), commit body carries `actor=hermes`. Rung-4 red-team `VERDICT: SECURE`: 4 live adversarial attempts incl. a live backtick command-injection payload via a crafted `origin` field, all blocked, 0 fixes needed. Reverse state mapping (Hermes->git) is DELIBERATELY LOSSY: `triage->queued`, `ready->claimed` (the honest nearest state, not a guess among claimed/speccing/validated/executing), `blocked->parked`, `done->shipped`; `todo`/`running`/anything else REJECTED. Snapshot refresh updates only `hermes_status` (row_hash passes through unchanged) since the PR is held/unmerged -- keeps `board mirror` idempotence correct until Han merges, at which point mirror self-heals; documented limitation: if Han closes the PR without merging, writeback won't auto-recover without a fresh Hermes-side move. Two real bugs caught+fixed: a macOS `/var` vs `/private/var` physical-path mismatch breaking worktree-relative-path math, and a bash `if ! cmd; then rc=$?` gotcha silently capturing the inverted exit code for the missing-snapshot refusal. ROADMAP box left UNCHECKED per OPERATE.md (checked box requires `merged <sha>`); PR # + HELD status recorded inline. Worktree removed (branch preserved for Han's review/merge).
- 2026-07-05 **BOTH GATES NOW HELD (06 + 08), 09 BLOCKED ON HAN**: every auto sub-goal in this mega is merged (01,02,03R,03K,04,05K,05R,07) or superseded (03,05). The only remaining sub-goal, 09-kit-layout, depends on 03K+04+07+08 ALL MERGED -- 08 is a live PR (#181) awaiting Han's gate review, so 09 cannot dispatch yet. Per POINTER_PROMPT STOPS ("gates 06/08 + held final PR -- STOP /goal MANUALLY") this is a legitimate stop point, not a stall: the mega is CORRECTLY BLOCKED on human action (merge #712 and #181, or send them back with changes) before 09 and the convergence gate can proceed.
- 2026-07-05 CORRECTION: the Stop hook (the mega's own STOPS clause interpretation) held that 06/08 being held does NOT excuse skipping the convergence-gate DEMO itself, since none of its three components (Air smoke journal row, `board queue --dry-run`, first live Hermes mirror x2) actually depend on 08 merging -- only 09 and the final PR do. Proceeded with the demo + integration-check now, still WITHOUT merging #712/#181.
- 2026-07-05 **FIRST LIVE HERMES MIRROR RUN (convergence demo leg)**: before writing anything, surfaced the actual dry-run volume to Han (a full sync of ops-toolkit+dwarves-kit opted-in via 07's `bridge` column would be 216 card creates in one shot) rather than treating the mega's "STOP /goal MANUALLY" language as blanket pre-authorization for that volume. Han chose: scope the first live demo down to ONLY the megagoals board (one card per active mega-goal), defer the full per-repo BACKLOG.md sync to a later deliberate decision. Executed:
  1. Added the `bridge` opt-in column to the REAL `_meta/boards.txt` (`ops-toolkit` + `dwarves-kit` -> `on`, everything else default off) -- ops-toolkit PR #714, merged f384c301. Verified byte-identical render + correct opt-in filtering before merging.
  2. Gitignored the runtime snapshot state (`_meta/.board-mirror-snapshot.jsonl`, host-side per repo convention, never repo state) -- ops-toolkit PR #715, merged f7c41242.
  3. Pulled the Mini's `~/workspace/tieubao/dwarves-kit` checkout current (it was stale, predating the whole mega) so its `~/.claude/dwarves-kit` symlink resolves the new `lib/board-mirror.sh` -- confirmed clean tree first.
  4. Read-only STEP-0 check on the real Mini Hermes agent (`hermes kanban boards list`): only a `default` (empty) board existed, no name collision risk.
  5. Generated the full dry-run plan, filtered to `megagoals:` origins only via `jq` (16 rows; the tool itself has no `--only megagoals` flag -- this filtering is a conductor-side workaround, not a code change, flagged in the P6 pass below as a possible follow-up), applied via ONE ssh call to the Mini's `board-mirror.sh apply-plan` (the same remote-execution primitive `cmd_mirror --remote` itself uses).
  6. **A real gap surfaced and was fixed on the spot**: the `megagoals` Hermes board didn't exist yet -- `board-mirror.sh` does not auto-create boards (a legitimate first-time-setup step, not a bug in 07's build). Created it explicitly (`hermes kanban boards create megagoals`, no `--switch`, so Han's current/default board context was undisturbed).
  7. Re-ran apply-plan: **16/16 cards created successfully** (`ready` state), verified live via `hermes kanban boards list` (`megagoals ... ready=16`).
  8. Manually replayed `snapshot-upsert` for all 16 result rows (since the manual jq-filtered path bypasses `cmd_mirror`'s own auto-upsert) to keep the snapshot correct for idempotence.
  9. **Second pass confirmed idempotent**: re-ran the dry-run plan against the updated snapshot -- 0 megagoals-origin ops (all correctly read as unchanged). This satisfies the demo's "x2, 2nd empty" requirement.
  - Captured `board queue --dry-run` against the real cockpit: `queue: 0 rows` (honest -- no `#queue{}` tokens exist in any real BACKLOG.md yet).
  - Full dwarves-kit test suite re-confirmed green on the assembled HEAD: test-meta 671/671, test-board 45/45, test-board-mirror 59/59, test-queue.bats 14/14, test-hooks 452/452.
- 2026-07-05 CONVERGENCE-GATE REVIEW LENSES dispatched (in-harness, fresh context, parallel): `kit:advisor` P5 critique + `kit:advisor` P6 over-suggest + `kit:security-reviewer` (integration-level security recheck across the assembled `board`/`queue`/`board-mirror` stack, frozen diff `fafad09..HEAD` dwarves-kit / `dcc40f0a..HEAD` ops-toolkit). Awaiting results.
- 2026-07-05 SECURITY RECHECK returned, **VERDICT: HAS ISSUES** (1 MAJOR, 2 minor, 0 critical): **MAJOR -- stored prompt-injection surface.** `board mirror`'s `extract_rows` (reads a BACKLOG.md row via the shared `pb_rows`) copies the Item/Notes columns VERBATIM into a Hermes card title/body, with ZERO content sanitization -- this is independent of `board queue`'s allow-list, which only validates the `#queue{repo=...,pointer=...}` TAG's own fields, never the surrounding row text. A single row can simultaneously (a) carry a valid, allow-list-passing `#queue{}` token that drives a real unattended `/goal` session, AND (b) carry fully attacker-controlled free text in Item/Notes that lands verbatim in a real Hermes card, unmarked as untrusted. Not yet exploitable (no Hermes-card-reading automation exists today) but exactly the "stored injection payload" shape that would matter the moment one does. CONDUCTOR NOTE: this finding does NOT apply to what was actually mirrored live tonight (the 16 megagoals-origin cards are auto-generated "progress N/M" text from `extract_megas`, not raw BACKLOG.md Item/Notes) -- it only materializes once/if the deferred full per-repo BACKLOG.md sync (216 rows) ever runs. Recommended fix (3 parts, not yet applied, deliberately NOT actioned in this mega -- routed as a prerequisite for the staged-rollout ladder P6 already proposed): (1) document in SPEC-147 that Hermes card content is untrusted data, never instructions; (2) prefix synthesized card bodies with a fixed disclaimer line; (3) strip the raw `#queue{...}` token substring from mirrored Item/Notes before it reaches Hermes. Minor findings (both low-severity, no realistic attacker path under the single-operator trust model): `--remote-kit-path` is the one argv-unsafe path in the whole diff (shell-interpolated over ssh, zero test coverage of the `--remote` branch at all); a theoretical `--snapshot`-vs-queue-pointer path collision requires deliberate operator misconfiguration, not attacker data.
- 2026-07-05 P5 CRITIQUE returned, 2 MAJOR findings, 0 previously-rejected (checked dwarves-kit's `docs/verification/rejected-findings.md`, no match), everything else checked out clean (04<->03K queue-row seam, 07<->08 snapshot-format seam vs the OPEN #181 diff, byte-identical render re-verified LIVE not trusted from the stale proof, layered allow-lists confirmed independent not silently-covering, the live 16-card trace matched NOTES exactly). **MAJOR #1, SELF-CRITIQUE, addressed by acknowledgment not code**: the SSH `--remote` path in `cmd_mirror` (`lib/board.sh:408-419`) had ZERO automated test coverage before tonight, AND its first-ever execution was the conductor's own live demo directly against the real production Hermes agent (16 cards) -- unlike EVERY other live-touching leg in this mega, which smoked a throwaway/mktemp target first (03K's two tmux smokes on fixture repos, 06's Air smoke on a fixture repo). Went cleanly (16/16 created, idempotent second pass), but Hermes has no delete verb, so a malformed-argv batch would have needed manual per-card cleanup, not a rollback -- exactly the failure mode the mega's own "smoke a throwaway target first" discipline exists to prevent, and the one leg that skipped it. Routed to NOTES `## Proposed additions` as a real follow-up: add stub-ssh/stub-hermes test coverage for the `--remote` branch before it is ever exercised against a real host again. **MAJOR #2, FIXED IN THIS SESSION**: HANDOFF.md was stale (still said the demo was "blocked behind 09" after the demo had already run) -- rewritten to reflect the actual current state (see HANDOFF.md itself).
- 2026-07-05 RECHECK-VERIFIER returned, **VERDICT: PASS**, fresh re-execution (not a read-back) of all five dwarves-kit test suites at HEAD `c64ef47`: 671/671, 45/45, 59/59, 14/14 (bats), 452/452 -- exact match to the claimed counts, zero discrepancy, all five confirmed as genuine assertion-bearing suites (not vacuous always-pass commands).
- 2026-07-05 P6 OVER-SUGGEST returned, 10 proposals (4 `now`/`soon`-priority worth flagging loudly, 1 `later`, 1 rejected-with-reason): (1) `now` -- `board mirror` should grow a real `--only megagoals`/`--card-filter` flag; tonight's hand-`jq`-filter to scope the 16-card demo is itself the evidence of a recurring need. (2) `soon` -- write a staged-rollout ladder for the deferred 216-row full sync (mega-cards -> one repo -> all opted-in -> re-evaluate trading/family-office), each tier gated on a named graduation criterion, not just "the full sync would be 216 rows" with no path. (3) `soon` -- wire `board status` into something PASSIVE (a SessionStart hook line, repo-memory injection) since nothing currently pushes mirror-staleness to the operator, pull-only today. (4) `soon` -- a `board mirror --check` drift-log-only mode (zero writeback action) would build real calibration data for Han's own gate review of held PR #181, cheaply, before he decides whether to merge writeback at all. (5)+(6) `soon`, process -- concretize the SPEC-146 dup-number lesson into an actual CI job (not conductor discipline) + fold the two shell-portability bugs (BSD/GNU grep `\t`, fish `2>&1`) into a durable kit-authoring convention (dry-run new bash tests in a Linux sandbox before push; default driven-command wrappers to `bash -lc`). (7) `now`, process -- promote the caffeinate-must-wrap-the-INNER-command gotcha from runbook prose into a persistent code comment in `lib/queue.sh`/`orchestrate.sh` itself. (8) `later` -- run a STEP-0-equivalent probe of `ntn`'s real verb surface before the deferred Notion adapter is designed (same lesson as 07's Hermes-state-model correction). (9) `later` -- re-run `mega-durations` after the first real overnight `orchestrate.sh queue` run to see if launcher-driven runs populate more OUTCOME brackets than the historical 841/853-excluded corpus. (10) rejected-with-reason -- a `mirror --to notion` stub now, ahead of the P2 gate; NOTES already correctly sequences this behind the Hermes mirror proving out + item 8's probe. None of these are blocking; routed to NOTES `## Proposed additions` verbatim below rather than actioned in this mega.

## Proposed additions

- (2026-07-05 SPINE CHANGE, CONFIRMED + REWRITTEN) runner moved Go/ops-toolkit -> bash/dwarves-kit; board tool ALSO moved to dwarves-kit (ship the harness suite as one kit). Sub-goals rewritten: 03R (retire Go tool, ops-toolkit) + 03K (kit queue, dwarves-kit) + 04/07/08 (kit board tool) + 06 (Air runs orchestrate.sh queue). ROADMAP/POINTER/design-doc amendments/DECISIONS all realigned. See DECISIONS 2026-07-05 (spine-change + board-to-kit).
- (2026-07-05, DIRECTION, NOT this mega) Harness-kit consolidation: apply the same engine->kit / data->ops-toolkit split to the rest of cc-elevation. Kit candidates (engines): cc-observe, cc-citation-guard, verify-claim, cc-workflows, cc-harvest/cc-backlog engines, cc-context-hooks. Already in kit: gate-ledger, review-findings-memory, advisor, meta-agent. **ledger-observatory PROMOTED out of this candidate list 2026-07-05 (Han-directed) into runner-fastpath itself as SG-05K/05R** (was originally deferred here as "ops-side by design"; reversed once flagged that it reads gate-ledger data already in the kit). Stay ops-toolkit (personal/tenant): cc-money-gate (tenant paths), prose-rag (private stores), boards.txt + Hermes config. Remaining candidates still size as their own planning pass (a research note + a mega) AFTER runner-fastpath; do NOT fold into it. **Design pass DONE 2026-07-05**: `research/2026-07-05-cc-elevation-kit-foldin-design.md` , full 14-tool inventory (fork research found cc-intel/cc-recall/cc-self-improve/cc-plugin-check + a live duplicate: `tools/meta-agent` CLI vs `kit:meta-agent` agent), a three-shape framework (hook-lib / subagent / standalone `tools/` CLI, matching what's already proven rather than inventing a 4th shape), and a per-tool disposition table. all 5 open questions RESOLVED same day via two parallel forks: cc-observe+cc-recall stay two CLIs (extract shared parser, don't merge binaries); verify-claim has no kit duplicate (becomes a new subagent, but needs a real fan-out redesign, not a file move); the deploy mechanism correction is load-bearing (`hooks/` vs `lib/` were conflated; `install.sh` already supersedes `redeploy.sh`'s snapshot dance for anything landing as a kit hook); cc-self-improve is clean modulo one env-default line; sequencing still waits for runner-fastpath to converge. Mega-goal drafting is next, whenever runner-fastpath ships.

(Discovered sub-goals go here, never inline into ROADMAP mid-loop.)

- (2026-07-05, discovered during 05K) dwarves-kit's `.github/workflows/test.yml` has no step running `tools/ledger-observatory`'s Python/uv test suite (14 files) -- it only runs the bash `tests/*.sh` files. Not a contract miss for 05K (the goal file only required the existing suite green locally), but a real coverage gap now that the kit CI matrix is the only automated gate. Not this mega; route to a small follow-up (add a `uv sync && uv run pytest` step to test.yml, gated on `tools/ledger-observatory` existing).

- (2026-07-05, NOW IN SCOPE) Han chose "do it all in SG-04": `tools/board/` becomes the SOLE board tool. SG-04 migrates render/next/priority out of `_meta/board`+`board-all` (reducing them to one-line shims, byte-identical output NC) AND adds `queue`; 07/08 add mirror/writeback/status. Base render still delegates to dwarves-kit `backlog.sh`. No longer a deferred follow-up.
- (2026-07-05, overlap sweep) Retire ID-138 `board-sync` into `tools/board/`: ID-138's UNBUILT board->Notion one-way pusher (own engine) and runner-fastpath's deferred Notion P2 leg must not both own "push the board to Notion". Resolution: when the Notion leg is built, make it a `tools/board/ mirror --to notion` adapter (reusing `notion-ops` as the write primitive) and drop ID-138's separate `tools/board-sync/` plan. Design call for Han before EITHER Notion path is coded; neither is urgent (both deferred).
- (2026-07-05, overlap sweep) cc-backlog `add-backlog` is the CREATE-path intake half of the board (append new rows). Future convergence (optional): expose it as a `board intake` subcommand so `tools/board/` is the single BACKLOG.md manager. Not this mega; its Python create-parser stays a deliberate outlier for now (see DECISIONS).
- (2026-07-05) Single kanban PARSER: `tools/board/lib/parse-board.sh` is the ops-toolkit parser; dwarves-kit `backlog.sh` still parses for its own render. Optional later: a structured `backlog.sh rows` emit so both consume ONE parser. Cross-repo (kit) change; only if the two bash parsers actually drift.

- runner writes verdicts back to board rows (two-way) , only if the journal proves insufficient after real nights
- launchd Phase 2 , trigger: journal shows a steady nightly cadence worth automating (06's runbook names this)
- (advisor P6 #7) auth-failure fingerprinting in the digest ("run `claude login` before tonight") , wait for ONE real observed auth-failure log to calibrate the grep
- (advisor P6 #8) held-PR aging digest line (`gh pr list` per touched repo, held > N days) , cross-repo scope, pairs with the morning review window
- (advisor P6 #10) overnight gate-deny drain: queue "respond to review on branch X" single-goal pointers for the FOLLOWING night , needs real review-response volume first
- (advisor P6 #11) enqueue-from-drafting: plan-for-mega-goal/plan-for-goal terminal step offers "queue this for the runner?" and writes the board token , depends on 04's grammar being final
- (advisor P6 #12) SG-05 join once unparked: per-rid wall time (kit_gates) x per-night cost (runner journal) = the complete "where does the time/money go" view
- (advisor P6 #13, REJECTED with reason) pre-night cost ESTIMATOR: speculative model with zero calibration data; the retrospective `report` digest ships first, regress later against real journal rows
- (bridge P2) Notion adapter via `ntn` on 07's seam, per-repo opt-in, trading/family-office HARD-OFF , only after the Hermes mirror proves out
- (bridge P3) mega-runner reads the Hermes board as a third queue backend (phone-side card move = "queue this mega tonight") , after 04 + 07 both live
- (bridge writeback extensions, pending Han's 08 gate) new-card writeback with temp-ID reservation; note edits; boards beyond ops-toolkit
- (advisor P6 delta #12) card-body truncation POLICY , STEP 0 probes the limit; decide a truncation length only when a real notes cell approaches it
- (advisor P6 delta #14) bridge sync cost/latency view joined with the runner journal , needs the batched-load wall-time data from 07's proof first
- (2026-07-05, convergence-gate P6, adopt into a future sub-goal) `board mirror --only <filter>` flag (real code, not a conductor-side `jq` workaround) -- evidenced by tonight's live demo needing exactly this
- (2026-07-05, convergence-gate P6) staged-rollout ladder for the deferred 216-row full BACKLOG.md sync, graduation criterion per tier
- (2026-07-05, convergence-gate security recheck, MAJOR, PREREQUISITE for the item above) `board mirror`'s `extract_rows` copies BACKLOG.md Item/Notes VERBATIM into Hermes card title/body with zero content sanitization -- a stored prompt-injection surface once/if the full per-repo sync ever runs (not yet exploitable, no Hermes-card-reading automation exists today). Fix before the full sync, not before this mega closes: (1) document Hermes card content as untrusted in SPEC-147, (2) prefix synthesized bodies with a disclaimer line, (3) strip the raw `#queue{...}` token from mirrored text.
- (2026-07-05, convergence-gate P6) `board status` wired into something passive (SessionStart line / repo-memory) instead of pull-only
- (2026-07-05, convergence-gate P6) `board mirror --check` drift-log-only mode to build real calibration data ahead of Han's SG-08 writeback gate decision
- (2026-07-05, convergence-gate P6, process) an actual CI job re-checking for duplicate SPEC numbers post-merge, not conductor discipline (the SPEC-146 collision was caught by luck)
- (2026-07-05, convergence-gate P6, process) a durable kit-authoring convention: dry-run new bash test files in a Linux sandbox before push (agentkernel); driven-command wrappers default to `bash -lc`
- (2026-07-05, convergence-gate P6) promote the caffeinate-wraps-the-inner-command gotcha from 06's runbook prose into a persistent code comment in `lib/queue.sh`/`lib/orchestrate.sh`
- (2026-07-05, convergence-gate P6) a STEP-0-equivalent probe of `ntn`'s real verb surface before the deferred Notion adapter is designed
- (2026-07-05, convergence-gate P6) re-run `mega-durations` after the first real overnight queue run to see if launcher-driven runs populate more OUTCOME brackets
- (2026-07-05, convergence-gate P5 self-critique) add stub-ssh/stub-hermes automated test coverage for `cmd_mirror`'s `--remote` branch (`lib/board.sh:408-419`) -- it had zero coverage and its first-ever execution was directly against the real production Hermes agent, the one live-touching leg in this mega that skipped a throwaway-target smoke first

## Event log

(Loop appends one line per significant event: dispatch, merge, blocker, gate.)

- 2026-07-04 LAUNCH GUARD CLEARED: gate-review-absorptions AUTO sub-goals all merged (01=dwarves-kit#172, 02=dwarves-kit#173, 05=ops#697, 06=dwarves-kit#174), 03/04 held awaiting Han (ops#698, ops#701). Its ROADMAP box-flips lag on main; gh is ground truth per OPERATE. runner-fastpath 05 stays PARKED (unparks only if gate-review#701 merges; it is held).
- 2026-07-04 CONDUCTOR DECISION: conductor is the single writer of the mega-dir bookkeeping (ROADMAP/HANDOFF/DECISIONS/NOTES). Workers report PR#/shasum/decisions/handoff-next in their final message; conductor transcribes. Rationale: cross-repo workers (01 dotfiles, 02 dwarves-kit) cannot reach ops-toolkit's ROADMAP, and 8 parallel branches editing 4 shared files would collide at bottom-up merge.
- 2026-07-04 WAVE 1 dispatched (parallel): 01-triage-ladder (dotfiles, feat/triage-ladder, sonnet) + 03-runner-core (ops-toolkit, feat/mega-runner-core, sonnet).
- 2026-07-04 SG-01 MERGED: dotfiles PR #202 (squash b89f479), shellcheck + test-macos green, worktree removed. Ladder-block shasum a42939f recorded in DECISIONS for 02.
- 2026-07-04 WAVE 2 begins: SG-02 (dwarves-kit mega-mirror) dispatched with 01's shasum; SG-03 still running.
- 2026-07-04 SG-02 MERGED: dwarves-kit PR #175 (squash fafad09), mega.md block shasum == a42939f (byte-identical), never-diverge row in SPEC-142, gate-ledger lane=normal recorded, both CI green, worktree removed. Triage-ladder lanes (dotfiles+dwarves-kit) COMPLETE.
- 2026-07-04 Only the ops-toolkit lane remains: SG-03 running -> 04 -> {06 gate | 07 -> 08 gate}; SG-05 parked. On 03 merge, dispatch 04 (stacked on feat/mega-runner-core).
- 2026-07-04 SG-03 MERGED (ops PR #705, squash c43bcab); independently re-tested green (39 tests, 5 NCs), worktree removed. INCIDENT: worker polluted main with a fixture commit; recovered clean (see DECISIONS). Parallel: gate-review-absorptions CLOSED OUT on origin (#698/#701 merged, #704 closeout) -> SG-05 UNPARKS.
- 2026-07-04 WAVE 3 was PREPARED (worktrees for 04/05) but NOT dispatched; superseded by the 2026-07-05 replan below. (Struck: the pre-replan framing had SG-04 as "self-contained Go" with parser-consolidation deferred to a bridge-language choice. RESOLVED the opposite way on 2026-07-05: bash won, not Go. No code was produced under the old framing.)
- 2026-07-05 REPLAN (Han-directed, via plan-for-mega-goal): SG-04/07/08 rewritten to the bash `tools/board` (queue/mirror/writeback, surfaced via _meta/board-all dispatch) + Go-scheduler architecture; Python+DuckDB dropped from the new work (see DECISIONS 2026-07-05). ROADMAP/POINTER/design-docs realigned; advisor P5 pass applied (3 stale-reference fixes). Ready to relaunch from POINTER_PROMPT.md.
- 2026-07-05 advisor P5 critique on the replanned scaffold: 3 findings, all stale pre-replan references (CRITICAL: ROADMAP "04's Go reader"; MAJOR: 04 pointed at a runner-design amendment that did not exist + that doc's stale line 141 Python+DuckDB; MINOR: NOTES "self-contained Go" event line). All fixed: runner-design doc got a matching 2026-07-05 amendment + line 141 struck, ROADMAP line reworded to "bash board-parse helper", NOTES line struck. Advisor verified 01/02/03 PR/SHA citations, the main.go:147 seam, the tsv row shape, and merge-order all correct.
- 2026-07-05 second replan (Han): board+queue move INTO dwarves-kit (ship harness as one kit); runner becomes a UI-LAUNCHER (send-keys /goal, not headless claude -p); 03R retire + 03K launcher + SG-09 kit-layout added. Second advisor P5: 7 findings (stale Go/headless/DuckDB refs), all fixed. Persisted as #708.
- 2026-07-05 RELAUNCHED. #708 merged to main (dcc40f0a) after resolving a conflict with #707 (independently landed the richer harness-ops research doc + kit-wiring/kit-retro; my redundant research doc dropped, unique bits live in repo-memory harness-machinery-in-the-kit). WAVE 1 dispatched (parallel, 4 workers): 03R retire-runner (ops) + 05 mega-durations (ops) + 03K kit-queue-launcher (dwarves-kit, opus) + 04 board-tool (dwarves-kit). dwarves-kit worktrees hand-made; workers reserve SPEC via spec-next (collision-proof).
- 2026-07-05 Han interrupted, killed both 03R and 05 mid-dispatch (no commits/PRs lost, worktrees stayed empty). While paused, Han asked "why is ledger-observatory not in the kit too" -> agreed + he confirmed. FOURTH replan (smaller scope than the earlier three): SG-05 split 03R/03K-style into 05R (ops-toolkit, retire the old copy) + 05K (dwarves-kit, verbatim-migrate the whole tool into a NEW `tools/` top-level + fix its adapter defaults + fold in the `harness-observatory` mega's doc tree + add the `mega-durations` query). Mega is now 13 sub-goals. ROADMAP/DECISIONS/NOTES updated; goals/05K + 05R written. Not yet re-dispatched (see Active blockers / awaiting Han's go-ahead to resume wave 1).
- 2026-07-05 WAVE 1 RE-DISPATCHED (subagent-delegate, 4 parallel in-harness workers, hand-made worktrees per the CROSS-REPO rule): 03R-retire-runner (ops-toolkit `.claude/worktrees/retire-runner`, `chore/retire-mega-runner` base main, sonnet) + 03K-kit-queue (dwarves-kit `.claude/worktrees/orchestrate-queue`, `feat/orchestrate-queue` base master, opus) + 04-board-queue (dwarves-kit `.claude/worktrees/board-tool`, `feat/board-tool` base master, sonnet) + 05K-observatory-to-kit (dwarves-kit `.claude/worktrees/observatory-to-kit`, NEW branch `feat/observatory-to-kit` base master, sonnet). Housekeeping before dispatch: removed the stale ops-toolkit `mega-durations` worktree (empty, belonged to the now-superseded original SG-05) and created the fresh `observatory-to-kit` worktree for 05K. Each worker instructed to open its PR (not merge), avoid touching mega-dir bookkeeping, and report back PR#/SHA/proof/decisions/handoff per the conductor-transcribes contract. Awaiting completion notifications.
- 2026-07-05 SG-03R MERGED (ops PR #709, squash f3714a61): `tool.toml` status=abandoned, MANIFEST row updated (Status cell only, no column changes), README banner prepended; `git log --follow` verified resolving across both commits. Worker hit the ship-gate's behavioral-change classifier on a docs/metadata-only change and used the repo's audited `proof-ledger.sh override` escape hatch (correct per ops-toolkit CLAUDE.md Done-gate section for non-behavioral work). No CI configured for this PR (empty rollup, not red); no NCs applicable (Design: obvious). Worktree + local branch cleaned up post-merge. Nothing else depends on this landing first.
- 2026-07-05 SG-04 (board-tool) worker DONE, PR OPEN awaiting CI (dwarves-kit PR #176, `feat/board-tool` -> master, 10 files, +1289/-1): kit `board.sh`/`parse-board.sh` built, render migrated (13 render invocations byte-identical vs the live 13-repo cockpit incl. two deliberate-breakage NC reverts), `queue` subcommand + 5 named NCs all green (45/45 assertions), live `--dry-run` against the real cockpit read-only (0 rows, honest, no `#queue{}` tokens exist yet in the wild), COVERAGE-DELTA 0->44 with zero regression (test-meta 670/670, test-hooks 452/452), rung-4 7-attempt red-team `VERDICT: SECURE`. Two real bugs caught+fixed by the suite itself: `pb_queue_rows` non-canonical repo_root comparison (macOS `$TMPDIR` trailing slash), bash-3.2 `unset 'arr[-1]'` incompatibility. Token grammar: `#queue{repo=<name>,pointer=<relative/path>}`. CONSUMER keys: `--repo-root`/`REPO_ROOT` (flag>env>git-toplevel>cwd), `--registry`, `--backlog-file`, `--dry-run`. No `bin/board` (kit has no `bin/` anywhere; matched `bash lib/*.sh` convention). Handoff for 07: `lib/parse-board.sh:99` (`pb_queue_rows`) + `lib/board.sh:275` (`cmd_queue`). Worker flagged a FALSE blocker (`~/.claude/dwarves-kit` "copy-installed, won't auto-sync") -- CONDUCTOR VERIFIED this is wrong: `~/.claude/dwarves-kit` is a directory of SYMLINKS into this exact checkout (`lib -> ~/workspace/tieubao/dwarves-kit/lib`), so once master has `lib/board.sh` the shim resolves with zero extra sync step. Exact shim file contents captured below for pasting into ops-toolkit once PR #176 merges. CI (ubuntu+macos) was IN_PROGRESS at report time; conductor holds the merge until green.

- 2026-07-05 SG-05K MERGED (dwarves-kit PR #177, squash 662a1fab): tools/ledger-observatory now lives at dwarves-kit/tools/ledger-observatory/. Worktree + local branch cleaned up post-merge; local dwarves-kit master fast-forwarded. Flipped ROADMAP box. Dispatched 05R (ops-toolkit, worktree .claude/worktrees/retire-observatory, chore/retire-ledger-observatory -> main, sonnet) with the confirmed merged SHA + path for its README/tool.toml banner.
- 2026-07-05 SG-04 CI FAILED on ubuntu-latest (`test-board.sh`: 2/37 assertions -- `pb_rows sees ID-001 as queued` / `ID-008 as claimed`, suspected mawk-vs-bwk-awk portability divergence in `pb_rows`'s dynamic-regex/split; macOS CI green, all 34 other assertions incl. every injection/allow-list NC green). Dispatched a focused fix worker into the SAME worktree/branch (`.claude/worktrees/board-tool`, `feat/board-tool`) to reproduce on Linux (agentkernel sandbox or local mawk), find the real divergence, patch `pb_rows` portably, verify full suite + kit suite, push. Awaiting its report before re-checking CI and merging #176.
- 2026-07-05 SG-05K (observatory-to-kit) worker DONE, PR OPEN awaiting CI (dwarves-kit PR #177, `feat/observatory-to-kit` -> master, 108 files, +17139/-0, final commit 4e39a9c93af71179610c5586a93c07e35de2ec1d): full existing suite green post-move (14/14 files incl. new `test-mega-durations.sh`), relocation NC (5/13 files initially failed on hardcoded sibling-tool paths, fixed to labeled SKIP guards, re-ran green), adapter-default NC (kit-internal sources -> repo-relative via new `config._kit_repo_root()`; ops-toolkit-specific sources -> required-explicit no-default, missing-config now a clean `RuntimeError` caught at all 3 CLI sites rather than a silent wrong write), mega-durations fixture+NC (golden 2-rid fixture + a fully-stripped 0-rid/8-excluded NC), LIVE table over the real corpus: **n=9 rids, 841/853 rows excluded** (most gates don't emit OUTCOME brackets yet -- ship-gate.sh is the only live emitter -- so this is an honest partial answer, not a bug; the query itself is confirmed correct). Real data bug caught+fixed by the golden fixture: `kit_gates.start_ts`/`end_ts` are Unix epoch seconds not ISO8601 -- an early `TIMESTAMPTZ date_diff` draft silently returned NULL, fixed to integer `TRY_CAST...BIGINT` subtraction. `kit:code-reviewer` pass: 9/10, no CRITICAL/MAJOR, both findings addressed. Confirmed (again) `--repo-root`/`_repo_root()` is the real kit convention, ported into Python. No skill-distribution or tools-index precedent found in the kit (noted, not invented). CONDUCTOR NOTE: the worker did NOT add a `.github/workflows/test.yml` step for the new Python/uv-based tests (`git diff master...HEAD -- .github/workflows/test.yml` is empty) -- the goal file only required the existing suite green locally, so this isn't a contract miss, but it's a coverage gap (ledger-observatory's tests do not run in kit CI). Flagging for a possible follow-up (NOTES Proposed additions), not blocking this PR. Handoff for 05R: needs this PR's merged SHA + path `dwarves-kit/tools/ledger-observatory/`.

- 2026-07-05 SG-04 CI FIXED + MERGED (dwarves-kit PR #176, squash 2694457): the fix worker found the ROOT CAUSE was NOT mawk vs bwk-awk in `pb_rows` (that hypothesis was wrong; `pb_rows` itself works identically on both) -- it was GNU grep vs BSD grep in `tests/test-board.sh` itself: `grep -qE '^ID-001\tqueued\t'` relies on `\t` expanding to a real tab, which BSD grep (macOS) does and GNU grep 3.12 (Ubuntu CI) does not. Fixed by switching the test to bash ANSI-C quoting (`$'^ID-001\tqueued\t'`) so bash itself expands the tab before either grep sees it. Verified in a Linux sandbox (mawk+GNU-grep) before and after: 34/37 -> 37/37. Merged after re-check green.
- 2026-07-05 SG-04 SHIM APPLIED (conductor, ops-toolkit PR #711 merged 2622749c): pasted SG-04's exact shim content into `_meta/board`+`_meta/board-all` (68+114 lines -> 7+7 lines each, one-line `exec` against the kit `board.sh`). Re-verified byte-identical render myself across all 13 registered repos + both single-repo and cross-repo commands (13/13 IDENTICAL via `/usr/bin/cmp`) BEFORE committing (not just trusting the worker's own proof). Did the work in a hand-made worktree (`.claude/worktrees/board-shim`, `chore/board-tool-shims` -> main), not directly on main. Ship-gate flagged it `behavioral` (correctly -- this swaps the whole renderer); wrote a REAL proof-of-done (`docs/verification/board-tool-shims.md`) with a genuine negative control (corrupted the shim's flag name, confirmed RED, restored, confirmed GREEN) rather than reaching for the override escape hatch, since real verification was cheap here. Confirmed `~/.claude/dwarves-kit/lib/board.sh` resolves automatically via the pre-existing symlink (no extra install/sync step needed).
- 2026-07-05 SG-03K MERGED (dwarves-kit PR #178, squash 358373c): `orchestrate.sh queue` launcher built (tmux send-keys, real `/goal` sessions, NOT headless `claude -p`). 14/14 bats green (7 NCs incl. marker-wrap-false-positive, stalled-twice-stops-night). TWO live tmux smokes on real tmux+claude (throwaway mktemp repos only, no real checkout touched), both `done`; first smoke caught 2 real bugs (indented completion line, dropped early Enter) the stub mux suite couldn't. Rung-4 3-round red-team `VERDICT: SECURE`. Mid-flight: worker's own dispatched security+code review found 2 CRITICAL (marker false-trigger from the wrapped echo of the typed `/goal` prompt itself containing "RUNNER_DONE"; no defense-in-depth allow-list on `--from-boards` rows) + 1 HIGH (invented `cmux` verbs that don't actually exist per this repo's own SPEC-119/121 -- dropped cmux, tmux only) + 3 MEDIUM/LOW, all fixed before push. CONSUMER config: `TERMINAL_MUX`(tmux-only)/`MUX_CMD`/`QUEUE_MUX_SESSION`/`QUEUE_CLAUDE_CMD`/`QUEUE_CLAUDE_FLAGS`/`QUEUE_JOURNAL`/`QUEUE_POLL_SECS`/`QUEUE_TIMEOUT_SECS`/`QUEUE_RETRY_SLEEP_SECS`/`QUEUE_STARTUP_SECS`/`QUEUE_SUBMIT_SETTLE_SECS`/`QUEUE_BOARD_CMD`/`QUEUE_ALLOWED_POINTER_GLOB`. Journal: append-only tsv `ts/slug/verdict/reason`. Handoff for 06: mux+marker-poll logic at `lib/queue.sh:_launch_once` (~229-257) + `lib/queue.sh:_scan_marker` (~209-224); CONSUMER config block at lines 24-77; entry point `lib/orchestrate.sh` `main()`.
- 2026-07-05 SG-05R MERGED (ops PR #710, squash bfa4d0a): `tool.toml` status=moved + `moved_to` field, README banner prepended, MANIFEST row updated -- same shape as 03R. Grep sweep (~50 hits) found nothing asserting the old path as live/canonical going forward; one judgment call flagged (an active experiment `plannotator-gate` references the old path in working reproduce-commands, code still there so not broken, left alone with a note for later if the kit copy diverges). Worker again used the ship-gate override for this docs/metadata-only retire (same precedent as 03R).
- 2026-07-05 **WAVE 1 (+ its direct dependent 05R) FULLY CLOSED**: 03R/03K/04/05K/05R all merged; the board-shim paired change applied and merged. Remaining: 06 (ops, waits 03K MERGED -- now unblocked) + 07 (dwarves-kit, waits 04 MERGED -- now unblocked) can dispatch next; 08 stacks on 07; 09 waits on 03K+04+07+08 all merged.
- 2026-07-05 WAVE 2 DISPATCHED (parallel, hand-made worktrees): 06-deploy-runbook (ops-toolkit `.claude/worktrees/runner-air-deploy`, `feat/runner-air-deploy` base main, sonnet, GATE -- worker explicitly instructed never to merge) + 07-bridge-mirror (dwarves-kit `.claude/worktrees/board-mirror`, `feat/board-mirror` base master, sonnet, auto). Both instructed to open PRs (not merge, 06 doubly so), report back per the conductor-transcribes contract. Awaiting completion.
- 2026-07-05 SG-06 DONE, PR #712 OPEN + HELD FOR HAN (do not merge; ops-toolkit `feat/runner-air-deploy` -> main, final commit d3ebd976, 3 files +416): runbook at `_meta/megagoals/runner-fastpath/docs/RUNBOOK-air-queue-deploy.md`, proof at `.../docs/verification/06-deploy-runbook/proof-of-done.md`. REAL live smoke run on this machine (the Air): a throwaway mktemp fixture repo/pointer, launched via the actual kit queue launcher inside `caffeinate -dims` + tmux, journaled `done` (`2026-07-04T21:18:10Z sg06-air-smoke done`); confirmed zero writes to any real checkout. Corrected the goal file's placeholder invocation against the ACTUAL merged 03K/04 interfaces: no `bin/orchestrate.sh` (real path is `lib/orchestrate.sh` via the `~/.claude/dwarves-kit/lib` symlink), `queue` already implies `run` (no literal `run` arg), `--from-boards` needs `QUEUE_BOARD_CMD="bash .../lib/board.sh"` + `REPO_ROOT=<ops-toolkit path>` (confirmed AGAIN no `CONSUMER_ROOT` exists anywhere in the kit). TWO real deployment gotchas found live (now in the runbook): (1) `caffeinate` MUST wrap the long-running command INSIDE the tmux pane, never the outer `tmux new-session` call -- wrapping the client call releases the sleep-assertion almost instantly since `new-session -d` returns as soon as the detached session exists (verified experimentally: `caffeinate -dims sleep 8` inside a pane lived exactly 8s, outside it didn't); (2) this machine's pane shell resolves to fish (`UserShell`, not `$SHELL`), whose `2>&1` syntax differs -- first smoke attempt died silently for this reason, fixed by always wrapping the driven command in `bash -lc '...'`. Launchd Phase 2 trigger (a steady multi-week manual cadence) + Mini migration (`ssh mini-tieubao`, same steps) named in prose only, per scope. Deviation flagged by the worker (not silently decided): `lane-classify.sh` mis-tagged this a `full` lane (external-provider keyword false-positive on a docs+smoke change); worker kept ceremony proportionate per its own "design: obvious" instruction and relied on the proof-of-done doc instead of full gate-ledger recording -- Han may want to review this call. ROADMAP box left UNCHECKED per OPERATE.md's hard rule (checked box requires `merged <sha>`); PR # + HELD status recorded inline instead. Worktree removed (branch preserved locally + on remote for Han's review/merge).
- 2026-07-05 **GATES MERGED (Han-authorised) + SECURITY FIX SHIPPED + BRANCH CLEANUP**. Han lifted the 06/08 hold ("merge PRs") and directed a wrap-up. Merged: 06 (ops #712, squash 526a2bf -- rebased first, GitHub's stale CONFLICTING cleared to a clean rebase), 08 (dwarves-kit #181, squash ba5e1dd), and the convergence-gate security follow-up (dwarves-kit #182, squash f036168 -- see below). Integration-check on the merged dwarves-kit master: board-mirror 72/72, board 45/45, board-writeback 53/54 (1 live-hermes SKIP), meta all-pass, queue 14/14 -- 08's writeback + 182's content-trust compose cleanly. Branch cleanup: deleted 7 stale ops-toolkit local branches (gone-upstream + merged-into-main) + this session's merged remote branches in both repos (ops: chore/retire-*, chore/board-tool-shims, chore/boards-bridge-opt-in, chore/gitignore-*, docs/runner-fastpath-{checkpoint,run-report,backlog-flip,replan}; kit: feat/{board-mirror,board-tool,orchestrate-queue,observatory-to-kit}). Left untouched: open-PR branches (ops obs-av-02/03/04, cc-elevation-redeploy; kit docs/adr-execution-hygiene) and the ambiguous unmerged `feat/mega-runner-v0` (has a remote, not merged -- flagged, not deleted).
- 2026-07-05 SECURITY FIX (dwarves-kit #182, `fix/mirror-content-sanitize`, squash f036168) -- closes the convergence-gate security MAJOR (board mirror copied raw BACKLOG Item/Notes into Hermes card title/body/comment unsanitised). Fix: an untrusted-content MARKER on every agent-visible field (`MIRROR_UNTRUSTED_PREFIX` on bodies+comments, a compact `[untrusted]` title tag) + `_strip_routing_tags` removing every `#queue{...}` token on BOTH extract paths. TDD (test-first surfaced a real bug in my OWN test -- wrong TSV column indices; caught because I wrote it to fail first). A fresh-context `kit:security-reviewer` reviewed a body-only first cut, live-reproduced TWO uncovered paths (CHANGE-comment + megas-title) AND that my code comment overclaimed coverage; I closed all three (took the reviewer's title-marking argument over my narrower first scope), and the re-review confirmed all 3 findings CLOSED, VERDICT: SECURE, independently reproduced. Suite 72/72 (NC7 CREATE + NC7b megas + NC7c CHANGE), negative control 71/72, shellcheck clean. Proof: `dwarves-kit/docs/verification/mirror-content-sanitize/proof-of-done.md`; SPEC-147 gained a `## Content trust` section. OPERATIONAL CALL FOR HAN: every mirrored card title now reads `[untrusted] <name>` on the cockpit -- security-complete but noisier on his own board; shortening/dropping the title tag is a one-line follow-up if it annoys. The two MINOR findings (`--remote-kit-path` argv-safety + `--snapshot`-path collision) remain deferred (see Proposed additions).
- 2026-07-05 **MEGA STATE: 12 of 13 sub-goals shipped** (01,02,03[superseded],03R,03K,04,05[superseded],05K,05R,06,07,08). ONLY 09-kit-layout remains -- now UNBLOCKED (its 4 deps 03K/04/07/08 all merged) but NOT started, left for a future session per Han's "wrap up". Its scope: regroup dwarves-kit's ~32 flat `lib/` files into subsystem subdirs behind symlink shims, full suite as the gate. The mega's deployable-tools terminus + convergence-gate demo (RUN_REPORT.md rendered, first live Hermes mirror x2) are already DONE; 09 is a code-tidiness pass, not part of the terminus. Convergence-gate row ID-274/ID-275 already flipped to `executing` (ops #717).
- 2026-07-05 **SG-09 REHOMED, runner-fastpath CLOSES at 12/13.** Han directed folding the rest of cc-elevation into the kit too; the design pass (research/2026-07-05-cc-elevation-kit-foldin-design.md) showed SG-09's lib/ regroup and the cc-* fold-in both rewrite lib/, so the taxonomy is decided + executed ONCE in a new mega. Drafted `_meta/megagoals/kit-foldin/` (7 sub-goals; SG-09 becomes its SG-01). runner-fastpath ROADMAP SG-09 box set to `[~]` (rehomed, never `[x]` here); ID-274 flipped to `shipped`; new backlog row ID-276 for kit-foldin. Terminus (build+merge + convergence demo) was already DONE, so closing at 12/13 is correct , 09 was a tidiness pass, not part of the terminus. Mega folder co-location per megagoal-lifecycle-rule can happen whenever; the engine is quiescent.
