# NOTES , understanding-gate

## Active blockers

- [blocked] gate-zero: ADR-0031 (understanding gate) is Proposed (dwarves-kit PR #126), not yet Accepted. This mega-goal must not build until Han blesses it. · prerequisite: Han reviews + merges PR #126 with Status flipped to Accepted · last verified: 2026-07-03 (scaffold time)

## Proposed additions

- 2026-07-03: instrumentation , SG-04's quiz gate makes THIS mega-goal a candidate to gate its own final PR with a quiz on a future re-run; skipped during the build (circularity), but worth trying the first time the gate exists on a LATER mega-goal.
- 2026-07-03: the significance classifier (SG-02) shares the additive-ledger-marker convention with kit-face SG-03 (TOKENS) + SG-05 (efficacy lines); reuse that shape, do not invent a third.
- 2026-07-04 (TIER-4 advisor over-suggest): **wire `significance-classify record` at `/kit:ship`** , the fat classifier line (sig/wor/verdict) is currently un-wired, so the "silent wave, but LOGGED" half of the debt budget (ADR-0031 Refinement) is never written live; the weekend batch only ever sees loud (human-responded tap) items. Small change (verb + schema + marker already exist). DEFERRED from this run's seam fix on purpose , it is a design decision (when/where the classifier auto-records) Han should bless, exactly the kind of direction the understanding gate exists to gate. [Note: the seam fix `fix/debt-ledger-response-seam` makes the ledger forward-carry so that once `record` IS wired, sig/wor/verdict flow through the human-response line correctly.]
- 2026-07-04 (TIER-4 advisor over-suggest): a **`debt stats`/`budget` verb** (paid / waved / deferred / pending counts over a window) , gives the "cognitive-debt budget" metaphor a real dashboard and makes the classifier's over/under-firing (the ADR's load-bearing knob) measurable instead of vibes. `weekend-batch._disposition` already classifies every line.
- 2026-07-04 (TIER-4 advisor over-suggest): a **session-close nudge for orphaned taps** (`verdict=tap`, no `response`) at wrap-session , an un-answered tap is "pending" and invisible until the weekend batch runs; a one-line "N open understanding taps" catches dropped nudges (the exact "lost the plot" failure the ADR targets).
- 2026-07-04 (TIER-4 advisor over-suggest): **dogfood the gate on this mega-goal's own diff** on a future run (`lib/explain.sh render f1c47e1..HEAD` + the 5-Q quiz over the understanding-gate branch) , cheapest real-world calibration of whether the quiz is genuinely diff-grounded, and a fitting proof the axis survives contact with its own change.
- 2026-07-04 (TIER-4 test-coverage): SG-01's refuse-empty-Design NC is a DISCLOSED self-reference (it mutation-tests a bash reimplementation of Reviewer 6 living inside the test, not the live `spec-validate.md` prompt; the structural-wiring grep IS genuine). Accepted limitation (same class as SPEC-008 advisory reviewers). A future improvement: a golden-prompt check that the mirrored algorithm stays in sync with the shipped Reviewer 6 steps.

## Event log

2026-07-03 HH:MM · scaffold · understanding-gate mega-goal created from ADR-0031 + the Litt-article research note. 6 sub-goals, gh-stacked + auto-bottom-up + gated-final, cross-repo (dwarves-kit + dotfiles/ops-toolkit halves in 01 + 05). Gate-zero on ADR-0031 Accepted; sequenced after kit-face. Framing harvest SKIPPED (ADR-0031 pre-resolved the design; per-sub-goal /spec re-frames; 3 genuine forks surfaced in ROADMAP for async answer).
2026-07-04 · RUN COMPLETE (execution) · 6 sub-goals built + verified via subagent-delegate. Merged 5 to master (dwarves-kit #148 SPEC-123, #149 SPEC-122, #150 SPEC-124, #151 SPEC-125, #152 SPEC-126) + a TIER-4 seam fix (#154). SG-06 docs (#153 SPEC-127) HELD as the final PR for Han. TIER-4 close (5 in-harness lenses) surfaced one real cross-sub-goal bug (debt-ledger two-writer schema mismatch -> weekend-batch mark-paid exit-64 on the live path), reproduced live + fixed + re-verified; rest clean (integration-verifier PASS, security SECURE+1LOW-fixed, architecture 7/10, test-coverage 8/10). 6 advisor over-suggests + the record-at-ship design decision routed to `## Proposed additions` above. Full assembled CI (24 suites) green. Co-location deferred until Han merges #153 (foreign-repo owner -> `_meta/megagoals/_archive/understanding-gate/`). Full record: `RUN_REPORT.md`; TIER-4 detail: `.planning/TIER4-FINDINGS.md`.
2026-07-04 · FULLY CLOSED · Han approved wrap-up; SG-06 (#153) merged 13c95c2 , all 6 sub-goals + the seam fix now on dwarves-kit master. dwarves-kit worktrees + feat/ug-* + fix branches pruned (local + remote). Folder co-located to `_meta/megagoals/_archive/understanding-gate/` (foreign-repo owner). Dotfiles halves remain on their local feat branches for Han's reconciliation vs origin #193.
2026-07-03 HH:MM · refinement (operator: conscious-debt budget) · the gate does NOT quiz everything (Han's hands-off default is deliberate + valid). Folded into ADR-0031 (Refinement) + here: (1) SG-02 becomes TWO signals (significance × understanding-worthiness), taps only high×high, waves+logs the rest (anti-fatigue); (2) SG-04 becomes a NUDGE (engage/defer/wave, all logged to a DEBT LEDGER), never must-pass-to-merge; (3) impl-notes (`docs/implementation-notes/`) are the agent-side FEED of the ledger , this REVERSES the 2026-07-02 audit's "impl-notes are write-only, drop them" (ID-234): keep + wire for spec work; (4) SG-05 becomes the debt PAYDOWN (reads the ledger's deferred+waved items). Resolves open-forks 1 + 2; fork 3 (batch cadence) still open. Added an OSS-readiness close-out obligation (mostly kit-face's job). ID-234 needs reconciling , flag on the ops-toolkit board.
