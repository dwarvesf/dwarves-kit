Mega-goal: harness-loop. Work repo: ~/workspace/tieubao/dwarves-kit (kit-adopted; NEVER push master). Roadmap folder: _meta/megagoals/harness-loop/ , re-read ROADMAP.md + the current sub-goal's goals/NN-*.md at the start of EVERY turn; they are the source of truth, not memory.

Destination: the Specify -> Execute -> Observe -> Govern -> Learn loop CLOSES: run ledgers distill weekly into evidence-cited backlog proposals behind the unchanged human gate (board promote), a per-mega HTML dashboard becomes the sign-off surface, the surface consolidates per ADR-0034, and the front door (README, /kit:onboard, bin/config) tells the five-leg story. Quality bar: propose never dispose; every proposal cites lens + figure + rids or it does not stage; no new diaries (every artifact has a named reader or is a forcing function); one word one meaning (retro = per-run, propose = cross-run).

Rules:
- One PR per sub-goal, STACKED (gh): a dependent sub-goal branches off its PARENT sub-goal's branch and opens with --base <parent-branch>; independents branch off master, base master; fan-in sub-goals (2+ parents) base master after parents merge. Each goal file's PR-base field is authoritative. A local-only diff is an unstarted sub-goal.
- Record the PR # on the ROADMAP line the moment it exists; a checked box is `- [x] ... PR #N` or it is invalid; record the merge SHA when merged.
- Dependencies come from each goal file's literal `Depends on:` field. A `gate` sub-goal (01, 09) gates MERGING, not stacking: children MAY build + open PRs stacked on the gate's branch while Han reviews; NOTHING in that stack merges until the gate PR merges; a gate redline rebases the stack (accepted, Han 2026-07-12).
- Merge mode auto-bottom-up, autonomy gated-final: merge an `auto` sub-goal's PR yourself ONLY when all gates hold , its Done= verified by its own close-the-loop commands, `gh pr checks` all-green on the OPEN PR (never local tests), no CHANGES_REQUESTED, a committed proof-of-done with CAPTURED evidence (run-table/NC/screenshot/transcript, never assertion-only), tag=auto, AND its parent branch already merged (bottom-up; BEFORE merging+deleting a parent branch, retarget its child's base to master via `gh pr edit`, else GitHub auto-closes the child). NEVER merge a `gate` PR (01, 09) or the held final PR (10): open it, emit `🛑 NEEDS APPROVAL , sub-goal NN, STOP /goal MANUALLY`, stop.
- Kit-adopted SDD per sub-goal: classify the lane (bin/classify), run that lane's phases, record every gate via lib/gate/gate-ledger.sh BEFORE the push; the ship-gate is the Done check. Spec numbers are pre-reserved (ROADMAP Assumptions #2); do not renumber. SG-04 has a small dotfiles companion PR (chezmoi source; apply + stage + commit in ONE shell call).
- Never rewrite a Done= or outcome mid-loop; deviations go in that goal file's ## Notes. Discovered work -> NOTES.md ## Proposed additions. Blockers -> ## Active blockers as in-place fingerprints; retry only when the prerequisite changed.
- Handoff contract on each completion (per goal file): flip the box, overwrite HANDOFF.md (next sub-goal + concrete first action + file:line pointers), append durable invariants to DECISIONS.md, report IN the records, exit.
- Stop conditions: a gate PR awaiting Han; every remaining sub-goal blocked with unchanged prerequisites (final summary to NOTES ## Event log once, then short banners only); or SUCCESS = all boxes `[x]` with PRs merged-or-held per policy AND the close-out committed on SG-10's branch (RUN_REPORT.md + 2-3 freeze-PNG proofs of learn propose, the dashboard, config list).
- Before any success claim: extract every PR # from ROADMAP.md, `gh pr view <N> --json state,reviewDecision,statusCheckRollup` each; any checked box whose PR fails = unmark and keep working.
- Runner contract: on EVERY stop emit, alone on the FINAL line: `RUNNER_GATED: <sub-goal NN, reason>` when pausing (gate PR / held final / all-blocked), or `RUNNER_DONE` only on full success with close-out committed.
