Objective: turn the scattered ledgers (kit gate/proof/telemetry + learned-ledger + tide + tg-cleanup, plus planned debt & token) into ONE agent-callable, read-only observability surface , a `tools/ledger-observatory/` CLI (a DuckDB LENS over the files IN PLACE) + a render skill (terminal OR web Artifact) + a feedback loop that PROPOSES cockpit backlog rows. Files stay canonical. 5 sub-goals in `.../ops-toolkit/_meta/megagoals/ledger-observatory/`.

RUN MODE: DELEGATE (5 sub-goals). You are the THIN CONDUCTOR: do NOT run a sub-goal's /spec+/execute+review INLINE (inlining fills THIS context , the 873k ceiling this observatory answers). For each unchecked sub-goal make ONE Bash call to a fresh headless `claude -p` (plain `-p`, never `--stream`/`--verbose`) that runs the full /spec->/spec-validate->/kit:execute->PR lifecycle in ITS OWN context and prints ONLY a terse line (box flipped, PR #N, proof path). Absorb it, verify the box on disk, auto-bottom-up merge, continue; delegate TIER-4 likewise.

PRECONDITION (turn 1). cwd IS `~/workspace/<owner>/ops-toolkit` (kit-adopted , read AGENTS.md + WORKFLOW FIRST). Pre-flight `which gh duckdb uv`. Read ROADMAP.md (`## Assumptions` BINDING, `## Open forks` /spec-resolved), NOTES.md, every `goals/NN-*.md`. No gate-zero: the Addendum IS the decision. Phase 1 runs over EXISTING ledgers; debt/token CONFORM on arrival (SG-01 confirms, does not wait).

SOURCE OF TRUTH. ROADMAP.md = done + which PRs belong; each goals/NN is an immutable contract; the Addendum (ROADMAP Provenance) is intent. HARD CONSTRAINTS: DuckDB is a read-only LENS (files canonical, delete-and-rematerialize); the tool never mutates a ledger; REUSE lane-telemetry for the kit read; the feedback loop PROPOSES rows, never auto-files.

MERGE POSTURE (gh stacked + auto-bottom-up + gated-final). Stack: 01 off `main`, each next bases the prior, 05 LAST. One PR per sub-goal to `tieubao/ops-toolkit`. Merge bottom-up once a sub-goal's gates pass (Done= + gate-ledger PER-GATE + green CI + proof with CAPTURED evidence + NCs green); retarget children before deleting a parent. The FINAL PR (05) is HELD for Han.

PER SUB-GOAL (deps: 02<-01; 03<-02; 04<-02,03; 05<-ALL). In each `claude -p`: `bash lib/lane-classify.sh classify "<sub-goal>"`, run that lane; `/spec` + `/spec-validate` BEFORE code; `/kit:execute` (V-model fires). OVER-TEST 02 + 04 via `/kit:test-plan` with a COVERAGE-DELTA proof row (02: cross-format read + cross-ledger JOIN + delete-and-rematerialize; 04: threshold correctness + false-positive NC). Each named NC (in goals/NN Proof) is load-bearing; a proof missing its NC fails. Record gates via `bash lib/gate-ledger.sh record` PER-GATE. Branch `feat/lo-NN-<slug>`. ROADMAP line -> `PR #N` on open; `[x] ... merged <sha>` on merge.

TIER-4 CLOSE (after 01-04 merge, before 05/final): integration-verifier against the objective + a NO-ORPHAN CHECK over every artifact (schema doc, `ledger` CLI, render skill, feedback path) , defined-but-never-dispatched = blocking (a skill that never fires on its triggers, a CLI never invoked, a work-intake path never fed; kit-hardening c6fbd99 class); `/kit:review-team` with the security-reviewer lens; advisor in BOTH modes (over-suggest -> NOTES `## Proposed additions`). Fix, re-verify, then hold the final PR.

HARD RULES. A checked box is `[x] ... PR #N ... merged <sha>` or unchecked; "CI green" = the OPEN PR's checks. Never rewrite Done=/Outcome (append `## Notes`); no new sub-goals mid-loop (-> NOTES Proposed additions). First stop appends the summary to NOTES `## Event log`; later stops emit only `LOOP BLOCKED , STOP /goal MANUALLY`. Audit every ROADMAP PR # before claiming success (ROADMAP's audit cheat sheet).

STOP. Success = 01-05 merged (05 held as final), TIER-4 clean, final PR HELD for Han. Else stop on a genuine blocker (unchanged prerequisite) or token out.

CLOSE-OUT. Draft an ops-toolkit `_meta/LAB_LOG.md` entry (slug, 5 sub-goals, PR range, lessons) + a NOTES line; mark complete.
