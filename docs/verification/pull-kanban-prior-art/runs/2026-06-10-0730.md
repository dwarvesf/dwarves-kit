# Run: pull-kanban prior-art sweep (2026-06-10 07:30)

Immutable record of the survey execution (research dialect, test-design.md's matrix).

## Board mechanics exercised (the dogfood half)

- Command: `bash lib/backlog.sh next`
- Output: `ID-047`  (first queued row; enqueued top-of-file = top priority)
- Command: `bash lib/task-type-classify.sh classify "survey prior art for pull-based agent kanban and dynamic persona dispatch, cited report"`
- Output: `research`
- Command: `bash lib/proof-gate.sh contract "..."`
- Output: `type=research class=behavioral / proof: cited report + verified sources / owner: deep-research`
- Command: `bash lib/goal-registry.sh claim ID-047-pull-kanban-prior-art normal "_meta/BACKLOG.md" "docs/research/*" "docs/verification/pull-kanban-prior-art/*"`
- Output: `CLAIMED ID-047-pull-kanban-prior-art (lane=normal)`; `list` shows it running on feat/north-star-05-dogfood
- Command: `bash lib/backlog.sh set ID-047 claimed "pulled via assign --next flow, claim ID-047-pull-kanban-prior-art"`
- Exit: 0 (`ID-047 -> claimed`)

## Claim verification results

(appended as the sweep returns; every C# maps to test-design.md)

- C3 (GSD v2 standalone runtime): local half CONFIRMED, PHILOSOPHY §1 "GSD v2 disambiguation" names github.com/gsd-build/gsd-2, npm gsd-pi, TypeScript runtime on the Pi SDK, positioned as external engine to integrate, never rebuild. Web half: see C3-web below.
- C4 (Claude Code native task/queue surfaces): CONFIRMED by direct in-session probe, the harness tool registry exposes TaskCreate / TaskList / TaskUpdate / TaskStop / TaskGet / Monitor plus CronCreate / CronList / RemoteTrigger; an agent can create, list, and poll tasks natively. (Local fact of the running harness, not a doc claim.)
- C5 (goal-registry = cross-session claim guard): CONFIRMED live this run, the claim command above succeeded and `list` rendered the running goal; lib/goal-registry.sh header documents the disjointness moat (ADR-0022, SPEC-036).

- C1 (OpenClaw Workboard): CONFIRMED by local-probe persona. Control UI Kanban, cards in a
  gateway-local SQLite db under the OpenClaw state dir (SPEC-082; control-ui-operator-guide).
  BIDIRECTIONAL: agents pull via `workboard_claim` and push via
  `complete/block/heartbeat/decompose/dispatch` tools; humans post cards via UI. Key nuance for
  our next phase: the pull verb exists as an AGENT TOOL, not an operator command.
- C2 (OpenClaw persona catalogs): CONFIRMED by local-probe persona. 13 persona .md files under
  deploy/config/personas/ (header: `persona:` name + `use-when:` + `difficulty:` +
  `task-types:`; body = role prose). Selection is DYNAMIC IN-PROMPT at dispatch: the lead reads
  the catalog (assembled into its AGENTS.md at deploy time by apply-agents.sh), matches
  `use-when` against the task, and PREPENDS the chosen persona body into the sessions_spawn
  task string (SPEC-080 DEC-001: inlined, not agentId-attached, due to OpenClaw #50263).
  Nuance: personas are templates, not agents; selection is model judgment, not a lookup table.

- C3-web (GSD v2 runtime): CONFIRMED with corrections by web persona. Active home is
  github.com/open-gsd/gsd-pi (gsd-build/gsd-2 redirects); TypeScript app embedding the Pi
  coding agent; crash recovery first-class (crash-recovery.ts, auto-recovery.ts, "no in-memory
  state survives"); git automation is BRANCHLESS worktrees + auto-commit/merge (ADR-001), not
  branch creation. CORRECTION: task mechanics are a push-style dispatch loop deriving "next
  unit" from a milestone -> slice -> task dependency graph, NOT a pull board; "wave" is
  migration phasing, not runtime. npmjs.com page 403'd; npm registry JSON API substituted
  ([UNAVAILABLE noted], repo docs are the mechanics source).
- C6 (persona selection first-class at dispatch): CONFIRMED by web persona. Microsoft AutoGen
  AgentChat `SelectorGroupChat`: a model selects the next speaker PER TURN from the agents'
  name+description (persona as dispatch input), `selector_func` overrides with deterministic
  routing, `allow_repeated_speaker` tunes. Source:
  microsoft.github.io/autogen/stable/user-guide/agentchat-user-guide/selector-group-chat.html.
  (CrewAI's Agent(role=...) is the static counterpart; AutoGen chosen as the cleaner dynamic
  citation.)

## Verdict

All 6 matrix rows landed: 5 confirmed, 1 confirmed-with-corrections (C3). Hypothesis holds
(>= 3 prior arts with reusable mechanics). The falsifiability control worked as designed: C6
was refutable-by-absence and instead produced a concrete citation; C3's corrections prove the
matrix gates content (the report must NOT call GSD pull-based, which memory alone would have).

## Gate markers (literal, for proof-ledger)

- Command: `bash lib/backlog.sh next` -> ID-047; full lifecycle commands above.
- Exit: 0 (every board/claim command)
- NEGATIVE CONTROL: C6 was designed refutable-by-absence (no citation -> dropped from
  conclusions); C3's web probe REFUTED two memory-level assumptions (pull-board, branching),
  forcing corrections, the matrix went RED on memory and green only on evidence. Plus the
  board-machinery negative controls recorded in SPEC-055's build (assign --next bullet
  dropped -> pin RED).
- VERDICT: PASS (6/6 matrix rows verified; hypothesis holds; rollback: `git revert`, docs-only)
