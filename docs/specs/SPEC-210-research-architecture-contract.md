# Spec: agents/research-architecture.md contract (backfill)
Generated: 2026-07-31
Status: DRAFT (reverse-engineered)

Backfill item 3/6 of the ID-452 campaign. The feature is a subagent definition; its observable behavior IS its prompt body plus its frontmatter tool grant, and its dispatch contract lives in `commands/spec.md` Step 2. The acceptance criteria pin both sides as they exist today. Process deviation, stated honestly: the `/kit:test-plan` Step 0 research-agent dispatch was skipped, per the item-1/2 (SPEC-208/209) precedent; the surface is two small files, read directly. Unlike items 1-2, the target file is NOT hard-wrapped (single logical lines), so pins are line-stable. Two side findings, recorded not fixed: (a) the body's output contract says to write `docs/research/architecture.md` but the frontmatter grants no Write tool, so under a strictly enforced grant the agent cannot produce that file; (b) the Mode B inline fallback prompt asks only 4 of the agent's 6 questions (it omits shared utilities and config pattern), a Mode A/B content divergence. This spec pins the current text on both sides of each conflict and flags both for their own ID-452 rows.

## Acceptance Criteria
- [ ] AC-1: Identity: frontmatter names the agent `research-architecture`, the description carries both clauses (the auto-delegation trigger `Maps architecture patterns and conventions in an existing codebase.` and the dispatch trigger `Dispatched by /spec for brownfield projects. Read-only.`), the model tier is `sonnet` (cheap-worker default), and the body carries the single-job mission statement.
- [ ] AC-2: Read-only tool grant: the frontmatter `tools` roster is exactly Read, Grep, Glob, `Bash(find *)`, `Bash(git log *)`; no write-capable tool (Write/Edit/NotebookEdit) appears in the grant.
- [ ] AC-3: Dispatch contract, pinned from both sides: `commands/spec.md` Step 2 is brownfield-gated, dispatches all 4 research agents in parallel via the Task tool, its Mode A research-architecture dispatch line names the same write target the agent body names, and the Mode B inline fallback prompt is pinned verbatim (including that same target).
- [ ] AC-4: Research scope: the body carries the six numbered question labels (directory structure, error handling pattern, naming conventions, how similar features were built, shared utilities, config pattern) and the full conditional codebase-memory-mcp routing sentence.
- [ ] AC-5: Output contract: the body targets `docs/research/architecture.md` and its template carries the six section headings (Directory structure, Error handling, Naming conventions, How recent features were built, Shared utilities to reuse, Config pattern).
- [ ] AC-6: Rules: 60-line output budget with patterns-not-exhaustive-listings; concrete examples (actual paths, actual signatures) over abstract descriptions; an inconsistent codebase is reported as useful information, not papered over.
- [ ] AC-7: Distilled return (SPEC-087 Mechanism C): the return contract section exists, requires a bounded summary of exactly four fields (verdict, key findings, artifacts, read-next), never a dump, forbids re-pasting diffs/test logs/whole files, and states the hundreds-not-tens-of-thousands token bound.

## Test plan
Date: 2026-07-31 (revised once after the round-1 critique)
Source: this spec's ## Acceptance Criteria. Dialect: doc/prompt-file contract (the standard's doc dialect: pin the claims with grep assertions, prove falsifiability with a negative control). All proofs run from the repo root; `A=agents/research-architecture.md`, `C=commands/spec.md`. Proof cells escape a literal shell pipe as `\|` (markdown table convention, per SPEC-204); row 6's regex alternation pipes are escaped the same way for the table, and the materialized script uses bare `|` in both roles. Rows 1-23 are deterministic reads (grep/awk on the two tracked files, or on a mktemp scratch copy that is never a tracked file): no network, no clock, no ordering. Row 24 is the permanent in-suite negative control; row 25 is the one deliberate exception, a one-time recorded mutation with a byte-identity restore. Every pinned string was occurrence-counted 1 in its file at authoring time (the dispatch target is deliberately counted 2 within `$C`'s Step 2 region: once per mode).

| # | Case | Category | Covers (AC) | Expected | Proof |
|---|------|----------|-------------|----------|-------|
| 1 | frontmatter name pinned | happy-path | AC-1 | first frontmatter block contains `name: research-architecture` | `awk '/^---$/{c++;next} c==1' $A \| grep -qx 'name: research-architecture'` |
| 2 | description, both clauses | happy-path | AC-1 | the auto-delegation clause AND the dispatch-trigger clause | `grep -qF 'Maps architecture patterns and conventions in an existing codebase.' $A && grep -qF 'Dispatched by /spec for brownfield projects. Read-only.' $A` |
| 3 | model tier pinned | happy-path | AC-1 | `model: sonnet` in the frontmatter (cheap-worker tier; `tests/test-meta.sh` validates the alias set, this row pins the CHOICE) | `awk '/^---$/{c++;next} c==1' $A \| grep -qx 'model: sonnet'` |
| 4 | single-job mission | happy-path | AC-1 | `Your single job: map the architecture patterns so new code follows existing conventions.` (presence, not position; accepted) | `grep -qF 'Your single job: map the architecture patterns so new code follows existing conventions.' $A` |
| 5 | exact tool roster | security/safety | AC-2 | frontmatter tool list is byte-for-byte the 5 read-only entries, in order (an addition, removal, or reorder breaks the row deliberately) | `diff <(awk '/^---$/{c++;next} c==1' $A \| grep '^  - ') <(printf '  - Read\n  - Grep\n  - Glob\n  - Bash(find *)\n  - Bash(git log *)\n')` |
| 6 | no write-capable grant | security/safety | AC-2 | no Write/Edit/NotebookEdit line anywhere in the frontmatter (redundant with row 5's exhaustive roster by design: the pair survives a partial edit to either) | `! awk '/^---$/{c++;next} c==1' $A \| grep -qE '^[[:space:]]*-[[:space:]]*(Write\|Edit\|NotebookEdit)'` |
| 7 | brownfield gate on dispatcher | boundary | AC-3 | `### Step 2: Research (if brownfield)` heading in `$C` | `grep -qF '### Step 2: Research (if brownfield)' $C` |
| 8 | Mode A dispatch line | happy-path | AC-3 | the dispatch sentence names this agent and the exact write target (pinned without the list numeral, renumber-tolerant) | `` grep -qF '**research-architecture** agent: "Map architecture patterns and conventions. Write to `docs/research/architecture.md`."' $C `` |
| 9 | parallel 4-agent dispatch | happy-path | AC-3 | `dispatch all 4 via the Task tool in parallel` | `grep -qF 'dispatch all 4 via the Task tool in parallel' $C` |
| 10 | Mode B fallback prompt | boundary | AC-3 | the full inline architecture-research prompt line, verbatim (pins today's 4-of-6 divergence as-is; see side finding b) | `grep -qF 'Map architecture patterns. Find: directory structure conventions, error handling patterns, naming conventions, how the 2-3 most recent features were built (check git log). Show concrete examples. Max 60 lines. Write to docs/research/architecture.md.' $C` |
| 11 | write target agrees across files + modes | boundary | AC-3, AC-5 | `docs/research/architecture.md` appears exactly twice within `$C`'s Step 2 region (Mode A + Mode B; region-anchored so unrelated future mentions elsewhere in `$C` cannot flip it) and in `$A`'s output contract | `` test "$(sed -n '/^### Step 2: Research/,/^### Step 3/p' $C \| grep -cF 'docs/research/architecture.md')" -eq 2 && grep -qF 'Write to `docs/research/architecture.md`:' $A `` |
| 12 | research questions 1-3 | happy-path | AC-4 | bolded question labels: Directory structure, Error handling pattern, Naming conventions | `grep -qF '**Directory structure**' $A && grep -qF '**Error handling pattern**' $A && grep -qF '**Naming conventions**' $A` |
| 13 | research questions 4-6 | happy-path | AC-4 | bolded question labels: How similar features were built, Shared utilities, Config pattern | `grep -qF '**How similar features were built**' $A && grep -qF '**Shared utilities**' $A && grep -qF '**Config pattern**' $A` |
| 14 | codebase-memory conditional | boundary | AC-4 | the FULL routing sentence as one pin (guard + both calls co-located; fragment pins could go green with the guard decoupled from the calls) | `` grep -qF 'If codebase-memory-mcp is available, use `get_structure()` for directory layout and `search_symbols()` for patterns.' $A `` |
| 15 | output template sections | happy-path | AC-5 | all six `##` headings of the output template present | `grep -qF '## Directory structure' $A && grep -qF '## Error handling' $A && grep -qF '## Naming conventions' $A && grep -qF '## How recent features were built' $A && grep -qF '## Shared utilities to reuse' $A && grep -qF '## Config pattern' $A` |
| 16 | 60-line output budget | boundary | AC-6 | `Max 60 lines.` (first sentence of the budget rule; shares its physical line with row 17's pin, see NC note) | `grep -qF 'Max 60 lines.' $A` |
| 17 | patterns-not-listings rule | boundary | AC-6 | `Patterns, not exhaustive listings.` (second sentence, same physical line as row 16; the NC target) | `grep -qF 'Patterns, not exhaustive listings.' $A` |
| 18 | concrete-examples rule | happy-path | AC-6 | full sentence: concrete file paths + function signatures over abstract descriptions | `grep -qF 'Show concrete examples (actual file paths, actual function signatures) not abstract descriptions.' $A` |
| 19 | no-consistent-pattern honesty | boundary | AC-6 | the FULL sentence as one pin (say-so + useful-information co-located) | `grep -qF "If the codebase has no consistent pattern (different features use different approaches), say so. That's useful information." $A` |
| 20 | return-contract heading | happy-path | AC-7 | `## Return contract (distilled return, SPEC-087 Mechanism C)` | `grep -qF '## Return contract (distilled return, SPEC-087 Mechanism C)' $A` |
| 21 | four bounded return fields | happy-path | AC-7 | all four bolded field names present | `grep -qF '**verdict**' $A && grep -qF '**key findings**' $A && grep -qF '**artifacts**' $A && grep -qF '**read-next**' $A` |
| 22 | bounded summary, no re-paste, token bound | security/safety | AC-7 | `BOUNDED summary, not a dump`, the no-re-paste rule, and the hundreds-vs-tens-of-thousands bound | `grep -qF 'BOUNDED summary, not a dump' $A && grep -qF 'not as a re-paste of diffs, full test logs, or whole files' $A && grep -qF 'hundreds of tokens per dispatch instead of tens of thousands' $A` |
| 23 | read-only claim consistency | regression | AC-1, AC-2 | the word `Read-only.` in the description AND the row-6 negative both hold together (the pair that would catch a future grant widening that forgets the description, or vice versa) | `grep -qF 'Read-only.' $A && ! awk '/^---$/{c++;next} c==1' $A \| grep -qE '^[[:space:]]*-[[:space:]]*(Write\|Edit\|NotebookEdit)'` |
| 24 | in-suite negative control (permanent, scratch copy) | falsifiability | AC-6 | on a mktemp COPY of `$A` with ` Patterns, not exhaustive listings.` stripped (substring removal, tracked file untouched), the NC-SETUP GUARD `! cmp -s $A copy` proves the strip actually changed the copy (this guard, not the absence check, is the "in-suite NC setup assertion" that goes RED in row 25: sed is a silent no-op when the substring is already gone), then row 17's pin FAILS while two named survivors still PASS on the copy: row 16's `Max 60 lines.` (same physical line, discriminates substring-strip from whole-line delete) and row 18's sentence (next physical line, discriminates a line-scoped over-strip); EXIT-trap cleanup | inside the test script: mktemp scratch copy + sed substring strip + `! cmp -s` setup guard + `! grep -qF` on the stripped sentence + `grep -qF` on the two survivors |
| 25 | live negative control (one-time, recorded) | falsifiability | AC-6 | strip ONLY ` Patterns, not exhaustive listings.` from the tracked `$A` via `sed -i.bak` (BSD/macOS-safe, per the item-1/2 recipe; substring removal, never the whole line: row 16's pin shares the physical line), run `bash tests/test-research-arch-contract.sh`, expect EXACTLY this flip set: row 17 RED plus the in-suite NC setup assertion RED (nothing left to strip on the copy it derives, a named, predicted side effect); survivors that make the prediction meaningful: row 16 (same physical line) and row 18 (adjacent physical line) stay GREEN, and every `$C` row stays GREEN (untouched file); restore from the `.bak`, prove byte-identity via truncated sha256 + re-derive command, re-run green | recorded with both transcripts in `docs/verification/backfill-research-arch.md` |

### Coverage notes
- Every AC maps to at least one row and every row back to an AC; no orphans. Failure-injection is honestly thin: the subject is a static prompt file, so the only injectable failure is content drift, which IS rows 24-25 plus the regression row 23.
- Agent frontmatter structure (name/description present, model alias valid) and the MANUAL.md inventory cross-ref are NOT re-pinned here; `tests/test-meta.sh` owns those structural sweeps. Row 3 pins the model CHOICE, which test-meta deliberately does not.
- Exact-string pins are by design: the tested contract is the prose itself; a legitimate rewording is EXPECTED to break the matching row and prompt a re-verify (same stance as SPEC-208/209). The body is not hard-wrapped, so no pin spans a wrap boundary today; if either file is ever re-wrapped, the longest pins (rows 4, 8, 10, 14, 18, 19) break first. Snapshot, not exhaustive.
- Adjacency hazard, named per the SPEC-208 precedent: rows 16 and 17 pin two sentences of one physical line (`- Max 60 lines. Patterns, not exhaustive listings.`). Any future line-deletion-style negative control on AC-6 must strip a substring, never the whole line, or it flips both rows. Row 10's Mode B pin also contains `Max 60 lines.` inside `$C`; rows 16-17 grep `$A` only, so the NC blast radius does not reach it.
- The Write-tool conflict (side finding a) is deliberately NOT asserted either way: row 5 pins the grant and row 11 pins the instruction, so whichever side a future fix changes, exactly one row breaks and surfaces this spec. Same stance for the Mode B 4-of-6 divergence (side finding b): row 10 pins today's text verbatim; closing the divergence breaks row 10 and surfaces this note.
- Deliberately unpinned, accepted LOW: the output template's bracketed placeholders and tree-output instruction; the Mode A/B selection gate (`If the research agents are installed`, shared across all 4 siblings, deferred to whichever ID-452 item owns cross-cutting Step-2 text); row 4's position ("opens with") is proven by presence only.

## Test plan critique
Date: 2026-07-31
Spec: SPEC-210
Lenses run: Coverage completeness, Oracle & falsifiability, Determinism & maintainability (3 of the standard 6, dispatched as parallel read-only subagents). Skipped, honestly: Feasibility & reproducibility (every proof is a pasteable one-liner), Test-ladder & boundary depth (the ladder collapses for a static-file subject), Tiering & floor (N/A: no Tier column, no model call in scope). Same ID-452 calibrated-cost triage as SPEC-208/209, stated rather than hidden.

Rounds:
- [[QL-VERDICT round=1 clean=false findings=14]]
- [[QL-VERDICT round=2 clean=false findings=4]]

Round 1 (full 3-lens dispatch) returned 14 deduplicated findings: 1 CRITICAL, 4 HIGH, 5 MEDIUM, 4 LOW. One revision was applied (the matrix above). Round 2 was NOT a fresh lens dispatch: it was a narrow mechanical re-check by the coordinator, every new or changed pin run live against the real files with occurrence counts (all 1; the region-anchored target count exactly 2). K fell 14 to 4 and max severity fell CRITICAL to LOW; the 4 remaining are accepted LOW trade-offs, so the loop stops.

### Critical findings
1. Rows 8 and 11 (draft rows 8/10) escaped inline backticks as `` \` `` inside the proof cells; verified live, the literal-backslash form false-FAILs against a clean tree, and pandoc corrupts the rendered cell (determinism lens) -- fix: bare backticks restored, cells re-fenced with double backticks -- resolved in round 1.

### High findings
1. The description's primary clause (`Maps architecture patterns and conventions in an existing codebase.`), the string auto-delegation actually reads, was entirely unpinned (coverage lens) -- fix: AC-1 extended, row 2 now pins both clauses -- resolved in round 1.
2. Mode B's inline fallback prompt content was untested beyond a target-count, and it diverges from Mode A (asks 4 of the 6 questions) with no row catching it (coverage lens) -- fix: row 10 pins the full Mode B line verbatim; the divergence is recorded as side finding b, not silently normalized -- resolved in round 1.
3. The `\|` escaping convention was used but undocumented, unlike SPEC-208's preamble, and row 6 overloads it for regex alternation (determinism lens; the oracle lens filed the same gap as LOW, merged) -- fix: preamble callout added naming both roles -- resolved in round 1.
4. Row 25's live mutation named no sed recipe, and the natural GNU-style `sed -i` fails outright on BSD/macOS sed (determinism lens) -- fix: row 25 names `sed -i.bak` per the item-1/2 recipe -- resolved in round 1.

### Medium findings
1. The draft's cross-file target count was whole-file, so any unrelated future mention of the path in `$C` would false-RED it (oracle lens) -- fix: row 11 anchors the count to the Step 2 region -- resolved in round 1.
2. Row 4 was cited as an NC survivor but its survival was vacuous (different region, shares no strippable substring), the same class SPEC-209's critique caught (oracle lens) -- fix: dropped from rows 24-25's survivor lists; survivors are now only the same-line and adjacent-line pins -- resolved in round 1.
3. Draft rows 13/18 bundled unanchored fragments with no co-location guarantee (the guard could decouple from the calls and stay green) (oracle lens) -- fix: rows 14 and 19 pin the full sentences as single strings -- resolved in round 1.
4. The return contract's no-re-paste rule was unpinned (coverage lens) -- fix: AC-7 extended, row 22 gains the fragment -- resolved in round 1.
5. AC-4 claimed the six questions' content while the rows only pin their labels (coverage lens) -- fix: AC-4 narrowed to "question labels" -- resolved in round 1.

### Low findings
1. Row 4 proves presence, not the "opens with" position -- accepted, noted in the row and coverage notes -- OPEN as accepted.
2. The Mode A/B selection gate is unpinned -- accepted, shared across all 4 sibling agents, deferred; documented in coverage notes -- OPEN as accepted.
3. Row 6's alternation is not end-anchored (a hypothetical `WriteFile` would false-positive) -- accepted, row 5's exhaustive roster closes the gap; the intentional redundancy is noted in row 6 -- OPEN as accepted.
4. Rows 24-25's artifacts did not exist at critique time -- true by pipeline position (critique precedes materialization in SPEC-203); resolved at the materialization + verification steps.

### Scores (final round)
- Coverage completeness: 9/10
- Oracle & falsifiability: 9/10
- Determinism & maintainability: 9/10

### Verdict: SOLID
