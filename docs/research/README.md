# docs/research

Dated deep-scans that fed specific specs. Name every new file
`YYYY-MM-DD-<short-slug>.md`; pick the date the research actually happened
(git first-commit date if nothing better).

**Exception:** `2026-06-11-id081-flake-capture.txt` is a raw terminal
capture, not prose, kept as `.txt` on purpose.
