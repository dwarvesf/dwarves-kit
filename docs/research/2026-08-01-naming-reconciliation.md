# Naming reconciliation scan (2026-08-01)

One-shot audit-loop run over the kit's naming estate (SPEC-221 half A). Item set: every feature name + taxonomy vocabulary across `docs/FEATURES.md`, `agents/`, `skills/`, `commands/`, `docs/architecture.md`, `docs/WORKFLOW.md`, `docs/glossary.md`, `docs/verification/task-types.md`. Contract: names follow consistent conventions, measured against ADR-0029 (review-function naming axis), ADR-0034 (loop taxonomy, leg->stage), and the plain-words inventory. Evidence gathered by two read-only Tier-2 scanner passes (the `kit:audit-scanner` roster was unavailable in the loaded plugin snapshot, so the SPEC-220 documented read-only fallback ran).

Verdict discipline (operator-fixed): renames are report-only PROPOSALS with grep blast-radius counts; only zero-break fixes were applied (doc prose, glossary rows, description wording). Blast radius = files/occurrences of the exact token across `*.md` + `*.sh`, `.git` excluded.

## Findings

| # | Finding | Evidence | Verdict |
|---|---|---|---|
| 1 | The two big suffix families hold: all 7 `-reviewer`s are read-only static lenses; 6 of 7 `-verifier`s execute the artifact. `advisor` and `agent-effectiveness` are documented exceptions, not drift. | ADR-0029:29-32 defines the axis; `tests/test-meta.sh:2812` pins "NOT a naming violation -- do not \"fix\" its name to *-reviewer" | OK |
| 2 | `claim-verifier` is a static refutation panel wearing the dynamic `-verifier` suffix: it executes nothing. | `agents/claim-verifier.md:3` "Runs an in-harness panel of N independent skeptics ... Read-only." vs ADR-0029:30 "`-verifier` ... EXECUTES the artifact and observes" | PROPOSAL: `claim-reviewer` or a `-team` shape. Blast radius 23 files / 69 occ |
| 3 | `/kit:spec-validate` is the unmigrated ADR-0029 residue: a panel command (the `-team` class) wearing the explicitly retired `-validate` suffix. The ADR's own problem table names it; the rename map only covered `agents/*.md`. | ADR-0029:34 "Retire `-checker`, `-validate`-as-a-suffix, `-auditor` ..."; `commands/spec-validate.md:2` "6 specialist lenses attack the spec" | PROPOSAL: `/kit:spec-team`. Blast radius: `kit:spec-validate` 79 files / 169 occ; bare token 252 files / 599 occ. Highest-value single rename, and the most expensive |
| 4 | `-scanner` is a suffix family of one, born post-ADR-0029 with no amendment; by behavior (returns judged findings in the verdict grammar) it sits in the static-review class. | `agents/audit-scanner.md:3`; SPEC-220 has zero mentions of ADR-0029/naming/suffix | PROPOSAL: amend ADR-0029 to sanction `-scanner` as the evidence-gathering non-fixing class (cheaper), or rename to `audit-reviewer`. Blast radius 16 files / 69 occ |
| 5 | The write-capable implementer class is split across three suffixes: `-worker` (data-etl, db-migration), `-writer` (test-writer), `-agent` (fix-agent). And `-agent` means three different things: type (`fix-agent`), meta (`meta-agent`), grammatical object (`/kit:draft-agent` = draft AN agent). | `docs/architecture.md:115,164`; `agents/meta-agent.md:3` "The agent that drafts agents." vs `commands/draft-agent.md` | PROPOSAL: converge implementers on `-worker` (`fix-agent`->`fix-worker`, 60/215) so `-agent` means only meta; or leave and record the three grains in ADR-0029. `test-writer` 20/94, `meta-agent` 96/339, `draft-agent` 50/130 |
| 6 | Naming enforcement has a coverage gap that produced findings 2-4: the retired-suffix ban walks `agents/` only, and the positive-axis check runs over a hardcoded 11-name list, so commands and every agent added after the list froze are unpoliced. | `tests/test-meta.sh:2800` `for AGENT_FILE in "$KIT_DIR/agents/"*.md`; `:2818` REVIEW_AGENTS list lacks api/frontend/infra/performance-reviewer, audit-scanner, claim-verifier | PROPOSAL (test change, not a rename): roster-scan instead of the hardcoded list. 1 file / 2 sites; a smaller diff than any rename and prevents the next drift |
| 7 | `responding-to-review` is the estate's only gerund-phrase name; `research-*` is the only prefix family. Both were consciously fenced out of ADR-0029 scope. | ADR-0029:89 (Out of Scope) names both | UNSURE: real grain breaks, but reopening a settled scope is an operator decision, not an audit finding. `responding-to-review` 25/49; `research-*` 15-19 files each |
| 8 | Command grain is two coherent grains, not drift: bare-verb actions (`spec`, `review`, `ship`, `wayfind`, `dispatch`, `prototype`, `absorb`, `grill`) and noun/panel names (`kit-health`, `test-plan`, `ui-design`, the four `-team`s). The genuine breaks: `spec-validate`/`test-write` (noun-verb) vs `draft-agent` (verb-noun), plus `mega` (bare adjective) and `next` (bare adverb). | `docs/decisions/0034:128` permits verb-first for human-typed commands | PROPOSAL (low value): align `test-write` (16/60) and `draft-agent` (21/43) to one grain. `mega` (57/126) and `next` (35/72) are entrenched idioms: UNSURE, leave |
| 9 | Recent adoptions (wayfind, prototype, dispatch, absorb, adopt, grill, pitch) hold the older bare-verb grain cleanly. Skills have no naming contract at all (noun-noun, verb-noun, bare verbs mixed), so there is no contract to violate. | `commands/wayfind.md`, `skills/*/SKILL.md`; no ADR governs skill names | OK (skills: UNSURE, contract absent by design or by omission; operator call whether one is worth writing) |
| 10 | Lens vocabulary: "lens" dominates (149 occ vs 71 reviewer, 11 specialist, 2 critic in `commands/`), is glossary-blessed, and `devs-team`/`visual-team`/`test-plan-review-team` are internally clean. `review-team.md` mixed all three words for the same slot in its first five lines and its own headings. | `commands/review-team.md:2,5,70,82,110,126,184` (pre-fix) | FIX applied: `review-team.md` unified on "lens" (headings `Lens 1/2/3`, description + body prose). Zero-break verified: the pins in test-advisor/test-review-team-plants grep case-insensitive domain words, all green |
| 11 | `spec-validate.md` promises "6 specialist lenses" then delivers "## The 6 reviewers", two `Critic`s and two ADR-retired `Auditor`s. BUT the headings are pinned literally by `tests/test-meta.sh:1167`, `test-design-record.sh:169`, `test-references-field.sh:144`, and ADR-0029:59 explicitly exempts inline panel labels ("Aligning their labels ... is OPTIONAL / secondary"). | quoted pins above | PROPOSAL only (not zero-break): aligning the labels requires coordinated test edits; fold into the finding-3 rename if it ever runs |
| 12 | `skills/loop-engineering/SKILL.md` alternates "scanner" and "lens" for the same slot six times in one section, and it is the template every generated loop copies. BUT both words are pinned verbatim by `tests/test-loop-engineering-contract.sh:71,74,86` (including in the mutation fixture), so a prose unification is not zero-break. | `"who scans it (N lenses)"` and `"reviser must be distinct from every scanner"` both pinned | PROPOSAL: unify on "lens" (or define scanner=lens in the skill's own vocabulary block) together with its contract test in one diff |
| 13 | ADR-0034 leg->stage: zero stragglers in the live agent surface (`commands/ agents/ skills/ hooks/` all clean); README/architecture/module-registry carry only the sanctioned one-release parenthetical aliases. | `grep -rniE '\blegs?\b' commands/ agents/ skills/ hooks/` -> empty | OK |
| 14 | Eight leg stragglers in prose. Post-rename or present-tense ones fixed; genuinely historical records left alone. | `lib/gate/docs/proof-of-done.md:6` "the Govern leg's engine" (present-tense, its own README already says Check stage); SPEC-215 x3 (DRAFT spec); SPEC-200 x2 blocks (Draft spec); `DECISION-BRIEF-factory-legibility.md:71` (dated 2026-07-31, post-rename) | FIX applied to those six. Left as OK-historical: `lib/sync/docs/proof-of-done.md:36` (a recorded run transcript), `docs/verification/implementation-notes/session-audit.md:31` (entry dated 2026-07-14, pre-rename), shipped `_meta/BACKLOG.md` rows (records of shipped work), and the live homonym "leg" = one direction of a bridge/pipeline (README:382, architecture:23), which must never be swept |
| 15 | "phase" vs "stage" is a correct two-concept split (phase = a lane gate checkpoint, stage = one of the five loop stages), enforced down to `gate-ledger.sh normalize_phase()`, but neither word was defined anywhere. | counts: `commands/` phase 112 / stage 14; `docs/glossary.md:22` used "phase" undefined | FIX applied: glossary rows for both |
| 16 | Glossary coverage: the recent coined terms wayfind, doc-drift, feature-map, loop-engineering, audit-scanner had zero glossary presence; the `-team` suffix likewise. `prototype` is a standard industry word (belongs, if anywhere, in the kept-standard list). | 0 hits each in `docs/glossary.md` pre-fix | FIX applied: six 2-column rows added (matching the `spec-drift` row format). `prototype`: OK, standard word, not added |

## Applied fixes (all zero-break, suites green)

| File | Change |
|---|---|
| `commands/review-team.md` | unified lens vocabulary: description, opening line, step-2 heading, `Lens 1/2/3` headings, "3 specialists"/"all 3 reviewers" prose |
| `lib/gate/docs/proof-of-done.md` | "Govern leg's engine" -> "Check stage's engine (formerly \"Govern leg\", ADR-0034)" |
| `docs/specs/SPEC-215-runs-dashboard.md` | Observe/Execute leg + leg-spanner -> Watch/Build stage + stage-spanner (3 spots, DRAFT spec) |
| `docs/specs/SPEC-200-signal-pipelines.md` | five legs -> five stages; old five-name list -> Shape/Build/Watch/Check/Learn (Draft spec) |
| `docs/briefs/DECISION-BRIEF-factory-legibility.md` | Learn-leg gate -> Learn-stage gate (brief dated post-rename) |
| `docs/glossary.md` | 8 new reader-facing rows: phase, stage, wayfind, doc-drift, feature-map, loop-engineering, audit-scanner, -team |
| `docs/FEATURES.md` | regenerated (description change above + SPEC-221 refs) |

## Proposals for the operator (never applied)

Ranked by prevention value per unit of churn:

- [ ] Harden the naming enforcement (finding 6): roster-scan `agents/*.md` + `commands/*.md` for retired suffixes and derive the positive-axis list from the roster instead of the hardcoded 11 names. One test file; prevents findings 2-4 recurring.
- [ ] Amend ADR-0029 to either sanction `-scanner` as a class or fold `audit-scanner` into the reviewer axis (finding 4).
- [ ] Decide `/kit:spec-validate` -> `/kit:spec-team` (finding 3, 252 files / 599 occ: a dedicated migration PR with a legacy alias window, same playbook as leg->stage).
- [ ] Decide `claim-verifier` (finding 2, 23/69) and the write-capable suffix convergence (finding 5).
- [ ] Loop-engineering scanner/lens unification together with its contract test (finding 12).
- [ ] Optional: a one-line skill-naming convention (finding 9) before the skill roster grows further.
