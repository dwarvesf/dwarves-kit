v2.0.0 , production-facing + cost-measured

The kit-hardening megagoal (the release-hold's condition) is complete, and the kit-face megagoal
ships on top of it: dwarves-kit now presents production-ready and measures its own token cost.

BREAKING , three agents renamed to the -reviewer/-verifier convention (ADR-0029):
  integration-checker -> integration-verifier
  reviewer            -> code-reviewer
  security-auditor    -> security-reviewer
Update any subagent_type dispatch or invocation-template in a consuming repo. (A grep of the
sibling consumer repos found only historical/prose references, no live dispatch wiring.)

kit-face highlights:
  - cheap-first sonnet tier default across three surfaces (SPEC-107)
  - meta-agent provenance (generated-by) + runtime efficacy metric 11 (SPEC-108)
  - capture-gated token accounting: TOKENS ledger + lane-telemetry token section + mermaid (SPEC-110)
  - operator-persona 6th visual-team lens (SPEC-109)
  - UI done-modes + a bounded quiescence loop (SPEC-112)
  - a starter role-specialized agent roster + deterministic 2b-0 worker lookup (SPEC-111)
  - README mermaid lifecycle hero + test-pinned counts (SPEC-113); navigable docs map (SPEC-114)

Three version surfaces (VERSION, .claude-plugin/plugin.json, tool.toml) are aligned at 2.0.0 and
pinned together by test-meta. Full detail: CHANGELOG.md [2.0.0].
