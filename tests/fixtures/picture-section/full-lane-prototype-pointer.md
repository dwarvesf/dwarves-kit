# Spec: fixture -- full-lane spec whose Picture points at a /kit:prototype run
Generated: 2026-07-31
Status: DRAFT
Lane: full

## Problem

Fixture only. Stands in for a UI-shaped full-lane spec that routes `## Picture` to a
`/kit:prototype` branch instead of drawing ASCII, for `tests/test-picture-section.sh`'s
ID-448 routing control.

## Picture
prototype/settings-layout, variant 2: sidebar-first layout with the save button pinned top-right.

## Design
obvious: fixture, not a real design decision.

## Task Breakdown
- [ ] TASK-001: fixture task.

## Verification
`echo fixture-only, no real command`
