name        = "cc-self-improve"
description = "The skill half of the Hermes self-improvement loop for Claude Code: a PreCompact/SessionEnd hook spawns a detached no-write `claude -p` reviewer that drafts a reusable SKILL.md from the session; a trusted bash wrapper stages the draft under ~/.claude/skill-proposals/ (never ~/.claude/skills/) and logs cost. Promote via /skill-review; a curator consolidates + archives (never deletes). Propose-and-stage; the model has no filesystem write. cc-elevation-r4 (2026-06)."
language    = "bash"
status      = "pilot"
entry       = "bin/cc-improve"
created     = 2026-06-19

# Personal/standalone hooks. Wired into ~/.claude PreCompact + SessionEnd + SessionStart at deploy time.
consumers   = []

systems     = ["claude-code"]

secrets     = []

depends_on  = []
